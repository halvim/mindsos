This folder contains the code for MindsOS, a 5-layer intelligence system on FalkorDB metagraphs.

The five domain layers (the "stack"):

- **Layer 1 — Core** (`mindsos_core`). Graphs with nodes, edges, hyperedges; metagraphs (graph of graphs) with graphs as nodes, metaedges, metahyperedges. Plus identity, schema, persistence, reconstruction. **No reasoning.**

- **Layer 2 — Knowledge** (`mindsos_knowledge`). A metagraph where each contained graph is a knowledge role. **Currently shipped (Phase 13):** ontology, lexicon, concepts, alignment:* (canonical `alignment:<a>:<b>` per ADR-0154; D1 reconciliation lands Phase 39), `memories` (renames to `episodic_memories` at Phase 39 per ADR-0044 §am-3), promoted-pipelines, task-patterns, problem-trace, capacity-state. **Phase 43 (Rail A) adds:** parameter-staging (Local), pending-promotions (Local+Global), capacity-gaps (Global), learned-parameters (Local+Global). Closed role-set: 8 → 12 after Phase 43 ships ADR-0150 §am-5. `sense-correlations` **withdrawn** as standalone role-graph (L2 chat D-L2-2; data lives in lexicon empirical-layer). Global (shared) + per-user Local.

- **Layer 3 — Intellectual Capacity** (`mindsos_capacity`). Functions for acquiring and manipulating knowledge — perception, comprehension, derivation, decomposition, combination, path-finding, retrieval, scoring, trace, signalling, interaction, learning-methods. Capacities are fixed-not-learned (state lives in L4). **L1/L3 reframe ships Phases 40-42** retire TYPE_COMPAT + Phase 31 resident infrastructure; introduce bipartite `produces`/`consumes` IntergraphEdges + family-specific dont-know contracts + DataState realm naming + capacity registration contract v2 (ADRs 0155-0159).

- **Layer 4 — Intelligence** (**architecture settled at Chat A 2026-05-28; ships Phases 46-47**). Per-session orchestrator, learner, attention queue, dreaming, replan, promotion proposing. Per Chat A R1 boundary: L4 = substrate + control flow only; all decisions/computations are L3 capabilities.

- **Layer 5 — Mental Model** (**architecture settled at Chat B 2026-05-31; SHIPPED Phase 48 2026-06-09**). Three-sub-MM (knowledge / capacity / intelligence) per task; 6-level chain artifact (HintSet → MappingResult → Plan → Pipeline → PipelineRun → TaskRun); D'1 retention model (version-IRI freeze + pin-at-instantiation + lazy inline-on-retire); episode retained by default at task completion. Note-fork mechanism **retired**.

Plus an **orthogonal Server layer** (`mindsos_server`) that owns auth, sessions, capability-based authorization, audit, persistence orchestration, and lifecycle. **Server is not on the layer-composition axis** — it provides a runtime envelope that any consumer of the domain layers (CLI, future web UI, batch jobs) needs. Domain layers do not import Server (per ADR-0010); Server imports downward into the stack.

Plus a sibling **Instances package** (`mindsos_instances`) per ADR-0132 (2026-04-27) that holds the mental-model instancing vocabulary (`ElementInstance`, `CompositeInstance`, etc.). `mindsos_core` retains backward-compat re-exports during the v4-v5 transition window.

Persistence layout: graphs live in **FalkorDB** (per ADR-0121); non-graph state lives in **SQLite** (`server.db` for auth/sessions/audit, `version_db/` for the pivot release manifest + node versions + peer deps).

---

## For Claude / Cowork chats

**Read `HANDOFF.md` at the root FIRST.** It is the canonical entry point — the current state of MindsOS, the L4/L5 design state (settled vs contested), the 3 sister projects (DWF/WSD/FOL), the carry-forward backlog, and the per-chat-type required-reading map.

**Project status as of 2026-06-09:** Phase 38 closed the L0-L3 numbered-phase rollout. Four foundation chats closed May-June 2026 (Chat A L4 / Chat B L5 / L1-L3 reframe / L2 chat). **Chat C plan-authoring closed 2026-06-02** with `confirmation_docs/POST_PHASE_38_PHASE_MAP.md` — active plan for **Phases 39-49** (4-rail DAG: A rename+schema-v2; B reframe X1/X2/X3; C L0 substrate; D dream family — converging at L4 substrate Phase 46 → orchestrator → L5 → Integration C). **Phase 39 SHIPPED 2026-06-02** (Rail A slot 1; L2 `memories` → `episodic_memories` atomic rename + alignment `:` separator + multi-NodeType dispatch shape; 3501 passed cumulative gate). **Phase 43 SHIPPED 2026-06-03** (Rail A slot 2; L2 schema-v2 — 4 new role-graphs + mutation discipline runtime invariant + per-NodeType storage_mode + bootstrap applies_after field + ADR-0150 §am-5 + ADR-0153 §am-1 + ADR-0094 §am-1 detector + consolidate retarget Memory→Episode + episodic_memories body finalize per Chat B D-B47; ~3690-3700 passed cumulative gate; 18-round design pass closed pre-impl + ~12 impl-time pushbacks tracked in PHASE_43_DESIGN_LOG.md §9). Phase 43 critical reversals (against pre-R0 framing): L2Schema(Schema) subclass placement (not L1 amendment); per-NodeType storage_mode; bootstrap field-only at Phase 43 (Kahn scheduler defers to Phase 44 per L2-37 split); detector form (not migrator); consolidate retargeted at Phase 43 (not deferred to Phase 48). Pair-execution pattern (Cowork ↔ Mac ↔ Linux) established as default for future ship chats. **Phase 44 SHIPPED 2026-06-04** (Rail C; L0 substrate — `FalkorDBLocalPersister` native round-trip + scoped delete; Kahn topological-sort scheduler `kahn_sort` consuming the Phase-43 `applies_after` field + `BootstrapCycleError`; `CAN_READ_OTHER_LOCAL_EPISODIC_MEMORY` + `EVT_READ_OTHER_LOCAL_EPISODIC_MEMORY` roster add; ADRs 0160/0161 + ADR-0011 §am-3; 3630 passed cumulative gate; tag `phase-44-confirmed`). Phase 44 grounding-driven reversals (consumer discipline): CR-2 (both persisters → **Falkor-only**; the `mindsos_cli` state-file serializer is disk-coupled + SQLite had no v1 consumer — SQLite/`MetagraphDump`/serializer-promotion deferred), CR-3 (**MindsOSServer class refactor deferred** — login/logout don't write Locals at v1; orchestrator stays free-function per PB-38), S6 (`read_at_version`/`retire_version` deferred → Phase 48; marker name frozen in ADR-0161) + L2-10 deferred. Pre-existing `admin↔persistence↔mindsos_admin` import cycle documented for post-phase maintenance fix (PHASE_44_DESIGN_LOG §12 + L0_FUTURE_WORK L0-24). Full record: `PHASE_44_DESIGN_LOG.md`. **Phase 40 SHIPPED 2026-06-05** (Rail B slot 1; L3 X1 — ADR-0157 family-specific dont-know contracts + ADR-0158 DataState realm naming: `mindsos_capacity/family_rules.py` (FamilyDontKnowShape + FAMILY_RULES + `family_rule_for`) + 9 `REALM_*`/`RESERVED_REALMS` in `mindsos_capacity/identifiers.py` + `register_datastate` strict realm validation + `allow_new_realm`; 3670 passed cumulative gate; tag `phase-40-confirmed`). Phase 40 grounding-driven scope decisions: REALM_* home corrected to `mindsos_capacity` (NOT `mindsos_knowledge` per the PHASE_MAP row — dissolved the `identifiers.py` collision concern); `DontKnowReason.UNHANDLED_INPUT` **deferred to L4** (no v1 enum/consumer); `DS_UNHANDLED_INPUT` constant-only, no bootstrap node (no builtin DataState is product-registered); FAMILY_RULES transcribed verbatim with a **latent** vocabulary mismatch (`derive`/`signal` vs shipped `derivation`/`signalling` + 7 unkeyed categories — all shipped capacities still classify correctly) routed to the X3 Phase 27 audit. PB-2: confirm-phase **high-water-mark** under the rail DAG (`_phase_exceeds_manifest`) — a slot ≤ the high-water mark (40 ≤ 44) ships with **no version bump**; PHASE_MAP §1 manifest row amended. Full record: `PHASE_40_DESIGN_LOG.md`. **Phase 41 SHIPPED 2026-06-05** (Rail B slot 2; L3 X2 — ADR-0155 Monitor-lifecycle retirement from L3: surgically removed `start_resident`/`stop_resident`/`active_subscriptions` + `_subscriptions` + `ResidentSubscription` + `ResidentError` from `capacity_layer.py`/`runtime.py`/`exceptions.py` (NOT whole-module — those host Phase-30 occupants); `KIND_RESIDENT`→`KIND_MONITOR` (value `"resident"`→`"monitor"`) + `NODE_KINDS`; added `cl.iter_monitors()` producer (no v1 consumer — L4 `MonitorSubscriptionRegistry` lands Phase 46); `__all__` 114→112; 9 phase_31 resident test files deleted + `_fixtures.py` pruned + `tests/phase_41` (4); export-slate flipped 114→112 across phase_29/31/33/34 + membership flipped in phase_31/33; ADR-0073 → Superseded by ADR-0155; 3660 passed cumulative gate; tag `phase-41-confirmed` at `ba7c469`; no version bump slot 41 ≤ 44). Phase 41 grounding-driven scope corrections: "Phase 31 deletes whole" was test-only (9 files; production was surgical); `docs/concepts/monitors.md` is a phantom (PHASE_MAP referenced a non-existent file → doc edits redirected to glossary/summary/internals); grep-zero pass criterion unsatisfiable repo-wide (ADR-0155 self-documents) → retirement sentinel scoped to the shipped package (importability + `mindsos_capacity/**` grep). Full record: `PHASE_41_DESIGN_LOG.md` + HANDOFF §3.1.16. **Phase 42 SHIPPED 2026-06-07** (Rail B slot 3; L3 X3 — **completes Rail B X1→X2→X3**; ADR-0156 bipartite topology + ADR-0159 capacity registration contract v2 + Phase 27 dont-know audit (L3-57) + Model C remediation. `register_capacity` emits explicit `PRODUCES`/`CONSUMES` IntergraphEdges (uppercase rel-type per ADR-0021); `discovery.py`/TYPE_COMPAT/`SuccessorHop`/`DiscoveryFailedError`/`rediscover` retired whole; edge-sourced view walks + bipartite `find_pipeline`; +6 `_CapacityBase` fields + typed `CapacityContext` + 4 Protocols + 5 verdicts in new `context.py` (import-isolated); `mindsos_instances` IntergraphEdge/HyperEdge instances (catalog 8→10); `tools/check_phase_42_bipartite_state.py` detector; `FAMILY_RULES` L3-57 reconciliation (rename derive→derivation/signal→signalling + add consolidate/trace; 5 deferred via `DEFERRED_DEFAULT_CATEGORIES`); ADR-0069/0086 superseded by 0156 + 8 amendment ADRs; `__all__` 112→117; 3669 passed cumulative gate; tag `phase-42-confirmed` at confirm-artifacts `e0a1453` (ship squash `39a312c`; release.yml requires the confirmation doc at the tagged commit — initial tag-at-squash was red, retagged); no version bump slot 42 ≤ 44). Phase 42 grounding-driven deferrals (consumer discipline): **PB-23** capacity-body `context["kl"]`→`context.kl` + `invoke`→CapacityContext plumbing → Phase 46 (ADR-0159 CapacityContext has no session-object field for ADR-0146 write-body gating; L4-boundary); **PB-24** instance `materialise` → Phase 46; **PB-7** migrator→detector (in-memory-first CapacityLayer, no persisted Global capacity state); **PB-16** mkdocs `--strict` re-scoped (17 pre-existing broken-link warnings → docs-maintenance); **PB-22** edge values uppercase; parity fixes (6 fields not 5; 10-field CapacityContext not 9). Full record: `PHASE_42_DESIGN_LOG.md` + HANDOFF §3.1.17. **Phase 45 SHIPPED 2026-06-07** (Rail D; L3 dream family ratification — **closes the last pre-convergence rail**; combined design+ship option-C via DREAM_FAMILY_CHAT. ADR-0162: 3 directive-emitter dream capacities `dream.maintenance`=`replay_recorded`/`dream.exploration`=`re_execute_capacities`/`dream.retry`=`re_execute_capacities`+replan-injection; new `DreamCapacity(_CapacityBase)` kind (`execution_policy`+`entry_point`); `CATEGORY_DREAM` lazy-installed, NOT in `FUNCTIONAL_CATEGORIES` per text.* precedent; `builtins/dream.py` (`DreamExecutionPolicy` 2-value + `DreamDirective` + `ReplanInjectionDirective` + 2 DataStates + idempotent installer); `__all__` 117→118; **9-surface version bump 44→45 — first slot > high-water 44**; 3694 passed cumulative gate; tag `phase-45-confirmed` at confirm-artifacts `e76a1a3` (ship squash `ab32e3d`)). Phase 45 consumer discipline: dream bodies have no v1 L3 consumer (L4 dream-cycle timer + MM deep-copy + live re-execution + ALS firing → Phase 46/47/48); pre-provisioned `FAMILY_RULES['dream']`/`REALM_DREAM` needed zero edits; `dream_source_episode_iri` ships as a directive field (live signal tagging Phase 48). **All four rails (A 39/43, B 40/41/42, C 44, D 45) now closed — Phase 46 (L4 substrate convergence) can open R0.** Full record: `PHASE_45_DESIGN_LOG.md` + HANDOFF §3.1.18. **Phase 46 SHIPPED 2026-06-08** (convergence; **first L4 code** — NEW top-level package `mindsos_intelligence` (8th): IntelligenceLayer lifecycle + priority-tier Executor + worker pool + writer-preferred MM RWLock + three-sub-MM container + deep-copy + MM resolver as concrete `MMHandle` + cooperative cancellation + signal-triage thread (passthrough stub) + ALS subsystem registry + MonitorSubscriptionRegistry + dream-cycle timer; L3 `mindsos_capacity/tiers.py` `TierEnum` + `TierVerdict.tier` narrow; `mindsos_instances` `materialise` for the 2 intergraph instance subclasses; ADRs 0163-0170; 9-surface bump 45→46 + manifest 8th package; 3793 passed cumulative gate; squash `47c3568`, confirm `18ba793`, tag `phase-46-confirmed`). Phase 46 grounding-driven decisions: **PB-8** TierEnum home = L3 not L4 (layer isolation test-enforced); **S12 deferred wholesale to Phase 47** (`invoke`→CapacityContext + body migration + write-gate flips a corpus-wide signature with no Phase-46 caller — PB-23 closes at 47); single PR (PB-0 two-PR plan collapsed once S12 + the L3 signal-classifier deferred). Carry-forward to Phase 47 (L4 orchestrator): six-phase lifecycle + `planning.*` v0 + consume the deferred surfaces; Phase 48: dream live re-execution + MM `attention_score` write-through + inline-on-retire (D'1). Full record: `PHASE_46_DESIGN_LOG.md §9` + HANDOFF §3.1.19. **Phase 47 SHIPPED 2026-06-08** (convergence; L4 orchestrator over v0 catalogs — six-phase lifecycle `orchestrator.py` + `phase_1`/`plan_construction`/`execution`/`phase_6`/`replan_check`/`sufficient_predicate` + `dispatch.py` L4Dispatcher + `chain_artifacts.py` (8 chain types) + 10 signal-source + 11 ALS skeletons + v0 catalogs `builtins/{planning_v0,phase1_v0,orchestration_v0}.py`; ADRs 0171-0175; 9-surface bump 46→47; 3832/9/0; squash `cd8abb0`/Linux-canonical `6f49524`, confirm `db1a562`, tag `phase-47-confirmed`). S12 split read/write (ADR-0175 §am-1: read-half at 47, write-half → 48); dream driver deferred → 48; worker-per-task lifecycle (ADR-0171). Ship-closure completed at the start of the Phase-48 chat. Full record: HANDOFF §3.1.20 + `PHASE_47_DESIGN_LOG.md`. **Phase 48 SHIPPED 2026-06-09** (final convergence; L5 v1 — MM consolidation → Episode + Memory + `MEMORY_CONTAINS_EPISODE` edge on all terminal paths (`mindsos_intelligence/consolidation.py`, ADR-0176); **S12 write-half closed** via the pre-authorized scope-aware `writeable` capability on `CapacityContext` built by a shared `make_writeable` (ADR-0180; gate travels with the capability — L4 dispatch for tasks, `capacity_layer.invoke` write-branch for the CLI; ADR-0146/0170 preserved; **PB-23 authorization half done**); full D'1 KL stack `kl.read_at_version`/`retire_version` + `_retired_inline_pending` marker (ADR-0177 + ADR-0161 §am-1 — Phase 44 had shipped none of it); D'1 read consumer `retention.py` (unit-test-only at v1, PB-9); dream-cycle driver `dream_cycle.py` (ADR-0178; v1 re-runs from episode `task_input`); crash recovery `crash_recovery.py` tombstone markers + startup scan (ADR-0179); retention monitoring `monitoring.py` (instrumentation only); 3 concept docs; ADRs 0176-0180 + amendments 0146/0161/0170/0175; 10-surface bump 47→48; 3863/10/0; squash `af331e8`, confirm `1952260`, tag `phase-48-confirmed`). Phase 48 grounding-driven decisions: **A1′** (the CLI `mindsos capacity invoke` is a write path → shared gate factory + `capacity_layer.invoke` write-branch; read-path dict + union annotation kept one more phase); **PB-10** Local-write gate fix (Phase-47 blanket pre-gate would have denied a normal user's Local consolidate); **PB-9** dream re-runs from `task_input` not full reconstruction (→ S7 unit-test-only). Deferred → Phase 49 / WSD / v1.5: union-drop + read-path CapacityContext migration; faithful episode→MM reconstruction + `replay_recorded` differentiation + real ALS firing (WSD); partial-MM crash-content recovery + retention policy + durable Falkor checkpoint store (v1.5). Phase 48 full record: `PHASE_48_DESIGN_LOG.md` + `PHASE_48_CONFIRMED.md` + HANDOFF §3.1.21. **Phase 49 SHIPPED 2026-06-09** (Integration C — the **LAST numbered phase**; composes shipped L0–L5 into one end-to-end trivial-task scenario test (`tests/phase_49/`) + `usage/cookbook/end-to-end.md` + ADR-0181 Falkor index strategy **decide-and-document** (PB-HHH; **zero index code**; physical creation routed to WSD retrieval) + 10-surface bump 48→49; no new feature surface). Probe findings: `text.tokenize`→`text.space_split` (the shipped capacity); the v0 lifecycle dispatches **no real L3 capacity** (`execution.py` emits a notional step), so the scenario is **two stitched slices** sharing one session+KL (PB-1a). **PB-RT** (R2 reanalysis): the L0 node persister stores node `value` as a primitive, but the L5 Episode `value` is a dict — so episode-flush-to-Falkor would error; **descoped** the live episode flush (exercise the Phase-44 machinery via the proven Global-pair round-trip; Episode asserted in-memory) and routed the durable-episode-persistence gap to **L0-26**. Live integration scenario ran+passed at the gate; 3868/11/0; squash `149fb26`, confirm `cc8a7f8`, tag `phase-49-confirmed`. Full record: `PHASE_49_DESIGN_LOG.md` + `PHASE_49_CONFIRMED.md` + HANDOFF §3.1.22.) **ALL Phases 39–49 SHIPPED — the post-Phase-38 plan is COMPLETE. Next chats = the downstream sequence (SKILL_ACQUISITION_PROCESS → WSD / FOL / code-skill / adapter; L4-v2 follow-up; MAINTENANCE; DWF parallel) per `POST_PHASE_38_PHASE_MAP.md §6`.**

**S9 BLOCKER RESOLVED 2026-06-02.** A0 housekeeping (4 commits A0-1…A0-4) + Stream A items A9 + A1 all landed on `main`. `main`-tip = `f33db02`; tagged `a0-corpus-landed`. Pre-A0 baseline retained at tag `pre-a0-backup` (`5236857`). Cumulative gate result: 3429 passed / 8 skipped / 0 failed; `mkdocs build` clean. Phase 39 hard prereqs satisfied; `phase-39` can branch off `main`-tip. Forensic landing trace + 3 incidental surface findings (Dockerfile drift, missing `docs/dev/internals/knowledge.md` vendor, stale `tests_server/` layer-isolation roster) at `HANDOFF.md §3.1.10`.

Both Stream B rail-prerequisite design chats are now CLOSED: `L0_SUBSTRATE_CHAT` (absorbed into Phase 44 R0, SHIPPED 2026-06-04) and `DREAM_FAMILY_CHAT` (absorbed into Phase 45 R0, SHIPPED 2026-06-07). **All four rails (A/B/C/D) closed — the next chat is Phase 46 (L4 substrate convergence); see `confirmation_docs/PHASE_45_NEXT_CHAT_PROMPT.md`.** Five downstream installation chats are sequenced post-Phase-49: `SKILL_ACQUISITION_PROCESS_CHAT`, `WSD_INSTALLATION_CHAT`, `FOL_INSTALLATION_CHAT`, `DWF_INSTALLATION_CHAT` (parallelizable; L2-only), `CODE_SKILL_INSTALLATION_CHAT`, `ADAPTER_FAMILY_CHAT`, `MAINTENANCE_CHAT`, `L4-v2 follow-up chat`. See `POST_PHASE_38_PHASE_MAP.md §6` for sequencing.

**Sister projects:** `projects/dwf_mapping/` (knowledge acquisition), `projects/wsd/` + `projects/fol/` (skill acquisition). Each has an `ANALYSIS.md` + `FUTURE_CHAT_PROMPT.md` + `source/`. See `projects/README.md` for the recommended chat ordering.

**Folder structure:** See `HANDOFF.md` §7.3 for the canonical layout.

**Note on naming:** This folder is `MindsOS/` locally; the git origin remote is `git@github.com:halvim/mindsos.git` (GitHub repo name `halvim/mindsos` — discrepancy intentional, see HANDOFF.md §7.1).

## Cowork project setup

When you (or a fresh chat) first open this folder:

1. In Claude desktop app → Cowork mode → Add new project → point at this MindsOS/ folder.
2. The CLAUDE.md (you are reading it) loads automatically as project instructions.
3. Read `HANDOFF.md` at the root.
4. For mkdocs preview: `pip install mkdocs mkdocs-material && mkdocs serve` → http://127.0.0.1:8000.

## Memory

Cowork memory is **per-project**. If this folder is opened as a fresh Cowork project (different from "Layered Intelligence" where the housekeeping was done), the memory entries don't migrate. `HANDOFF.md` is fully self-contained for this reason — memory `[[name]]` pointers in any doc are optimizations, not load-bearing.
