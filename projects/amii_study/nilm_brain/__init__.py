"""NILM leaf-learning brain — a standalone consumer of MindsOS (amii study).

Registers the full NILM application-doc registry (DataStates §7, capacities
§6, the cycle recognition pipeline §6.1) into a pinned core and recognizes
grid cycles by dispatching L3 capacities the finder composes and core's
`execute_pipeline` runs. No `mindsos_*` edits. See README.md for scope + gate.
"""

__version__ = "0.0.0+nilm-v0"
