"""Baseline arms for the study (torch/Avalanche live here, never core).

Import concrete arms directly (e.g. ``from study.baselines.cnn import CNNArm``)
so that torch is only pulled in when a torch-based arm is actually used.
"""
