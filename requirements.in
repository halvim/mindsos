# mindsos_cli runtime dependencies (Phase 00).
# Edit this file when adding/removing top-level deps; then run tools/lock.sh
# to regenerate requirements.txt with hashes.
#
# Phase 00 keeps the surface intentionally small. Later phases add deps for the
# packages they bring online (mindsos_core, mindsos_knowledge, etc.).

typer>=0.12,<1.0
falkordb>=1.6.1,<2.0

# Phase 15a — admin importers parse external knowledge sources:
# * rdflib parses DOLCE OWL (RDF/XML, Turtle).
# * lxml accelerates OEWN-LMF + FrameNet XML; stdlib xml.etree fallback supported.
rdflib>=7.0,<8.0
lxml>=5.0,<6.0

# Phase 18 — argon2id password hashing for mindsos_server per ADR-0003
# + Phase 18 PB-14. tools/lock.sh reads requirements.in (NOT pyproject.toml)
# per `feedback_lock_sh_reads_requirements_in.md`; the dep MUST land in
# BOTH this file AND pyproject.toml to produce a Docker install that ships
# argon2-cffi.
argon2-cffi>=23.0,<24.0
