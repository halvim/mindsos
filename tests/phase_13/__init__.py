"""Phase 13 — L2 Schemas (alignment / lexicon / ontology / concepts +
5 upper-layer schemas; ontology HyperEdgeType lift; dispatch +
schema_for_role; CLI schema show + schema validate).

Closes the L2 schema dispatch table per PB-1 — all 8 builders + the
alignment parametric branch + the dispatch function. Per PB-3 /
ADR-0149, all 8 schemas ship at strict=False.
"""
