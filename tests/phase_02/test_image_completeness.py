"""Phase 02 image-completeness — RELOCATED in Phase 03.

The image-completeness regression test moved to
``tests/test_image_completeness.py`` (root, cumulative) in Phase 03 with
the sentinel list extracted to ``tests/_shared/sentinel_paths.py``.

This stub remains so anyone with a pinned `pytest tests/phase_02/test_image_completeness.py`
selector sees a clear pointer instead of a collection error. The
relocated test runs as part of the standard `pytest tests/` cumulative
sweep in CI.
"""
