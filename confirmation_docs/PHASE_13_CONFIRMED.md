# Phase 13 — Confirmation

> Generated by `mindsos confirm-phase --phase 13 --notes-file …`. Tester
> may hand-edit before commit. CI's smoke check verifies "exists and
> non-empty", not field structure.

---

## phase_number

13

## phase_title

The phase title as it appears in `confirmation_docs/PHASE_MAP.md` §3 / §4 / §5.
Example: `Tooling infrastructure`

L2 Schemas — 9 role-graph builders + dispatch + alignment parametric (ADR-0149 strict=False lock; ADR-0150 reserved for Phase 14a)

## git_sha

2e959824a649cc69befcc8e6b5d0610221364dd1

## image_build_hash

sha256:41c41fa75f4db9be54483076e97e3d23274fb43457a3db3c02440e357254970a

## falkordb_version

falkordb/falkordb:v4.18.3@sha256:30c530c193ac48cb6ea8c6cae745f793d2c098a0a138f7b3e46c1d90848845ba

## automated_test_summary

- count: 2037
- passed: 2027
- skipped: 10
- failed: 0
- suite_hash: sha256:ae148644e82b5580858f335903ae8e219877e58cbddadf86576df06d16539614
- pytest_summary: 2027 passed, 10 skipped, 109 warnings in 1103.33s (0:18:23)

## tester_notes

Free-form. What you observed, anything surprising, deviations from PHASE_MAP's
pass criterion, open questions for the next phase chat. This is the
load-bearing field — read by future phase chats per PHASE_MAP §0.

══════════════════════════════════════════════════════════════════════
Phase 13 — L2 Schemas (8 named-role schemas + alignment parametric).
SHIPPED 2026-05-18.
══════════════════════════════════════════════════════════════════════

## Headline numbers

* Phase 13 isolated suite: 127 passed + 3 skipped (130 collected) in
  13.5s. The 3 skipped are ADR-amendment-sentinel tests
  (test_adr_amendment_sentinels.py::*) — ADR files live in the parent
  project tree (/Layered Intelligence/docs/decisions/adr/) outside
  the runtime container per Model C (mirrors Phase 12's ADR-0044
  amendment-1 skip pattern).
* Cumulative: 2027 passed + 10 skipped + 0 failed + 109 warnings in
  1107.42s (18m27s). Cumulative delta from Phase 12 (1890): +137
  (+127 Phase 13 isolated + 10 fanout from root-level
  test_image_completeness.py parametric over the 10 new sentinel
  paths). 0 prior-phase test patches needed across 11 Step-0 probes
  — third clean streak (Phase 11 + 12 + 13).
* 4-package version parity (no new top-level pkg this phase):
  mindsos_core / mindsos_cli / mindsos_instances / mindsos_knowledge
  all at 0.0.0+phase13; manifest.toml [mindsos] phase = "13";
  image tags mindsos:phase13-{prod,test}.

## What landed (mapped to PB-1..27 + Phase 14a row insertion)

* mindsos_knowledge/schemas/ NEW sub-package (10 files) per PB-1
  8-schema closure of the L2 dispatch table:
  - 4 seed roles (v3 verbatim ports): ontology, lexicon, concepts,
    alignment (parametric).
  - 5 NET-NEW upper-layer at strict=False per ADR-0149:
    promoted_pipelines, task_patterns, memories, problem_trace,
    capacity_state.
* Ontology HyperEdgeType lift per PB-4 — 7 v3 label-constants
  registered as HyperEdgeType instances (INTERSECTION_OF / UNION_OF /
  ONE_OF / PROPERTY_CHAIN / DISJOINT_UNION_OF / ALL_DISJOINT_CLASSES /
  ALL_DIFFERENT). Ordering semantics for PROPERTY_CHAIN stay at
  HyperEdge instance level (Phase 04-v2 HyperEdgeType has no
  `ordered` field — symmetric across members).
* schema_for_role(role, strict=False) -> Schema dispatch with
  alignment-prefix branch per PB-5 + PB-11; raises UnknownRoleError
  on miss with a message naming sorted(ALL_ROLES) + the alignment
  prefix hint.
* UnknownRoleError(KnowledgeError) NEW exception class per PB-11 —
  symmetric with Phase 11's UnknownEdgeTypeError discipline.
* mindsos knowledge schema {show, validate} CLI sub-subgroup per
  PB-6:
  - `show --role <role> [--json]` prints NodeTypes / EdgeTypes /
    HyperEdgeTypes / strict flag.
  - `validate --role <role> --graph-file <path> [--json]
    [--exit-zero]` runs L1 structural pass over state-file using
    canonical node_id / edge_id keys per B-11-T2 lock; semantic
    validation deferred to Phase 36 (ADR-0139).
* Alignment `extra_edge_types` kwarg retained per PB-14 — vocabulary
  is intentionally open.
* Advisory property constants per upper-layer schema per PB-8 —
  module-level frozenset constants, NOT in NodeType.property_types.
  Strict-tighten PR converts to typed declarations per ADR-0149
  §Revisions discipline.
* Dimensional snapshot sentinel per PB-17 — EXPECTED_DIMENSIONS
  table pins each schema's exact (nodes/edges/hyperedges) counts.
  Parametric over 9 schemas. Any future edit forces explicit table
  bump. (B-13-T1 caught a 21-vs-22 typo I had on lexicon — see
  hotfix ledger.)
* strict=False sentinel test per PB-3 parametrises over all 9
  builders; ADR-0017 §amendment-1 documents the 2-week-no-edit
  tightening rule.
* PHASE_MAP 6-site edit per PB-22 + PB-24 in ONE commit:
  - §1 NEW "design-only phases are an exception" clause per PB-20.
  - §Phase 13 row full rewrite.
  - §Phase 14a row insertion (new design-only phase).
  - §Phase 14 deps amended: 12, 13 → 12, 13, 14a.
  - §3 phase index table extended.
  - §1 phase count: 43 → 44.
* PHASE_14a_NEXT_CHAT_PROMPT.md handoff written per PB-26 — Phase 14a
  drafts ADR-0150 content + 3 lifecycle docs + 6 downstream
  PHASE_MAP row amendments; no code; no tag; no confirm-phase
  invocation.
* Import-isolation regression test extended per PB-18 — AST walk
  over mindsos_knowledge/schemas/*.py confirms no imports from
  mindsos_cli or mindsos_server.

## Hotfix ledger (B-13-T*)

* B-13-T1 — lexicon dimensional-snapshot off-by-one (21 → 22).
  test_dimensional_snapshot[lexicon] failed at first run with
  AssertionError: expected 21 edge types, got 22. Root cause: I
  miscounted LEXICON_EDGE_TYPES in EXPECTED_DIMENSIONS (it's
  actually 22 entries: 2 structural + 4 taxonomic + 6 part/whole +
  2 verb-synset + 3 misc synset + 4 sense-level + 1 example). Fix:
  2-file edit to tests/phase_13/test_dimensional_snapshot.py and
  docs/usage/knowledge/lexicon.md. One commit (amended); re-run
  produced 127 passed / 3 skipped. Pre-impl PB-25 API probe DID
  verify the API surface but did NOT cross-check the count against
  my hand-tabulated table — adding a "cross-check EXPECTED_DIMENSIONS
  against actual builder output during pre-impl probe" step to the
  audit class would prevent this. New feedback memory candidate:
  feedback_dimension_table_cross_check.md.

## Manual smoke verification

11 smoke boxes covered, all green:

* Smoke 1 — `mindsos knowledge schema show --role lexicon` →
  4 NodeTypes + 22 EdgeTypes + strict=False; exit 0.
* Smoke 2 — `mindsos knowledge schema show --role memories --json`
  → JSON with Memory NodeType + PART_OF_PIPELINE + USED_CAPACITY
  edges; exit 0.
* Smoke 3 — `mindsos knowledge schema show
  --role 'alignment:lexicon<->concepts' --json` → alignment-prefix
  branch returns parametric schema (1 AlignmentAnchor + 8 mapping
  edges); exit 0.
* Smoke 4 — `mindsos knowledge schema show --role not-a-real-role`
  → UnknownRoleError on stderr with valid-role list + alignment
  prefix hint; exit 1.
* Smoke 5 — `mindsos knowledge schema validate --role lexicon
  --graph-file tests/phase_13/fixtures/lexicon_happy.json` →
  OK message; exit 0. (Invoked via mindsos-test image — prod image
  doesn't include tests/.)
* Smoke 6 — `mindsos knowledge schema validate --role memories
  --graph-file tests/phase_13/fixtures/memories_bad.json` → 2
  violations (unknown_node_type + unknown_edge_type); exit 1.
* Smoke 7 — same as Smoke 6 with `--exit-zero --json` → JSON
  payload with ok=false + violation_count=2 + violations array;
  exit 0 (flag flips exit code).
* Smoke 8 — `mindsos doctor --self-test --json` → ok=true; all 4
  packages at 0.0.0+phase13 (mindsos_core, mindsos_cli,
  mindsos_instances, mindsos_knowledge).
* Smoke 9 — `mindsos knowledge --help` lists 4 subcommands
  (iri / ref-types / roles / schema) — schema sub-subgroup
  registered cleanly.
* Smoke 10 — `mindsos knowledge schema show` for all 5 NET-NEW
  upper-layer roles (promoted-pipelines / task-patterns / memories /
  problem-trace / capacity-state). Dimensions match EXPECTED:
  promoted-pipelines 2/2/0; task-patterns 2/2/0; memories 1/2/0;
  problem-trace 1/0/0; capacity-state 1/0/0.
* Smoke 11 — Phase 12 regression check: `mindsos knowledge iri
  build --role memories --kind memory --version 1 --user-id alice
  --memory-id m1` → `memories-1:memory:alice:m1`. Phase 12 surface
  unmutated by Phase 13's schema sub-subgroup addition.

## Phase 14a process exemption (PB-20 lock — first design-only phase)

Phase 14a is the FIRST phase under the §1 design-only-phases
exception clause Phase 13 added. Properties:
* No `phase-14a-confirmed` tag.
* No `mindsos confirm-phase` invocation.
* No version bump (the 4-pkg parity stays at +phase13 through the
  Phase 14a PR window).
* Downstream Phase 14 branches off main-tip after the Phase 14a
  docs PR merges — NOT off a tag.
* Deliverables: ADR-0150 (Reserved → Accepted), 3 lifecycle docs,
  6 downstream PHASE_MAP row amendments + §2 row-schema
  "Lifecycle role" sub-field amendment.

PHASE_14a_NEXT_CHAT_PROMPT.md captures the locks Phase 14a inherits.

## Cross-chat dependencies

### Closed (Phase 12 → Phase 13)

* phase-12-confirmed tag (commit a17307e) was the Phase 13 branch
  point.
* All Phase 12 surfaces (14 IRI builders, REF_TYPES, ref-key
  helpers, 8 role constants, parser, CLI iri / ref-types / roles
  verbs) unmutated by Phase 13.

### Forward (Phase 13 → Phase 14a → Phase 14+)

* L2 Phase 14a (design pass): drafts ADR-0150 content from the
  reservation stub; amends PHASE_MAP §14 / 15 / 16 / 17 / 24 / 37
  rows with the new "Lifecycle role" sub-field; writes 3 lifecycle
  docs.
* L2 Phase 14 (KL bootstrap): consumer for schema_for_role +
  _ROLE_SCHEMA_BUILDERS dispatch dict + UnknownRoleError. First
  MetagraphSchema-bump candidate (Phase 11 PB-7 C / Phase 12 PB-5 /
  Phase 13 PB-2 re-carry-forward). Owns the per-edge alignment
  anchor IRI builder decision (Phase 12 PB-4 / Phase 13 PB-5
  re-carry-forward).
* L2 Phase 15 (Importers): consumer for the 4 seed-role schemas
  (DOLCE → ontology, OEWN → lexicon, FrameNet → concepts,
  Alignments → alignment). Drives ADR-0134 Proposed → Accepted
  flip (Phase 11 PB-7 / Phase 12 PB-5 / Phase 13 carry-forward).
* L2 Phase 16 (Promotion): consumer for promoted_pipelines +
  task_patterns schemas.
* L0 Phase 18 (Server user store): inherits user_id charset (via
  ADR-0044 §amendment-1 from Phase 12).
* L0 Phase 25 (SessionProtocol seam): consumer for the
  import-isolation invariant Phase 12 PB-18 established + Phase 13
  PB-18 extended to schemas/*.
* L3 Phase 27 (DataStates): owes REF_TYPES parity test (Phase 12
  PB-3 carry-forward).
* L3 Phase 28 (12 categories): consumer for capacity_state schema.
* L3 Phase 30 (Pipeline finder): consumer for problem_trace schema.
* L2 Phase 36 (Hybrid validators, NEW CODE per ADR-0139): semantic
  validation layer that builds on Phase 13's L1 structural pass in
  the CLI `validate` verb.

## Carry-forward (out of Phase 13 scope; re-carried per PB-2/5/etc.)

* MetagraphSchema scanner → Phase 14 (KL bootstrap; first
  MetagraphSchema-bump candidate).
* Per-edge alignment anchor IRI builder → Phase 14 (KL bootstrap
  alignment-graph wiring).
* ADR-0134 Proposed → Accepted flip → Phase 15 (Importers).
* docs/dev/migration-playbook.md full content → Phase 15.
* ADR-0134 §amendment-3 → reserved for first KL consumer's
  structural feedback (Phase 15).
* Per-builder inverse field helpers (capacity_snapshot, pipeline,
  task_pattern, memory, problem_trace) → per-consumer phase
  (Phase 16 / 28 / 30 etc.).
* REF_TYPES L3 parity test → Phase 27.
* Server-side user_id charset enforcement → Phase 18.
* `strict_support.py` inventory helper (legacy §4.5) → first
  per-role strict-tightening phase (post-Phase-15 seed roles;
  post-Phase-30 upper-layer roles per consumer dates).
* Schema property-type strict declarations for the 5 upper-layer
  schemas (PB-8 advisory frozenset constants → typed declarations
  on NodeType) → first-tightening PR per ADR-0149 §Revisions.
* ADR-0150 content → Phase 14a (Phase 13 reserved the number only
  per PB-23).
* 3 lifecycle docs + 6 PHASE_MAP row amendments → Phase 14a.
* Real-user state-file access for `schema validate` (currently
  smoke uses test-image bind because prod image excludes tests/) →
  Phase 14a or Phase 26 (Integration A).

## ADR matrix (Phase 13 touches)

| ADR | Pre-Phase-13 | Phase 13 action |
|---|---|---|
| 0010 | Accepted | No edit; PB-18 isolation test extends Phase 12 to schemas/*. |
| 0014 | Accepted | No edit. |
| 0017 (Schema strictness opt-in) | Accepted | + §amendment-1 documenting L2 role-schemas at strict=False + 2-week tightening rule per PB-3. STAYS Accepted. |
| 0044 (memories Local + user_id) | Accepted | No edit. |
| 0045 | Accepted | No edit; closed in Phase 12. |
| 0047 | Accepted | No edit. |
| 0066 | Accepted | No edit. |
| 0067 | Accepted | No edit; L3 parity test deferred to Phase 27. |
| 0134 | Proposed | No edit; STAYS Proposed; re-carry to Phase 14/15. |
| 0139 (L2 hybrid validators home) | Proposed (Phase 36) | No edit; Phase 13 CLI validate ships L1 structural only. |
| **0149** (L2 role-schemas strict=False + 2-week rule) | (NEW) | **Accepted in Phase 13** per PB-7. |
| **0150** (L2 knowledge lifecycle) | (NEW) | **Reserved in Phase 13** per PB-23; content drafted in Phase 14a. |

## Next action

Awaits squash-merge of phase-13 → main, then
`git tag phase-13-confirmed <main-sha>` per
feedback_release_tag_after_squash_merge_only.md (tag MUST point at
the main commit containing this CONFIRMED doc + the B-13-T1 hotfix,
not the phase-13 branch tip).

After phase-13-confirmed is pushed and release.yml is green, open
Phase 14a using confirmation_docs/PHASE_14a_NEXT_CHAT_PROMPT.md
(design-only phase; no tag).
══════════════════════════════════════════════════════════════════════

## timestamp_utc

2026-05-18T22:24:51Z

## mkdocs_pages_updated

- docs/api/core/client.md
- docs/api/core/integrity.md
- docs/api/core/loaders.md
- docs/api/core/metagraph-snapshot.md
- docs/api/core/metagraph.md
- docs/api/core/repositories.md
- docs/api/core/soft-delete.md
- docs/api/core/wal.md
- docs/api/core/xref.md
- docs/api/knowledge/identifiers.md
- docs/api/knowledge/ref-types.md
- docs/changelog/CHANGELOG.md
- docs/concepts/identifiers.md
- docs/concepts/identity.md
- docs/concepts/references.md
- docs/concepts/soft-delete.md
- docs/dev/internals/core.md
- docs/dev/internals/snapshots.md
- docs/dev/migration-playbook.md
- docs/dev/repo-layout.md
- docs/usage/core/persistence.md
- docs/usage/knowledge/alignment.md
- docs/usage/knowledge/capacity-state.md
- docs/usage/knowledge/concepts.md
- docs/usage/knowledge/iri-cli.md
- docs/usage/knowledge/lexicon.md
- docs/usage/knowledge/memories.md
- docs/usage/knowledge/ontology.md
- docs/usage/knowledge/overview.md
- docs/usage/knowledge/problem-trace.md
- docs/usage/knowledge/promoted-pipelines.md
- docs/usage/knowledge/task-patterns.md
