# Phase 30 — Confirmation

> Generated by `mindsos confirm-phase --phase 30 --notes-file …`. Tester
> may hand-edit before commit. CI's smoke check verifies "exists and
> non-empty", not field structure.

---

## phase_number

30

## phase_title

The phase title as it appears in `confirmation_docs/PHASE_MAP.md` §3 / §4 / §5.
Example: `Tooling infrastructure`

L3 Invocation + BFS Finder + ProblemTrace

## git_sha

06ea0c10157885a27e318da0e6fb7f7c4aa8bbfd

## image_build_hash

sha256:ebbf4e0710d7b0ab979850c6d0f5246b838a7deadd94a3d8099489124a3dbaec

## falkordb_version

falkordb/falkordb:v4.18.3@sha256:30c530c193ac48cb6ea8c6cae745f793d2c098a0a138f7b3e46c1d90848845ba

## automated_test_summary

- count: 3218
- passed: 3173
- skipped: 45
- failed: 0
- suite_hash: sha256:abebf8c09ef14dd5b2297098ac392fe5092efcae386dde63bd8999b02903e247
- pytest_summary: 3173 passed, 45 skipped, 109 warnings in 1924.50s (0:32:04)

## tester_notes

Free-form. What you observed, anything surprising, deviations from PHASE_MAP's
pass criterion, open questions for the next phase chat. This is the
load-bearing field — read by future phase chats per PHASE_MAP §0.


### Background

Fourth L3 ship per PHASE_MAP §30. Adds 2 NEW `mindsos_capacity/` modules (`runtime.py` + `pipeline.py`) + `CapacityLayer.invoke` method + `self.problem_trace` attribute. Lifts `InvocationResult` + `call_capacity` exports (existed since Phase 27 for layout parity; public re-export new at Phase 30 per ADR-0066 §Implementation footer staging closure). Adds 2 new raisers (`PipelineNotFoundError`, `ProblemTraceError`) — 5→7 exception classes. Adds `mindsos capacity` CLI subapp with two verbs: `find` + `problem-trace tail` (NO `invoke` verb at Phase 30 per R3 PB-36 — deferred to Phase 31 alongside text builtins).

Residents (`start_resident` / `stop_resident` / `ResidentSubscription` / `ResidentError`) deliberately deferred to Phase 31 per PHASE_MAP §31 + ADR-0073. No new top-level package; no new admin/server surface.

### Design saturation

Six design rounds (R0-R5 with R4 = pre-impl probe execution). Total ~56 picks. R4 7-probe checklist obsoleted ZERO design picks (4 R4 sub-cases tightened R3 picks). R5 ship-lock declared no self-reversal warranted on R4's parent-divergent trims (R4 PB-46/47/48). Key locks: InvocationResult shape preserved with ADR-0072 §amendment-1 documenting the field rename; finder takes `session: SessionArg = None` per Phase 28 R1 PB-14 lock; CLI uses fresh in-memory layer per invocation (R2 PB-27(a)); single ProblemTraceSink per layer per ADR-0074 literal. Full pick ledger at `confirmation_docs/PHASE_30_DESIGN_LOG.md`.

### Ship surface

Source NEW (2): `mindsos_capacity/{runtime,pipeline}.py`. EDITED (3): `exceptions.py` (+2 classes), `capacity_layer.py` (+invoke method + problem_trace attribute + 3 imports), `__init__.py` (+11 exports = 84→95 + docstring rewrite + version bump). CLI NEW (1): `mindsos_cli/commands/capacity.py`. CLI EDITED (1): `mindsos_cli/app.py` (register_capacity_app). 21 NEW test files in `tests/phase_30/` totaling 52 cases. 2 Phase 28 sentinel flips: `test_invocation_not_exported.py` → file-renamed to `test_invocation_exported_phase_30.py`; `test_problem_trace_attribute_not_present_at_phase_28` → function-renamed to `test_problem_trace_attribute_present_at_phase_30`. 12-site version bump complete. Sentinel-paths +2.

### ADR amendments (parent tree per Model C; no .git)

5 touches across 4 ADRs in parent `/Layered Intelligence/docs/decisions/adr/`: ADR-0066 §Implementation (Phase 30 — InvocationResult + call_capacity export lift closure); ADR-0071 §Implementation (Phase 30 — datastate-keyed BFS shipped; halvim divergences); ADR-0072 §amendment-1 + §Implementation (field rename `failed`→`success` polarity-inverted; `exception`→`error`); ADR-0074 §Implementation (in-memory anomaly-only sink per-layer).

### Hotfix B-30-T1 (closed)

Sentinel-flip class extension. `tests/phase_29/test_phase_29_export_slate.py` shipped two Phase 29 sentinels asserting Phase 30 surface NOT exported (`test_phase_29_does_not_export_phase_30_surface`) and export count in 82-86 range (`test_phase_29_export_count_around_84`). Both naturally invert at Phase 30. Flipped per `[[feedback-parity-test-sentinel-flip-at-target-phase]]` into `test_phase_30_surface_exported_at_phase_30` + `test_phase_30_export_count_is_95`. Class lesson: future phases should step-0-grep `tests/phase_(N-1)/test_phase_*_export_slate.py` for forbidden-Phase-N sentinels alongside the existing `pytest.raises(Import/Module/AttrError)` probe.

### Test counts

* **Docker (Linux, prod image):** 3173 passed, 45 skipped, 109 warnings in 1907.88s (31m47s). Phase 30 isolated: 52 passed, 5 skipped. Skip delta: Phase 29 baseline 40 → Phase 30 45 = +5 — the 5 new `test_adr_amendment_sentinels.py` parametrize cases skipping per Model C (parent ADR dir not COPYed into runtime image).
* **Sandbox (Python 3.10):** 45/45 sandbox-runnable tests pass; 2 CLI test files collect-skip due to `datetime.UTC` 3.11+ requirement (transitive via `mindsos_server.admin` import).

### Smoke tests on prod image

1. `doctor --self-test --json` confirms all 7 packages at `0.0.0+phase30`; `expected_compose_image_phase: "30"`; `pyproject_version` + `init_version` aligned.
2. `mindsos capacity find` 4 exit-code paths verified: empty layer JSON → exit 1 + PipelineNotFoundError; start==target → exit 0 + "already at target" line; empty layer human → exit 1 + stderr message; missing args → exit 2 + Typer usage error.
3. `mindsos capacity problem-trace tail` peek paths verified: human form → "(no problem-trace records)" + exit 0; `--json --limit 5` → `[]` + exit 0.
4. Python REPL invoke envelope: `success=False, error=RuntimeError, problem_trace_sink len=1, error_kind=exception:RuntimeError` — confirms ADR-0072 envelope + ADR-0074 sink wired end-to-end.
5. Python REPL `find_pipeline` empty-layer: raises `PipelineNotFoundError` with diagnostic message — confirms BFS exhaust path.

### Carry-forwards to Phase 31+

Phase 30 closes 3 of Phase 29's 9 carry-forwards: #1 `building.md` (shipped alongside `retrieval.md`); #2 `mindsos capacity` CLI Typer group (partial — `find` + `problem-trace tail`; `invoke` verb deferred); #4 pipeline finder + invocation + InvocationResult / call_capacity + ProblemTraceRecord + sentinel flips. Phase 29 carry-forwards #3 / #5 / #6 / #7 / #8 / #9 UNCHANGED.

**Phase 30 new carry-forwards (6 items):** (1) `mindsos capacity invoke` CLI verb — Phase 31 alongside text builtins; (2) `--session-token` CLI flag — Phase 31+ when server/CLI session bootstrap matures; (3) Falkor-backed L3 bootstrap (`bootstrap_l3_from_falkordb`) — Phase 31+ when first concrete consumer arises; (4) L3 state-file serialization — rejected R2 PB-27(c); flagged for future; (5) per-user (Local-scoped) ProblemTraceSink — deferred to L4 per ADR-0074 multi-tenant scoping; (6) exit code 3 (envelope failure) — ships with invoke CLI verb at Phase 31 per R5 PB-61.

### Observed quirks / process notes

* B-30-T1 fired on first cumulative pytest run — Phase 29 sentinel-flip class wasn't audited pre-impl. Pre-impl probe checklist extended (see hotfix entry above).
* Linux smoke required prod-image rebuild after branch switch — `docker compose run --rm mindsos <verb>` auto-builds against current branch source, but only when image tag missing. Stale `mindsos:phase29-prod` from a prior session served until explicit `docker compose --profile cli build mindsos`.
* `mindsos capacity find` / `problem-trace tail` against fresh in-memory layer is structurally-correct-but-useless — exists to lock CLI surface; real-user flows arrive at Phase 31 when text builtins auto-register on layer construction.

## timestamp_utc

2026-05-25T20:42:16Z

## mkdocs_pages_updated

- docs/usage/capacity/building.md
- docs/usage/capacity/retrieval.md
