# Robot Demo — MindsOS Integration Design Log

**Started:** 2026-06-10 (Cowork, DM-1 chat). Decision/pushback record for the MindsOS-integration build of the robot demo. Companion to `ROBOT_DEMO_MINDSOS_PLAN.md` (the plan) — this log captures what changed *during* build and why. Critical-design-reviewer posture: every entry is flaw → options → chosen.

Pair-execution pattern (Cowork ↔ Mac ↔ Linux) applies to anything touching the server. No git mutations from the sandbox (Mac commits only).

---

## DM-1 — Deployment + bootstrap

### §1. Grounded probe results (read shipped code, did not run yet)

The plan's three DM-1 probes, answered by reading the shipped surfaces:

- **Probe (a) — `register_capacity(if_exists="upsert")` shadow per-CL (G-1).** Signature confirmed: `register_capacity(self, declaration, *, session=None, ref_to_global=None, ref_type=None, extra_properties=None, if_exists: Literal["raise","upsert"]="raise")` (`mindsos_capacity/capacity_layer.py:275`). Surface exists. **Open at build:** does `upsert` re-emit the `PRODUCES`/`CONSUMES` IntergraphEdges, or only swap the implementation callable? Verify at first demo registration (DM-4).

- **Probe (b) — `CapacityContext.mm_handle` write surface for chain artifacts (PB-B).** **Answer: NONE.** `mm_handle` is the `MMHandle` Protocol = 4 *read* methods only (`get_or_instantiate`, `find_instances_by_type`, `produces_of`, `consumes_of`; `mindsos_capacity/context.py:51`). Worse, `ChainArtifactWriter` is constructed *inside* `Orchestrator.run_lifecycle` and handed only to the phase functions — a capacity body never receives it (`mindsos_intelligence/orchestrator.py:132`). **Consequence:** §5.2 option-3 (demo capacities author the chain via `mm_handle`) **cannot be built** without editing `mindsos_intelligence` (forbidden, §9.1) → **deleted from the plan.**
  - **Good news (decides PB-B):** `phase_1.run` dispatches by fixed IRI and emits artifacts *from the dispatched capacity's outputs* — `writer.emit_hint_set(hints)` and `writer.emit_mapping_result(…, mapping["task_pattern_iri"], mapping["mapping_confidence"])` (`mindsos_intelligence/phase_1.py:43-72`). So **same-name override (§5.2 option-1) produces honest chains with zero domain edits, and is the only honest path.** Constraint: demo Phase-1 logic must register under the *literal* IRIs the phases dispatch — `capacity:process:identity`, `capacity:hint:global`, `capacity:decision:derive_goal`, `capacity:decision:map_to_task_pattern` — not the prettier `perception.ingest_order` names in §4.3. §4.3/§5.2 IRI naming to be reconciled in DM-4. (Plan-2 phases `plan_construction`/`execution` dispatch IRIs not yet traced — DM-4.)

- **Probe (c) — `promoted-pipelines` per-NodeType Local tier (§4.3 asterisk).** **Answer: NO.** Per-NodeType `storage_mode` is Global-only at v1 (Phase 43 / ADR-0151); promoted-pipelines NodeTypes ship `INLINE`, no per-user Local option (`mindsos_knowledge/schemas/promoted_pipelines.py`). **The §4.3 asterisk stands** — learned composites go to Local `capacity-state` as a `LearnedComposite` NodeType, name-mirrored only. "Delete this asterisk" branch removed.

### §2. Round-2.5 pushbacks (confirmed by the user 2026-06-10)

Adversarial re-read of §1 (deployment/bootstrap) against shipped code. All accepted.

- **P1 (= PB-B resolution):** `mm_handle` read-only → §5.2 option-3 dead; option-1 (same-name override under literal v0 IRIs) is mandatory and honest. Net-new finding → **Fn candidate** (chain-artifact authorship seam for consumers).
- **P2:** promoted-pipelines no Local tier → §4.3 asterisk stands (Local `capacity-state` `LearnedComposite`).
- **P3:** `if_exists="upsert"` present; edge re-emission to verify at DM-4.
- **P4 (superseded by Round-3 PB-J):** originally "use `bootstrap_kl_from_falkordb` not the pair helper." Round-3 changes the KL story (per-device) — see PB-J. The pair helper (`bootstrap_global_pair_from_falkordb`) is still wrong for the demo (it returns canonical+pending for the pivot-release flow, not Global+Locals).
- **P5:** **Orchestrator wiring is net-new and unspecified in the plan.** `IntelligenceLayer` exposes no `run_lifecycle` and no Orchestrator; `il.mm` exists only after `il.start()`. Consumer must build `Orchestrator(L4Dispatcher(cl, session=session, kl=kl), il.mm)` *after* start and run it via `il.enqueue(lambda: orch.run_lifecycle(task_input, task_id=…))` (runs on a worker thread). **Choice:** a `Brain` struct in `bootstrap.py` holds `(session, kl, cl, il, orch)`; smoke enqueues + `future.result()` to exercise the real worker-pool path the RAM/jitter gate measures.
- **P6:** "idempotent re-boot" needs get-or-create existence guards — `insert_user` raises on duplicate (UNIQUE). **Choice:** query-then-insert for admin + 4 users; pin `server.db` to the named volume so identities survive restart.
- **P7:** DM-1 jitter gate had no pass criterion. **Choice (provisional):** proxy = a busy stepping thread at 50 Hz + 4 ILs looping enqueued trivial lifecycles; **gate = p99 step interval ≤ 2× nominal at 50 Hz**, AND record the full distribution. Real bar lands in DM-3 with the actual MuJoCo loop (synthetic numbers won't transfer).
- **P8 (scope guard):** §1.3 steps 4–6 (bundles, Local seeds, demo L3 installers) are DM-2/DM-3. DM-1 `bootstrap.py` ships labeled no-op stubs for those steps; only admin + 4 users + per-device KL + 4 builtin CLs + 4 ILs + smoke are live.
- **P9:** real `login()`/`insert_user` sessions, not `Session.for_testing` — the whole P-1 rationale is that Local/Global/`writeable` are *real*, and the gate reads `session.capabilities`. Demo-fixed passwords in compose env.

### §3. Round-3 — per-device-instance architecture (user request 2026-06-10)

**Request:** future MindsOS installs per device type (computer/phone/robot), knows "where" it is, and installs device-type-exclusive capacities. Showcase by giving each of the 4 brains (mgr/arm1/arm2/conv) **individual Global/Local L2, L3, L4** — co-resident on the Linux server, sharing resources.

**Scoping clarification (PB-I):** L3 is already per-instance (per-brain CapacityLayer, P-4); L4 is already per-instance (per-brain IntelligenceLayer). **The only real change is L2:** replace the one shared `KnowledgeLayer` (1 Global + 4 Locals) with **4 independent `KnowledgeLayer`s** (each own Global + own Local), one Server process / one FalkorDB. Server stays shared (it is the runtime envelope, *not* on the layer-composition axis per CLAUDE.md) → one `server.db`, real auth/sessions/audit, real `writeable` gate. Cheaper than P-1 feared: single process ⇒ no cross-process Global sync; the bus already carries the only cross-instance traffic (peer transfer).

- **PB-J — shipped load-or-mint is single-Global-by-name.** `bootstrap_kl_from_falkordb` hardcodes `_GLOBAL_METAGRAPH_NAME`; `KnowledgeLayer.bootstrap()` has no name param → 4 persisted Globals collide on `find_by_name`. FalkorDB *can* hold 4 distinctly-named Globals (docstring: "all Metagraphs coexist, keyed by metagraph_id"). Options: (a) per-device named load-or-mint helper in `demo_backend` (mint → set metagraph name → persist; load by name); (b) DM-1 uses 4 fresh **in-memory** Globals (no persist), defer named Falkor round-trip to DM-2; (c) edit `mindsos_*` — violates §9.1. **Chosen: (b) for DM-1, (a) for DM-2.** Keeps the smoke off the naming/idempotency risk; named persistence built+tested in DM-2 with the G-5 episode-flush probe. **Build-time verify:** is `global_metagraph().name` settable without breaking the 6 ensured role-graphs / `find_by_name` idempotency.
- **PB-K — `capacity-gaps` is Global-tier; brains can't write it live (pre-existing defect).** §3.1's "beats 1 & 5 write capacity-gaps (Global) live" fails the `writeable` gate for a normal brain session (`CAN_WRITE_GLOBAL` = admin) in *both* models. **Chosen:** gaps go to each device's **Local** `capacity-state` (§4.1 already does the Local write); Manager aggregates via `comms.report`; drop the Global capacity-gaps write. Dissolves a latent P-5 contradiction.
- **PB-L — beat-4 peer transfer becomes genuinely cross-installation.** arm1's KL Local → arm2's KL Local = two independent installs sharing a learned skill over the bus. Strengthens the thesis; no mechanism change (F1-min already = receiver-writes-own-Local). **Chosen:** adopt; narration → "across installations."
- **PB-M — "knows where it is" = a `demo_backend` DeviceProfile driving Phase-50 bundle selection (net-new → F7).** `device_type` string per instance (real hardware detection is future). Profile → bundle map: `core`→all 4, `arm-suction`→arm1, `arm-jaw`→arm2, `conveyor`→conv, `manager`→mgr. Mechanism = shipped Phase-50 install; selection logic = `demo_backend`. **Chosen:** `device_type` field on a `DeviceInstance` struct + selection map in `demo_backend`; manifest-level device gating filed as **F7** for future MindsOS. DM-1 only plumbs the field; selective install lands DM-2/DM-3.
- **PB-N — RAM gate budgets 4 Globals, not 1.** Minor (demo Globals small). **Chosen:** record the 4-KL footprint in the DM-1 measurement; no threshold change.

**Net effect on the plan:** P-1 revised (per-device 4 KLs); new P-8 (device profile); §1.1 diagram + §1.3 bootstrap updated; capacity-gaps re-homed to Local (PB-K); F7 added. Zero `mindsos_*` edits. Open build-time verifications: PB-J name-settability; P3 upsert edge re-emission; P5 Orchestrator wiring confirmation against Phase-47 tests.

### §4. Round-4 — Docker/build + smoke grounding (2026-06-10)

Read the shipped `Dockerfile` + `docker-compose.yml` + the consolidation path. Findings:

- **PB-O (HIGH) — `mindsos[demo]` extras are inert.** `prod` stage = `pip install --require-hashes -r requirements.txt` then `pip install --no-deps .` → a `pyproject.toml` extras group is never installed. Options: (a) add demo deps to the core lockfile (pollutes everyone with MuJoCo); (b) new hash-pinned `requirements-demo.txt` + `demo` stage `FROM prod`; (c) drop hash-pinning (breaks supply-chain discipline). **Chosen: (b).** Replaces §1.2's extras mechanism.
- **PB-P (MED) — `demo_backend`/`sim`/`web` not COPYed into any image.** The `prod` stage COPYs an explicit package list excluding them. **Chosen:** the new `demo` stage adds `COPY demo_backend` (DM-1) + `COPY sim`/`COPY web` (DM-3); `demo-backend` compose service uses `entrypoint: ["/usr/local/bin/entrypoint.sh"]` + `command: python -m demo_backend.main`. Verify `entrypoint.sh` passes an arbitrary command through the gosu drop. `falkordb` healthcheck (`redis-cli ping`) + `prod` target both confirmed present — the rest of §1.2 stands.
- **PB-Q (MED) — smoke can silently skip consolidation.** `consolidation_enabled(dispatcher)` (`mindsos_intelligence/consolidation.py:28`) returns False (skip, not fail) unless `consolidate:mm` is registered AND a KL is bound; docstring explicitly names "the v0 orchestrator smoke" as a graceful-skip case. Both conditions hold for us (`consolidate` ∈ `FUNCTIONAL_CATEGORIES`, line 139 of `identifiers.py` ✓; we bind `kl` ✓), so consolidation will be *enabled* — but a wiring regression would yield a green lifecycle with 0 Episodes. **Chosen:** smoke asserts `consolidation_enabled(dispatcher) is True` per brain before the run + asserts the Episode after; folds into the P5 Orchestrator-wiring confirmation (Phase-48/49 assert in-memory Episode consolidation).
- **PB-R (LOW) — DM-1 idempotency gate is thin.** Per-device KLs are in-memory at DM-1 (PB-J) → reboot idempotency only exercises `server.db` user get-or-create. **Chosen:** state the scope; full KL load-or-mint idempotency drill at DM-2.

**Still zero `mindsos_*` edits** (the Dockerfile/compose/requirements changes are build-config, not domain code). New build-time verifications: `entrypoint.sh` command pass-through; `requirements-demo.txt` hash-compile (mujoco wheels); `global_metagraph().name` settability (PB-J); `upsert` edge re-emission (P3).

### §5. Round-5 — bootstrap prerequisites (2026-06-10)

Read `mindsos_server/_schema.py` + `_db.py` + `KnowledgeLayer.local_metagraph`.

- **PB-S (HIGH, blocker) — server.db schema init omitted.** §1.3 step 1 created the admin without first creating the auth tables. Entry: `mindsos_server._schema.init_or_migrate(conn)` (`_schema.py:345`; forward-only `CREATE TABLE IF NOT EXISTS` for users/sessions/audit + `schema_version`). **Chosen:** new bootstrap **step 0** — open a conn via the `_db.py` context manager (WAL + `foreign_keys=ON` + `busy_timeout=5000`), call `init_or_migrate`, before any `insert_user`. Idempotent → also satisfies re-boot. Folded into §1.3.
- **PB-T (LOW, resolved) — Local consolidation target.** `KnowledgeLayer.local_metagraph(user_id)` (`knowledge_layer.py:242`) lazily auto-creates the Local with `episodic_memories` + `capacity-state` ensured (per `__init__.py` ADR-0042 PB-9). The smoke's normal-user consolidate has a valid write target with no explicit Local minting. **Chosen:** bootstrap pre-touches each device's Local (`kl.local_metagraph(brain_user_id)`) defensively so wiring problems surface at boot, not mid-smoke.
- **PB-U (resolved → discipline) — sqlite threading.** `_db.py:95` opens short-lived, context-managed, per-call conns (`isolation_level="DEFERRED"`, default `check_same_thread=True`, WAL + `busy_timeout=5000`). No long-lived conn shared across worker threads. **Chosen:** `demo_backend` follows the same per-operation-conn discipline; runtime audit writes on worker threads each open their own conn (WAL handles concurrency). Verify only if a runtime write path is found reusing a bootstrap-thread conn.

**Assessment:** reanalysis is at diminishing returns — PB-S is the last plan-level omission found; the remaining unknowns are build-time verifications (listed across §§3–5), answerable only by running code. Recommend opening the DM-1 build.

---

### §6. DM-1 build (2026-06-10) — code landed, gates pending Mac run

Built `demo_backend/` (consumer package, zero `mindsos_*` edits) + build config + `tests_demo/`. **Sandbox-validated the full per-device core** (Python 3.10 + `tomli`, duck sessions): 4 independent KLs → 4 CLs (builtin catalog) → 4 IntelligenceLayers started → Orchestrator per brain → `il.enqueue(run_lifecycle)` on the worker pool → **4 Episodes consolidate**, `consolidation_enabled` True for all, per-device Local isolation confirmed (mgr-only run leaves the other 3 Locals empty). `tests_demo`: **4 passed, 1 skipped** (real-server test skips on 3.10).

Grounded-at-build confirmations:
- **PB-J** — `global_metagraph().name` IS settable (per-device KL naming works).
- **PB-Q** — `consolidate` ∈ `FUNCTIONAL_CATEGORIES`; the smoke asserts `consolidation_enabled` per brain (no silent skip).
- **P5** — wiring confirmed against `tests/phase_47/_fixtures.py` + `tests/phase_49/integration_c.py`: `Orchestrator(L4Dispatcher(cl, session, kl), il.mm)` built after `il.start()`; run via `il.enqueue`.
- **PB-P** — `entrypoint.sh` execs `gosu mindsos "$@"` → the demo command passes through the privilege drop. Verified.
- **Builtin catalog** = `install_planning_v0 + install_phase1_v0 + install_orchestration_v0 + install_consolidate_capacities + reset_v0_verdicts` (the shipped builtin install fns; NOT `create_global`).

Files (after the Round-6 restructure below): `robot_demo/backend/{__init__,profiles,brain,bootstrap,main,__main__,reset,measure}.py`; `robot_demo/requirements-demo.in`; Dockerfile `demo` stage (`FROM prod`, COPYs `robot_demo/`); `robot_demo/deploy/docker-compose.demo.yml` overlay (reuses root `falkordb`); `robot_demo/tests/{__init__,conftest,test_dm1_bootstrap}.py`.

### §7. Round-6 — repo restructure + docs + deploy (2026-06-10)

User asks: (1) docs for what/why/how; (2) consolidate the demo into one repo folder; (3) a separate folder on the Linux server; (4) Linux testing. Decisions (user-approved): umbrella **`robot_demo/`** with package **`robot_demo.backend`** (renamed from top-level `demo_backend`); **reuse the existing `falkordb`** (overlay merged with the root compose, not a separate instance).

- Moved `demo_backend/`→`robot_demo/backend/`, `tests_demo/`→`robot_demo/tests/`, `requirements-demo.in`→`robot_demo/`. Added `robot_demo/__init__.py`. Test imports → `robot_demo.backend.*`. Re-validated in sandbox: **4 passed / 1 skipped**, all backend modules import, YAML + `bash -n` clean.
- Dockerfile `demo` stage now COPYs `robot_demo/` + runs `python -m robot_demo.backend.main`; the root `docker-compose.yml` demo-backend service was **reverted** — the service lives in `robot_demo/deploy/docker-compose.demo.yml` as an overlay (`docker compose -f docker-compose.yml -f robot_demo/deploy/docker-compose.demo.yml`), reusing root `falkordb`, with demo-scoped data under `./.mindsos-demo/`.
- The Dockerfile `demo` stage **must** stay in the root Dockerfile (build context = repo root to reach `mindsos_*`).
- Docs: `robot_demo/README.md` (what/why/how + topology), `robot_demo/docs/DM1_DEPLOYMENT.md` (architecture + bootstrap walkthrough + run matrix), `robot_demo/deploy/README.md` (server runbook).
- Linux testing: `robot_demo/deploy/run_linux_tests.sh` — preflight → `pip-compile` the hash-pinned lockfile → build `demo` image → **container bootstrap smoke** (real server, 4 brains, 4 Episodes) → idempotent re-boot → in-container RAM+jitter; optional host pytest (`RUN_PYTEST=1`). The container smoke is the authoritative gate (self-contained; no host Python deps).
- **DM-1 is headless** — no browser-linked test yet; the first is DM-4. Flag the user when a live/browser step is reached.

Pending on the Mac/3.12 host is unchanged (pip-compile, the gate run, measurements) — now via `bash robot_demo/deploy/run_linux_tests.sh`.

### §8. Round-7 — Dockerfile decoupling (separate-repo question, 2026-06-10)

User asked whether the demo needs a **separate repo** given the Dockerfile coupling. Decision: **stay in-repo, but the demo owns its Dockerfile.** Reasoning: the demo imports `mindsos_server` *private* internals (`_db.open_db`, `_schema.init_or_migrate`, `users._insert_first_admin`, `_argon2.PRODUCTION_PARAMS`) and co-evolves with freshly-shipped phases — a versioned repo boundary would make those imports brittle and lag HEAD, and a split would need wheel/registry publishing that doesn't exist yet. The actual smell (a demo stage inside the *core* Dockerfile) is removed without a split.

- **Reverted** the `demo` stage from the repo-root `Dockerfile` (now demo-free, with a one-line pointer).
- **Added** `robot_demo/deploy/Dockerfile`: `FROM ${MINDSOS_BASE:-mindsos:phase51-prod}` + demo deps + `robot_demo/`. Build context = repo root (to COPY `robot_demo/`).
- **Overlay** `build:` → `dockerfile: robot_demo/deploy/Dockerfile` + `MINDSOS_BASE` arg (no more `target: demo`).
- **Build order** (run_linux_tests.sh step 2): `docker compose --profile cli build mindsos` (base prod image) → then build `demo-backend`.
- **Revisit a repo split** at demo-v1-stable; the precondition is a **public** mindsos bootstrap API so the demo stops importing server privates. Tracked as a follow-up (no Fn — it's a packaging decision, not a MindsOS feature).

Net: core Dockerfile untouched by the demo; demo image explicitly version-pins its base. Still zero `mindsos_*` edits.

**Pending on the Mac/Linux 3.12 host (pair-execution):**
1. `pip-compile --generate-hashes -o requirements-demo.txt requirements-demo.in` (MuJoCo wheels; the `demo` build needs the `.txt`).
2. Run the real-server gate: `pytest tests_demo/ -m integration` (real `insert_user`/`login` + `init_or_migrate`; asserts 4 Episodes + idempotent re-boot).
3. `python -m demo_backend.measure` → record RAM (4-Global footprint, PB-N) + jitter p99 vs 2× nominal (P7).
4. `DEMO_BOOTSTRAP_ONLY=1 docker compose up --build demo-backend` → DM-1 gate: 4 brains start, 4 Episodes, idempotent re-boot.
5. P3 (`upsert` edge re-emission) stays open → DM-4.

Still **zero `mindsos_*` edits**; commits are Mac-only (standing rule).

---

### §9. Live-run fixes (Linux, 2026-06-11)

First container bootstrap on the Linux gate host surfaced one real defect (caught by the smoke, as designed):

- **PB-V — `login()` enforces one active session per user; bootstrap wasn't idempotent against a persisted `server.db`.** On a re-boot (or any run against an existing `./.mindsos-demo/server-db` volume) `login()` raised `AlreadyLoggedInError` ("use logout or kill_my_own_sessions"). **Fix:** `_login_all` calls the shipped self-recovery valve `kill_my_own_sessions(conn, user_id, password)` before each `login`, then commits. This clears the prior process's stale session and is exactly what makes the P6 idempotent-re-boot gate meaningful (vs. wiping the volume). No `mindsos_*` edit — consumer-side only.

**DM-1 GATE GREEN ON LINUX (2026-06-11).** Mac Mini, real 3.12 prod image, real `mindsos_server` bootstrap: 4 device-instances boot → 4/4 Episodes consolidate → **idempotent re-boot verified** (two consecutive `DEMO_BOOTSTRAP_ONLY=1` runs, both `exit=0`, the second on the existing `server.db`). Measurements: **RAM 38.3 MB** full 4-brain stack (35.5 MB baseline → +2.8 MB; 4 in-memory Globals are negligible, PB-N). **Jitter** (synthetic 50 Hz proxy under 4-brain load): n=396, p50 20.00 ms, **p99 20.08 ms**, max 20.17 ms vs 20.00 nominal — PASS (provisional bar p99 ≤ 40 ms; real bar at DM-3). One live fix landed (PB-V); zero `mindsos_*` edits. **DM-1 done.**

## DM-1 build status

- [x] `demo_backend/` skeleton package (profiles, brain, bootstrap, main, reset, measure)
- [x] `docker-compose.yml` `demo-backend` service + `demo` Dockerfile stage + `requirements-demo.in` (PB-O/PB-P — replaces the inert extras group)
- [x] `bootstrap.py` (schema init → admin → 4 users → login → 4 per-device in-memory KLs → 4 builtin CLs → 4 ILs started → Orchestrator per brain → smoke ×4) — **core sandbox-validated**
- [x] `reset.py` stub (G-11 restart reset; run-scoped wipe body deferred to DM-2)
- [x] `tests_demo/` scenario (4 passed / 1 integration-skip in sandbox)
- [x] RAM + jitter-proxy measurements — **Linux, 2026-06-11: RAM 38.3 MB, jitter p99 20.08 ms PASS**
- [x] real-server gate + docker build/up + `requirements-demo.txt` compile — **Linux, 2026-06-11: green, idempotent re-boot verified (PB-V fix)**

---

## DM-2 — L2 initial knowledge + device-type bundles + per-device Falkor persistence

**Started:** 2026-06-11 (Cowork, DM-2 chat). Reanalysis-before-code per the project posture. Probes run in the 3.10 sandbox against the real `mindsos_capacity`/`mindsos_knowledge`/`mindsos_core` (not `mindsos_server` — 3.11+ `datetime.UTC`); `install_skill` + Falkor round-trip are Linux-gated.

### §10. DM-2 reanalysis — grounded pushbacks (flaw → options → chosen)

Probe results that reshape the plan-as-written (all run, not read):

- **Probe-1 (the decisive one):** `Graph.add_node` enforces NodeType **registration** whenever a schema is attached, *even at `strict=False`* (`strict` only toggles per-property `PropertyType` maps). Unregistered `type_name` → `UnknownTypeError`; an edge with an unregistered rel-type → `CypherError`. Confirmed live.
- **Probe-2 (per-role rosters):** the closed-13 role schemas are **domain-specific, not generic** — `ontology`=OWL (`Class`/`Individual`/`ObjectProperty`/`Restriction`… + `SUBCLASS_OF`/`TYPE_OF`/`RESTRICTS_PROPERTY`…); `concepts`=FrameNet (`Frame`/`FrameElement`/`LexicalUnit`/`SemanticType`); `lexicon`=WordNet (`Lemma`/`Sense`/`Synset`/`SenseExample`); `task-patterns`=`TaskPattern`/`SubgoalTemplate`; `capacity-state`=**single `CapacitySnapshot`, zero EdgeTypes**; `episodic_memories`=`Episode`/`Memory`; `installed-skills`=`SkillInstallRecord`.
- **Probe-3 (manifest shape):** the bundle `[[l2.content]]` path is **nodes-only** — `driver.install_skill` iterates `manifest.l2_content` and calls `graph.add_node` only; **no edge slot exists** in `L2ContentEntry`. Node `value` is `Any` (TOML inline table/array → dict/list, persisted via the ADR-0182 `_value_json` codec); `properties` go through `validate_user_properties` (reserved keys like `kind`/`type`/`id` are rejected; lists *are* accepted).

**PB-W — the §3.3 embodiment subgraph can't be built in `capacity-state` as typed nodes+edges (BLOCKER as written).** New NodeTypes (`BodyPart`/`EndEffector`/`AffordanceProvision`) and `has-part`/`provides` edges all fail under the single-NodeType / zero-EdgeType `capacity-state` schema; fixing the schema is a forbidden `mindsos_*` edit. Options: (a) encode embodiment as `CapacitySnapshot` nodes whose `value` is a structured dict (parts/affordances/reach), feasibility reads the bag; (b) new `embodiment` role — needs ADR-0150 amendment, out of scope; (c) edit the schema — forbidden. **Chosen: (a).** Local-only ⇒ rides the `make_writeable` Local path with no `CAN_WRITE_GLOBAL`. The property-encoded (vs walkable-subgraph) embodiment is a real limitation → **F8** (future real `embodiment` role).

**PB-X — bundle L2 node_types must be pre-registered per role (same enforcement).** §3.1/§3.3 content names aren't NodeTypes. **Chosen:** map every demo Global node onto its role's existing registered NodeType, semantics in `value`/`properties` (ref-bundle "shouting"=`Frame` precedent). Concretely: ItemKinds/Affordances/regions/grid/belt → `ontology` `Class`/`Individual`; Order/OrderLine/Carrier/Cargo/Handoff/Workaround → `concepts` `Frame`; order + position vocabulary → `lexicon` `Lemma`/`Sense`; demo task-patterns → `task-patterns` `TaskPattern`.

**PB-DD — bundles seed nodes only; all relationships become node properties.** The §3.1 ontology *requires-edges* (Sheet→grasp:suction) and §3.3 *provides/has-part* edges have no bundle vehicle and (for `capacity-state`) no registered EdgeType. **Chosen:** encode requirements/affordances as **node `value`/property data**, not graph edges — e.g. ontology `Class("Sheet")` carries `value={"requires_affordance":["grasp:suction"]}`. The DM-3 `validate.feasibility` capacity reads this data; it never walked edges. Harmonizes with PB-W (everything is node+bag). Genuine OWL/edge modeling of the world is deferred (not demo work).

**PB-Y — bundle taxonomy double-specified (§3.4 3 content-bundles vs `profiles.py` 5 distribution-names).** **Chosen:** adopt the distribution scheme already in `profiles.py`. `core@1.0`→all 4 (ontology world-model + §4.0 DataStates + realm `robot` via its L3 installer); `manager@1.0`→mgr (lexicon + concepts + task-patterns); `arm-suction`/`arm-jaw`/`conveyor@1.0`→their device (a thin Global self-descriptor `concepts.Frame` at DM-2 — device-type-exclusive + queryable, deliberately minimal, grows DM-3 when embodied-adjacent Global content exists). Exercises P-8 4-way selective install on the real gate without theater.

**PB-Z — persist Globals only; Locals stay in-memory + re-seeded each boot.** Reloading a persisted Local would accumulate Episodes across reboots (breaking the smoke's `==1`/`==4` assertions) and fight the G-11 reset intent. **Chosen:** the per-device named Falkor helper persists the **Global** (seeds + install records survive → install no-ops on reboot); Locals are re-minted in-memory and idempotently re-seeded every boot; G-5 is an **isolated** episode round-trip probe, not "Local is the durable store." Keeps the DM-1 smoke valid and reset trivial. Durable-Local persistence → future (F-note).

**PB-AA (retraction + refinement) — `bootstrap_kl_from_falkordb` DOES exist**, in `mindsos_server.persistence.bootstrap` (DM-1 grep searched the wrong package). It is single-Global-by-name (`_GLOBAL_METAGRAPH_NAME`), so still unusable per-device as-is. **Chosen:** build a per-device `load_or_mint(client, profile)` in `robot_demo` mirroring it — `find_by_name(profile.kl_name)` → `loader.load` + `KnowledgeLayer(global_metagraph=…)`, else `KnowledgeLayer.bootstrap()` → set `.global_metagraph().name` → `repo.persist`. **Surfaced win:** install records (`_roster_value`) AND Episodes are both dict-valued, so Global-persist idempotency rides the *same* ADR-0182 codec as G-5 — one mechanism, two payoffs. First boot order: mint → build CL → `install_skill` (writes Global L2 + runs the DataState installer into CL) → seed Local → `repo.persist(global)`. Reboot: load → `install_skill` no-ops (digest match) → re-persist (MERGE-idempotent).

**PB-BB — install authorization: real admin session, not the `session=None` carve-out.** §3.4 says "eat the Phase-50 dogfood / real ADR-0180 gate." **Chosen:** bootstrap logs in `admin` (ADMIN_CAPS carries `CAN_INSTALL_SKILL`+`CAN_WRITE_GLOBAL`, confirmed) via the PB-V kill-stale→login pattern, and passes that session to `install_skill`. Asymmetry noted: the bundle's own L3 installer runs as `fn(cl)` with no session (driver passes only `cl`), so the realm/DataState registration writes each device's Global DataState graph via the ADR-0080 session-less carve-out — unavoidable and fine.

**PB-CC — digest stability.** **Chosen:** ship real `.toml` bundle files under `robot_demo/bundles/` + `parse_manifest` (digest = SHA-256 over file bytes → stable across boots/re-installs); name+version+digest idempotency holds. No hand-built in-memory `SkillManifest`.

**Sandbox/Linux split:** sandbox (3.10) validates the L3 installer (real `CapacityLayer.register_datastate`), the Local seeds (real `KnowledgeLayer` + `make_writeable`), bundle TOML parse (raw `tomli`), and the load-or-mint logic against a fake in-memory client. Full `install_skill` (driver + preflight + records + audit) and the Falkor round-trip (per-device Globals + G-5 episode) are the Linux gate.

### §11. DM-2 build (2026-06-11) — code landed, Linux gate pending Mac run

Built the DM-2 surface as additive `robot_demo` consumer code (zero `mindsos_*` edits). **Grounded-at-build confirmations (sandbox, real `mindsos_capacity`/`mindsos_knowledge`):**

- **Probe-1/2/3 ran** (design log §10) — type registration enforced at `strict=False` (`UnknownTypeError`/`CypherError`); role rosters captured; manifest L2 is nodes-only. All 26 demo L2 nodes add cleanly onto the real role schemas (PB-X mapping correct).
- **Installer** (`installers.py`) — 32 `robot.*` DataStates register idempotently (all-present no-op; 1/32 partial detected); realm `robot` accepted via `allow_new_realm=True`.
- **Local seeds** (`seeds.py`) — embodiment `CapacitySnapshot` bag per brain, get-or-create idempotent, distinct affordances (arm1 suction / arm2 jaw / conv belt / mgr none), readable via `read_local_embodiment` (PB-W).
- **Preflight pre-cleared** — bundle roles (`ontology`/`concepts`/`lexicon`/`task-patterns`) ∈ `_GLOBAL_NAMED_ROLES`; per-device install order (`core` first) satisfies `requires_bundles`.
- **Reset** (`reset.py`) — wipe body implemented (live in-memory Local wipe keeping the embodiment seed); test removes the Episode, keeps the seed.

Files: `robot_demo/backend/{installers,bundles,seeds,persistence}.py` (new) + `brain.py` (kl-injection) + `bootstrap.py` (admin login + per-device install/seed/persist + G-5 probe) + `main.py` (DM-2 markers) + `reset.py` (wipe body); `robot_demo/bundles/{core,manager,arm_suction,arm_jaw,conveyor}/manifest.toml` (5 bundles, 26 L2 nodes); `robot_demo/tests/test_dm2_bundles.py` (8 core + 2 integration). Tests: **12 passed / 3 integration-skipped** in the 3.10 sandbox.

**Two build-time deferrals filed (DEMO_DERIVED_FEATURES):**
- **F8** — a real Local `embodiment` role-graph (typed BodyPart/EndEffector + provides/has-part edges). DM-2 property-encodes the embodiment in a `CapacitySnapshot` value because `capacity-state` is single-NodeType / zero-EdgeType and a schema edit is forbidden (PB-W).
- **F9** — durable Local persistence + bundle-installer re-activation on reboot. DM-2 persists Globals only (PB-Z); and `install_skill` no-ops on reboot (digest match) so a bundle's L3 installer does NOT re-run via the install path — DataStates survive only because they live in the persisted Global. Re-activating L3 *capacities* on reboot is `apply_installed_skills`' job (no bundle registers capacities at DM-2, so latent).

**Pending on the Mac/Linux 3.12 host (pair-execution, Mac commits only):**
1. `bash robot_demo/deploy/run_linux_tests.sh` — extended with DM-2 assertions (bundles installed per device-type, Local seeds present, `GLOBAL PERSIST: falkor`, first-boot G-5 `round-tripped intact`, second-boot bundle `(no-op)`).
2. `RUN_PYTEST=1` → the two `@integration` tests (`install_skill` same-KL no-op; full DM-2 bootstrap).
3. Confirm `requires_mindsos_phase=50` clears preflight on the running base image (`DEMO_MINDSOS_PHASE` override if the image reports a higher phase).
4. Confirm the G-5 episode `value` is dict-shaped so the round-trip exercises the ADR-0182 `_value_json` codec (if primitive, it still round-trips — the probe asserts equality either way).

Still **zero `mindsos_*` edits**.

### §12. DM-2 post-build re-analysis (2026-06-11) — adversarial re-read of the landed code

Re-read the DM-2 implementation (not the plan). Episode `value` confirmed **dict-shaped** in the sandbox (`{'task_input_ref',…,'task_pattern_iri',…}`), so the G-5 probe genuinely exercises the ADR-0182 dict-value codec. Three pushbacks against my own code:

**PB-EE (HIGH) — the first-cut G-5 probe persisted the LIVE Local.** It `persist(local_mg)` + `load(local_mg.metagraph_id)`. Flaws: (1) the live Local is re-minted in-memory each boot with a fresh `metagraph_id` and never loaded back → one **orphaned Local metagraph leaked per boot** (no scoped-delete API ships — `FalkorDBLocalPersister` doesn't exist; only soft-delete replayers); (2) it **contradicts PB-Z** (Locals stay in-memory); (3) persist/load exceptions were **uncaught** → crash the gate, violating the "fall back if it resists" contract. Options: (a) isolated fixed-id throwaway probe holding the real Episode value dicts; (b) persist live Local + scoped-delete (no API → rejected); (c) persist live Local under a forced stable id (PB-Z smell → rejected). **Chosen: (a).** Build a throwaway `Metagraph(metagraph_id="robot-demo::g5-episode-probe")` with one no-schema `episodes` graph, copy the real Episode `value` dicts in, persist (MERGE-idempotent → fixed id overwrites each boot, zero leak), reload, compare; whole body wrapped in `try/except` → `ok=False` on any failure. Live Local untouched (PB-Z intact); real content still round-tripped.

**PB-HH (LOW) — core manifest declared only 8 of the 32 DataStates.** The installer makes 32; the manifest listed 8 → misleading + partial preflight collision coverage. **Chosen:** list all 32 in `[l3].datastates` (verified == `robot_datastate_iris()`). Manifest is now the honest roster; digest re-stamped (pre-Linux, harmless).

**PB-GG (MED, flagged not patched) — reboot idempotency rests on two unverified Phase-50 facts:** the persisted `installed-skills` record's **dict value** must round-trip via the codec (so `latest_records_by_bundle` reads `status`/`version`/`digest` → `install_skill` no-ops), and a fresh `CapacityLayer(kl=loaded_kl)` must accept a **schemaless** loaded Global (`loader.load` returns `schema=None`). Both are shipped paths but unverified by me; either failing fails **loudly** (reinstall → "node exists, not owned"), not silently. **Choice:** verify on the Mac/Linux run before patching; if it trips, the fix is `loader.load(id, schema=schema_for_role(...))` reattachment — don't pre-guess it. Added to the §11 pending-host list.

Sandbox after the fixes: **12 passed / 3 integration-skipped**; manifest roster == installer roster (32/32).

### §13. DM-2 re-analysis round 2 (2026-06-11) — Falkor namespace + a caught latent bug

**PB-JJ (MED, user-approved) — demo Falkor data shared the `"mindsos"` graph keyspace.** `FalkorConfig.from_env()` hard-codes `graph="mindsos"` (P86 B — graph is never env-sourced), so the 4 per-device Globals + the G-5 probe would land in the same keyspace a real MindsOS server uses for its canonical `global_knowledge` (distinct *names*, but one keyspace; no clean "drop demo data"). Options: (a) dedicated graph; (b) leave on `"mindsos"`. **Chosen: (a).** `open_client()` now builds `replace(FalkorConfig.from_env(), graph=os.environ.get("DEMO_FALKOR_GRAPH","robot_demo"))` — full isolation, trivially droppable, zero risk to any real Global. ~4 lines, robot_demo-only.

**PB-II (scoping, user-confirmed) — G-5 = codec-content round-trip, not live-Local flush.** The isolated probe proves the dict-valued Episode `value` survives the ADR-0182 codec; the brain's live Local is NOT persisted (PB-Z) and live-Local durability is **F9**, out of DM-2. The plan's "fall back to in-memory if it resists, document" explicitly covers this.

**Caught while verifying PB-JJ:** the new module-level `_DEMO_GRAPH = os.environ.get(...)` referenced `os`, which `persistence.py` did not import — a **latent NameError** the sandbox suite missed because nothing in it imports `persistence` at top level (the integration tests import `bootstrap` lazily, and they skip on 3.10). It would have fired on the Linux gate. Fixed (`import os`) + added `test_all_backend_modules_import` so any missing top-level import now fails the sandbox suite instead of the gate. Sandbox: **13 passed / 3 integration-skipped**.

**§14. First Linux gate run (2026-06-11) — one HARNESS bug, demo code clean.** First-boot passed every DM-2 assertion (bundles installed, Local seeds, `GLOBAL PERSIST: falkor`, G-5 `round-tripped intact`); **second boot failed the `(no-op)` idempotency assertion** — but the container exited 0, i.e. the re-boot *worked*, it just took the fresh-mint path instead of reload. Root cause in `run_linux_tests.sh`, not the demo: it ran each boot via `compose up --abort-on-container-exit demo-backend`, which **co-stops `falkordb`** when demo-backend exits, then `compose down` — so boot 2 hit a torn-down DB, `find_by_name` missed, and the bundles re-installed cleanly (no no-op marker). Two more issues caught alongside: (1) the demo **Dockerfile didn't COPY `robot_demo/bundles/`** → `install_skill` would `FileNotFoundError` at runtime (added the COPY); (2) `Dockerfile` fix shipped separately. **Harness fix:** bring `falkordb up -d --wait` ONCE and keep it up across both boots (mirrors reality — the DB is a long-running service, only the consumer restarts); run each boot as a one-off `compose run --rm demo-backend`; add a deterministic pre-gate reset (`GRAPH.DELETE robot_demo` on the isolated demo graph + clear `./.mindsos-demo/server-db`) so first boot is a true empty-start and second boot a true reload; single `compose down` at teardown. No demo-code change — the reload-idempotency path was correct; the harness was tearing down the store it was supposed to test.

**DM-2 GATE GREEN ON LINUX (2026-06-11).** With the harness fix (`falkordb` up across both boots) + the Dockerfile `bundles/` COPY, the full gate passed: first boot empty-start (mint + per-device-type bundle install + Local seeds + `G-5 round-tripped intact`), second boot reload (`core@1.0(no-op)` / `manager@1.0(no-op)` — bundles no-op on the reloaded Global), 4/4 Episodes both boots, RAM+jitter measured. **PB-GG resolved live:** the persisted `installed-skills` record dicts round-trip the ADR-0182 codec and a fresh `CapacityLayer` accepts the schemaless loaded Global — no `read_at_version`/schema-reattach needed. **PB-JJ confirmed:** demo data isolated in the `robot_demo` graph. Process lesson (cost ~5 cycles): the Linux box only runs **committed + pushed + pulled** code — a fix in the Cowork working tree is invisible to the gate until the Mac commits it; a stale `.git/index.lock` silently no-op'd one commit ([[no-sandbox-git-mutations]]). **DM-2 done; zero `mindsos_*` edits.** Next: DM-3 (Seam C body adapter + atomic L3 capacities + live-motion wrapper).

**Pair-execution roles clarified (user, 2026-06-11) — strict.** Cowork = build/validate-core only; **Mac = git only** (`add` scoped paths / `commit` / `push`) and runs **no** code or tests; **Linux = `git pull` then run + test ALL code** (pytest + `run_linux_tests.sh`, the authoritative gate). Code reaches Linux via the GitHub remote, not file copy. Documented in `robot_demo/deploy/README.md` + memory `pair-execution-workflow`. Supersedes any earlier "run on the Mac" phrasing in §11.

## DM-2 build status — DONE (gate green on Linux 2026-06-11)

- [x] §4.0 DataStates — `installers.py` registers realm `robot` + 32 `robot.*` DataStates (idempotent triple), via the `core` bundle's L3 installer.
- [x] Skill bundles — 5 TOML bundles under `robot_demo/bundles/` (`core` + 4 device-type); `core`→all 4, type bundle→its device by `DeviceProfile.bundle_names`; installed through the real `install_skill` ADR-0180 gate (admin session, PB-BB); 26 L2 nodes mapped onto registered OWL/FrameNet/WordNet NodeTypes (PB-X); nodes-only, relationships property-encoded (PB-DD).
- [x] Local seeds — `seeds.py` writes per-brain F4-min embodiment as a `CapacitySnapshot` property-bag via `make_writeable` (PB-W); idempotent.
- [x] Per-device Falkor persistence — `persistence.py` named load-or-mint for the 4 Globals (PB-J/PB-AA); Globals persisted, Locals in-memory (PB-Z); isolated `robot_demo` graph (PB-JJ).
- [x] G-5 — isolated fixed-id probe round-trips the real dict-valued Episode through the ADR-0182 codec (PB-EE); `ok=False` fallback documented.
- [x] Tests — `robot_demo/tests/test_dm2_bundles.py` (sandbox 13 passed / 3 integration-skipped) + `run_linux_tests.sh` extended (DM-2 assertions; falkordb up across boots).
- [x] Linux gate — **green** (first-boot empty-start + G-5 intact; second-boot reload + bundle no-op; 4/4 Episodes both boots). PB-GG resolved live.
- [x] Zero `mindsos_*` edits. Follow-ups: **F8** (real `embodiment` role), **F9** (durable Local persistence + installer re-activation).

---

## DM-3 — Seam C: body adapter + atomic L3 capacities + live-motion + fault injection

**Started:** 2026-06-11 (Cowork, DM-3 chat). Reanalysis-before-code per the project posture. Two reanalysis passes against the shipped `sim/` + `robot_demo/` + `mindsos_capacity` code (user-confirmed all choices). Sandbox is 3.10 + **no MuJoCo/GL** → only the MuJoCo-free half is sandbox-validated; live motion gates on the Linux container.

### §15. DM-3 reanalysis — grounded pushbacks (flaw → options → chosen)

Grounded facts (read, not guessed): one `Cell` holds **both** arms + belt + both boxes; arms are driven **kinematically** (`move_to` sets qpos + `mj_forward`; `mj_step` only for settle/drop); the 46 clips are authored offline by `combo_*` → `web/anim_*.json`; the executable checklist (`sim/gen_all.py::checklist`) is **clip-level** (needs grasp+seated events); `Cell.capture()` already emits `[x,y,z,qw,qx,qy,qz]` per body; capacity bodies are stored as a **callable** (`declaration.implementation(**inputs, context=context)`), signature `def impl(context=None, **inputs)`, inputs keyed by DataState IRI; read-bodies (declared outputs) get `ctx.writeable=None`; `family_rule_for` keys **name-prefix then category** against `FAMILY_RULES`, which contains `mechanism`/`validate`/`perception` (NOT in `FUNCTIONAL_CATEGORIES`, but `ensure_category_graph` accepts any category — the `text`/`dream` precedent).

- **PB-KK (HIGH) — one shared `Cell`, not three.** A per-brain `BodyHandle` owning its own `Cell` puts a1/a2 in separate physics worlds → the cross-belt handoff (the DM-5 thesis) becomes impossible. Options: (a) one **SimEngine on its own thread** owning the shared `Cell`; per-brain `BodyHandle`s enqueue commands; (b) three Cells, merge later; (c) per-brain Cells + coordinator. **Chosen: (a).** One sim owner; serializes all `MjData` access (no worker-thread race); *is* the PB-E "pin sim to its own thread" + the escape-hatch seam.
- **PB-LL (HIGH) — "checklist-verified" is undefined for atomics.** `gen_all.checklist` is clip-level (requires grasp/seated events); a bare `move_to`/`grip`/`sense`/`conv.run` produces none. Options: (a) author an `atomic_checklist` over the produced frames covering only the *applicable* invariants (joint-smoothness, no rack/conveyor intrusion, rest/clearance) + a per-capacity **effect assertion** (pose reached / attach engaged / belt displaced / diag verdict); (b) stretch the clip checklist (needs fabricated events — fake); (c) skip (violates P-7). **Chosen: (a).** Full clip checklist returns at DM-5 when `combo_*` are wrapped.
- **PB-MM (MED) — ⬡ atomics wrap `Cell` *primitives*, not `combo_*`.** The combos are the ◆ assembled rows (DM-5) and each builds its own `Cell` + writes JSON via `_save`. **Chosen:** DM-3 `BodyHandle` wraps the `Cell` primitive layer (`move_to`, `move_to_q`, `grasp_side`/attach, `home`, extracted belt-sweep) via the `_sync` hook; combos get wrapped at DM-5. §2.3's "combo machinery live" reads as DM-5.
- **PB-NN (MED) — diagnose can't write its Local gap via `ctx.writeable`, and a frozen joint is invisible under kinematic drive.** `invoke` ships `writeable` only to zero-output write-bodies; `diagnose_actuators` declares `DS_DIAG_REPORT` (honest PRODUCES edge) → read-body → `ctx.writeable is None`. And zeroing a MuJoCo actuator does nothing when qpos is force-set. **Chosen:** gap-write via a **registration-time closure** over `make_writeable(brain_kl, brain_session)` (independent of `ctx`); fault injection is a `SimEngine.freeze_joint(arm, j)` switch enforced in the apply-step (clamp the frozen joint while applying the commanded delta to the rest → real commanded-vs-actual delta).
- **PB-OO (MED) — PB-F's order-menu cache can't exist at DM-3** (combos + planner are DM-5/DM-8; plan §8 puts cache *fill* at DM-8). **Chosen:** DM-3 builds the **cache mechanism + miss path** (lookup → miss → generate on a **generator thread** while the sim holds pose → checklist → submit, or honest motion dont-know on a checklist-failing miss) seeded only with the **atomic verified targets** it can prove now; full order-menu fill stays DM-8.
- **PB-PP (LOW) — no standalone belt primitive.** Belt motion lives only inside `combo_load_convey` (cosine-smoothed qpos sweep, housing clamp). **Chosen:** extract that exact verified sweep into `SimEngine.move_belt(direction, distance)`; `conv.run`/`conv.stop` wrap it (move-only-on-command).
- **PB-QQ (LOW) — `sim/` is a flat non-package script dir** (bare imports, writes `web/`, prints). **Chosen:** a thin adapter `sys.path`-inserts `sim/`, imports `motion as M`, drives `Cell` primitives, captures `c.frames` via `_sync`, never calls `_save`. **Zero `sim/` edits** (other chats hold parked `sim/` work).
- **PB-RR (LOW) — real jitter bar.** **Chosen:** SimEngine thread = `mj_forward` + qpos-set at the authored cadence (`timestep × CAP_EVERY`), all 4 worker pools live + one live generation in flight on the generator thread; gate **p99 step-interval ≤ 2× nominal**, record the distribution, confirm generation spikes land off the sim thread. MuJoCo physics is headless (no GL).
- **PB-SS (HIGH) — SimEngine must own the clock + per-actuator slots, not block one trajectory at a time** — else DM-5 belt+arm concurrency / throughput interleave is foreclosed. **Chosen:** clock-owning scheduler, one active-trajectory slot per actuator (a1/a2/belt), one capture per tick; `submit` returns a future resolved when that slot drains. DM-3 exercises one slot at a time but the model is DM-5-correct.
- **PB-TT (HIGH) — keep MuJoCo behind the seam for testability.** `motion.py` does `import mujoco` at top → un-importable in the 3.10 sandbox → the whole DM-3 surface would be sandbox-dark. **Chosen:** hard split — MuJoCo only in `sim_engine.py`/`body_adapter.py`; the capacity declarations, `motion_cache`, `atomic_checklist` (numpy over a frames array + injected `StructureSpec`), and `pose_frame` are MuJoCo-free and sandbox-tested. Mirrors `brain.py` discipline.
- **PB-UU (HIGH) — embodied capacities must register `session=None` into the brain's own CL *Global*.** DM-2 installed the `robot.*` DataStates into each device CL's **Global**; `register_capacity` validates inputs/outputs against `_metagraph_for(target_uid)`, so a brain-session (Local) registration hits a Local with no `robot` DataState graph → `validate_for_registration` raises. Per-brain CLs ⇒ Global-of-its-own-CL is still private. **Chosen:** register `session=None` (parity with the builtin catalog); diagnose's Local gap-write uses the separately-captured Local `writeable` closure.
- **PB-VV (MED) — kinematic live tick, not naive `mj_step`.** Naive `mj_step` playback drops unpinned free items under gravity and desyncs from the verified frames. **Chosen:** tick = apply active-slot frames → `mj_forward` → capture, items pinned (the clip recipe); `mj_step` reserved for explicit settle/drop sub-phases; throttle each captured frame to `dt = timestep × CAP_EVERY` (also the PB-RR nominal).

**Category/family decision (resolves the §4.1 "judgment-call" flag):** the plan's family names are **dont-know-shape keys**, not capacity categories — but they double as the capacity `category` cleanly: `move_to`/`suction_set`/`jaw_set`/`conv.run`/`conv.stop` → `mechanism` (OPTIONAL_RETURN); `sense_poses` → `perception` (DATASTATE_MARKER); `diagnose_actuators` → `validate` (VALIDATION_RESULT). `ensure_category_graph` accepts them (not-in-FUNCTIONAL_CATEGORIES is fine); `family_rule_for` resolves the intended shape by category. No new `Fn`; no `mindsos_*` edit.

**Scope note:** `profiles.py::embodied` lists `pick`/`place_at_cell`/`stage_at` — those are ◆ DM-5, NOT DM-3. DM-3 registers only the ⬡ set: `{a1,a2}.move_to`, `a1.suction_set`/`a2.jaw_set`, `{a1,a2}.sense_poses`, `{a1,a2}.diagnose_actuators`, `conv.run`/`conv.stop`. The DM-3 atomic roster is defined in `capacities.py` (not read from `profile.embodied`) to avoid pulling DM-5 names in.

### §16. DM-3 ↔ DM-4 pose-frame interface note (WS contract §2.3)

Aligned with the UI/comms side so DM-4 needs no adapter:
- **Canonical 3D `bodies` = free match.** `Cell.capture()` emits `[*xpos, *xquat]` = `[x,y,z,qw,qx,qy,qz]` (MuJoCo quat is wxyz), meters, MuJoCo world frame — exactly the reserved `bodies[name]` shape. `pose_frame.py` exposes this as the source of truth (future-proofs the 3D robot view at zero DM-3 cost).
- **2D `items`/`eff` `[x,y]` is the one reconciliation.** The mock's example coords are hand-authored, not a physical projection, so the transform must be *defined*. Proposed: **front elevation** `screen_x ← world_x`, `screen_y ← world_z` (so shelf-row motion reads vertically), with an affine `screen = a·world + b` to be pinned once the UI side confirms the cell view's coordinate box (or, if the cell view is cell-indexed, emit shelf `(r,c)` instead). DM-3 ships `pose_frame.py` emitting raw-meter `bodies` + the `(x,z)` projection with an identity affine placeholder + documented TODO; baking the constants is a one-line change, no reshape at DM-4.
- **Body → contract-id map:** items `box1`/`tube1`/`sheet1` match the sim; sim also has `box2` (flag for the cell view). `eff.a1` ← body `a1_suction`, `eff.a2` ← body `a2g_base` (the checklist's grip refs; the TCP *site* is not in the captured frame — can add the pinch point if wanted). Brain-id alias for DM-4 `state` frames: device_id `arm1→a1`, `arm2→a2`; `mgr`/`conv` identical.

### §17. DM-3 build (2026-06-11) — code landed, Linux gate pending Mac run

Built the DM-3 surface as additive `robot_demo` consumer code (**zero `mindsos_*` edits, zero `sim/` edits** — `sim/` is wrapped via `sys.path` insert, PB-QQ). The PB-TT split is real: the MindsOS-facing half is **sandbox-validated** against the real `mindsos_capacity`/`mindsos_knowledge` (3.10 + `tomli`); the MuJoCo half gates on Linux.

**Files (new):** `robot_demo/backend/{motion_cache,motion_checklist,live_motion,pose_frame,capacities}.py` (MuJoCo-free) + `{sim_engine,body_adapter,dm3_check}.py` (MuJoCo host) + `robot_demo/tests/test_dm3_capacities.py`. **Modified:** `backend/bootstrap.py` (guarded step-6 hook), `deploy/Dockerfile` (`COPY sim` + `COPY web`), `deploy/run_linux_tests.sh` (DM-3 gate step 6).

**Sandbox-validated (real `mindsos_capacity`/`mindsos_knowledge`):**
- **Registration + invoke** — the 10 atomic capacities (4 arm-suction + 4 arm-jaw + 2 conveyor) register `session=None` into the brain's own CL Global (PB-UU) under categories `mechanism`/`perception`/`validate`; `if_exists="upsert"` re-register is a no-op; `cl.invoke` returns the output keyed by the declared output DataState IRI (the `{OUTPUT_IRI: payload}` return shape — a bare dict payload is otherwise misread as the output-map). Implementation signature is `def impl(context=None, **inputs)`, inputs read by DataState IRI.
- **diagnose → Local gap (PB-NN/K)** — a frozen-joint report writes a `capacity_gap` `CapacitySnapshot` to the brain's **own Local** `capacity-state` via a registration-time `make_writeable(kl, session)` closure (diagnose is a read-body, so `ctx.writeable` is None — the closure is the only path); healthy → no gap. No Global write.
- **Subset checklist (PB-LL)** — `atomic_checklist` over a frames array + injected `StructureSpec`: PASS on a smooth clear move; FAIL on conveyor intrusion and on a jerk spike.
- **Live-motion policy (PB-F)** — `run_motion`: cache hit → play (no regen); miss → generate → checklist PASS → cache + play; miss → checklist FAIL → honest motion dont-know; generation exception → dont-know (no stall).
- **Pose projection (§16)** — `project_pose` emits 3D `bodies` (raw) + 2D `items`/`eff` (front-elevation `x,z`, affine-ready); arm/item/eff bodies only; affine scaling verified.
- **Bootstrap guard (PB-TT)** — `_maybe_build_bodies()` returns None without MuJoCo (logs + skips); the DM-1/DM-2 smoke is unaffected. **Full `robot_demo` suite: 23 passed / 6 integration-skipped** (DM-3 added 10 passed / 3 skipped).

**MuJoCo seam (Linux-gated; written, not run here):** `sim_engine.SimEngine` — one shared `Cell` (PB-KK), clock thread at `dt = timestep×CAP_EVERY` (PB-RR nominal), per-actuator slots a1/a2/belt with drain-futures (PB-SS), kinematic tick `mj_forward`+pinned (PB-VV), `freeze_joint` clamp (PB-NN), `probe_actuators` (commanded-vs-actual), generation on a **scratch** `Cell` (off the live tick, PB-E), `move_belt` extracted from the verified `combo_load_convey` sweep (PB-PP). `body_adapter.BodyHandle` wires cache+checklist+`run_motion`; `build_body_runtime()` = one engine + three handles. DM-3 atomic `move_to` targets are verified configs (`home`/`rest` via `move_to_q` joint-space — the smooth, branch-stable primitive; the hard grasp/carry combos are DM-5). The container gate is the greppable `robot_demo.backend.dm3_check` (the image ships `sim/`+mujoco but not `tests/`).

**No new `Fn`:** DM-3 is entirely consumer-side; the actuation atomics sit honestly under `mechanism`/`validate` (no missing-family gap worth a feature), and the pose affine is a DM-4 interface, not a MindsOS feature. F8/F9 (filed at DM-2) unchanged.

**Pending on the Mac/Linux 3.12 host (pair-execution, Mac commits scoped paths only — the tree holds other chats' parked `mindsos_*` work; NEVER `git add -A`):**
1. Stage exactly: `robot_demo/backend/{motion_cache,motion_checklist,live_motion,pose_frame,capacities,sim_engine,body_adapter,dm3_check}.py`, `robot_demo/tests/test_dm3_capacities.py`, `robot_demo/backend/bootstrap.py`, `robot_demo/deploy/Dockerfile`, `robot_demo/deploy/run_linux_tests.sh`, `confirmation_docs/ROBOT_DEMO_MINDSOS_DESIGN_LOG.md`, `confirmation_docs/ROBOT_DEMO_STATUS.md`, `confirmation_docs/ROBOT_DEMO_MINDSOS_PLAN.md`.
2. Linux: `git pull` → `bash robot_demo/deploy/run_linux_tests.sh` — the new **step 6** is the DM-3 gate (each atomic moves the live shared sim checklist-verified; fault detected; real jitter p50/p99 recorded). `RUN_PYTEST=1` also runs the host integration tests if mujoco is present.
3. Open build-time confirmations to capture on the gate: `move_to_q` joint-space targets pass `atomic_checklist` cleanly; `rest_pose` returns non-None for both arms (else the named `rest` target raises — `home` is the safe fallback); `freeze_joint` clamp produces a real commanded-vs-actual delta; the real jitter p99 vs 2× nominal (sets the bar that replaces DM-1's synthetic 20 ms).

Still **zero `mindsos_*` edits / zero `sim/` edits**.

### §18. DM-3 Linux-gate iteration (2026-06-11) — five grounded fixes, then green-in-sandbox

The container gate surfaced a chain of environment/grounding issues (each caught by the gate as designed). All fixed; the full `dm3_check` gate now runs **EXIT=0 in the 3.10 sandbox** (mujoco 3.3.0 installed there for the diagnosis) and the 3 `@integration` tests pass too (26 passed / 3 skipped).

1. **Missing vendored sim runtime (blocker).** `motion.py` imports `reach_validate` and `build_cell.build_spec()` loads `sim/menagerie/**` (Franka + Robotiq XML/meshes), but those were **never committed** (untracked, not gitignored) → absent on Linux after a clean pull. **Fix (user-approved):** committed `sim/reach_validate.py` + `sim/menagerie/` (94 files, ~40 MB). This is *adding previously-uncommitted vendored assets*, not editing `sim/` code — the "zero `sim/` edits" invariant (no edits to existing `sim/` source) holds.
2. **MuJoCo version mismatch (blocker).** DM-1 pinned `mujoco==3.2.3`, where `MjSpec.from_file` is instance-only; `build_cell.py` calls the static factory `MjSpec.from_file(path)`. **Verified empirically** (sandbox, ran `build_spec()` across versions): 3.2.0/3.2.3 fail, **3.3.0 builds clean**. Bumped `requirements-demo.in` to `mujoco==3.3.0` (no `sim/` edit — the pin was wrong, not the code).
3. **PEP 668 on the gate host.** `run_linux_tests.sh` step 1 `pip install pip-tools` fails on the externally-managed Mac-Mini Python 3.12 (`RECOMPILE=1` needed for the 3.3.0 lockfile). Added a `--break-system-packages` fallback.
4. **`move_belt` scalar-index bug.** `qadr_of(b)` returns a scalar qpos address; `d.qpos[addr]` is a scalar → `IndexError`. Fixed to the position slice `d.qpos[adr:adr+3]` (matching `motion.py`).
5. **Checklist over-strict + no-op move target.** The `atomic_checklist` rack-AABB + ends-clear checks (borrowed from the clip checklist) **false-positived the folded home keyframe** (measured: a1 `link5/6` at y=−0.914 sit inside the coarse rack AABB though in no cubby; a1's *rest* pose is clean; a2 has **no** `rest_pose` — IK returns None). And `dm3_check` moved home→home (no motion). **Fix:** the atomic hard gate is now **joint-smoothness + no-conveyor-collision** only (rack/ends are informational — rack-entry is a DM-5 pick-place-phase invariant enforced by the full clip checklist); the move target is a **bounded base-yaw joint delta** (verified clean for both arms: jerk ~1 mm, no conveyor intrusion, real 0.46-rad motion, no `rest_pose` dependency).

**Jitter, honest bar (PB-RR):** the first proxy was 4 pure-spin GIL-hogging threads → an unrepresentative **p99 582 ms**. Replaced with baseline + a burst-then-yield worker proxy. Real numbers (sandbox): **baseline p99 24.9 ms, under-4-workers p99 22.2 ms** vs 20 ms nominal — comfortably within the 2× target; the sim clock holds under realistic contention (the integrated bar under 4 live ILs is the full-bootstrap `measure.py` concern; escape hatch = sim in its own process, PB-E, unneeded so far).

Files changed across the iteration: `sim/reach_validate.py` + `sim/menagerie/` (vendored), `robot_demo/requirements-demo.in`, `robot_demo/deploy/run_linux_tests.sh`, `robot_demo/backend/{sim_engine,motion_checklist,dm3_check}.py`. Still **zero `mindsos_*` edits / zero edits to existing `sim/` source**.

**DM-3 GATE GREEN ON LINUX (2026-06-11).** The full `run_linux_tests.sh` passed end-to-end: DM-1/DM-2 spine green (both boots, 4/4 Episodes, bundles no-op on reload, G-5 intact — and the body build is correctly kept out of the bootstrap-only boots), then **step 6**: each ⬡ atomic moved the live shared sim checklist-verified, the frozen joint was detected (`a1_joint3`) and cleared, and the jitter bar recorded → `DM-3 LIVE MOTION PASS` → `ALL MANDATORY CHECKS PASSED — DM-1 + DM-2 + DM-3 gate green`. Process note: the gate caught all five issues in sequence exactly as the pair-execution loop intends (committed→pushed→pulled→run); the decisive accelerator was installing mujoco 3.3.0 in the Cowork sandbox to reproduce + fix the live path locally before the final container run. **DM-3 done; zero `mindsos_*` edits.** Next: DM-4 (Seam B BrainBus + `comms.*` + WS thin UI slice; the pose-frame interface §16 is ready, affine constants pending the UI side).

---

## DM-4 — Seam B: BrainBus + `comms.*` + WS thin UI slice

**Started:** 2026-06-12 (Cowork). Five reanalysis passes before code (the override mechanism + the param channel are the long poles), build, a UI-coordination + IP-sanitization round, then the Linux gate. **Zero `mindsos_*` edits.**

### §19. DM-4 reanalysis — grounded pushbacks (flaw → options → chosen)

The decisive findings were **probed against the real `mindsos_intelligence`/`mindsos_capacity`, not read**:

- **PB-WW (HIGH) — same-name v0 override.** To make the panels narrate the REAL chain, the demo's logic must run under the literal v0 IRIs. **Probe overturned the plan's assumption:** `register_capacity(if_exists="upsert")` is a *behavioural no-op* — capacity_layer.py:350-371 reuses the node + back-fills edges but **never re-assigns `self._declarations[iri]`** (first-registration-only), and the dispatcher resolves the impl through `get_declaration → _declarations`. So upsert keeps running the v0 builtin (`get_declaration(...).implementation is ours == False`; override calls 0). **Chosen:** override via `cl._declarations[iri] = clone-with-new-impl` (`comms.install_override`), cloning the shipped DataState contract so the bipartite edges stay correct. Verified: bodies fire, chain still emits HintSet→…→TaskRun.
- **PB-EEE (HIGH) — dispatched params reach phase 2 only via `task_pattern_iri`.** `plan_construction.build` passes `derive_initial_plan` only `{task_pattern_iri}` (no `structured_input`/`task_id`; `current_task_iri=None`). **Chosen:** the phase-1 `map_to_task_pattern` override encodes the target into `task_pattern_iri`; the phase-2 `derive_initial_plan` override (whose output the phase **discards** — a clean side-effect seam) decodes it and invokes the atomic. Round-trip verified.
- **PB-FFF (HIGH) — bus must route off the IL worker pool.** A Manager capacity that dispatches + awaits blocks an IL worker (`mgr=2`); the arm runs on its single worker (`arm=1`). **Chosen:** a **dedicated per-brain consumer thread** owns the inbox + resolves the correlation `Future` — routing never touches an IL worker, so synchronous dispatch→report is deadlock-free; the arm handler enqueues the lifecycle and replies from a completion callback (DEFER). Push-cache (PB-C) kept for `query_capabilities` only.
- **PB-XX/ZZ/AAA/BBB/GGG (resolved):** dispatch the shipped ⬡ `move_to` (not `place_at_cell`, ◆ DM-5); pose callback = one `deque.append`, WS server owns its own asyncio loop (`FrameHub.publish`→`call_soon_threadsafe`) — socket I/O never on the sim clock. **Pose affine fitted from captured sim coords** (`ax=0.5417, bx=0; ay=−0.5818, by=−0.7118`), projecting **top-down `(x,y)`** (UI cell view is top-down, not DM-3's front-elevation `(x,z)`) and **dropping `bodies`** (UI consumes `items`/`eff` only). `comms.*` register under `interaction`; `comms.report` produces `DS_DISPATCH_ACK` (`DS_REPORT_SENT` isn't registered).

### §20. DM-4 build + UI coordination + IP sanitization (2026-06-12)

Built additive modules: `bus.py`, `comms.py` (verbs + `install_override` + codec), `frames.py` (FrameHub + DemoEvents), `ws_server.py` (`websockets` — lighter than FastAPI; page served separately), `wiring.py` (flow→frames + pose stream + live `move_to`), `dm4_check.py`, `main.py` serve path. Tests `test_dm4_{bus,comms,frames,ws_server,wiring,sanitization}.py` + `ui_seam_verify.js`. Sandbox: **48 passed / 3 integration-skipped + node verify**.

- **L5 export/server coordination** (`ROBOT_DEMO_DM4_L5_EXPORT_COORDINATION.md`): grounded that the reasoning **chain survives post-lifecycle** (`il.mm.intelligence_mm` `chain` graph holds all 8 nodes after consolidation; each node `value` IS the dataclass → cross-refs present) → Mode-A serializes by reading `il.mm` after `run_lifecycle`, no orchestrator edit. **Phasing locked:** Mode-A export + `server_status` in DM-4; live import + `server_event` next; Mode-B reload post-DM-6. Schema locked v1.
- **IP sanitization (policy B):** scrubbed `wiring.py` text to behavior-level (`move to …`, `reported: …`, `Assign task`, `placed an order`) + `frames.py` parties to `Fleet`/`Library`; a banned-token **guard test** enforces a clean wire.

### §21. DM-4 Linux gate — grounded fixes, then green (2026-06-12)

Five-pass reanalysis surfaced **PB-HHH** *before* the gate: the chain writer mints IRIs from `task_scope`, set per-*brain* in `brain.py` → a brain's **2nd lifecycle crashes** (`IdentityError: Duplicate id 'hintset:demo-mgr:1'`; the first `place_order` after the smoke). **Fix:** `brain.run_task` builds a fresh `Orchestrator` per task with a unique `task_scope`; routed the smoke + command handler + arm dispatch handler through it; 3-order regression added (the unique scope also gives Mode-A its per-task chain slice).

Three gate iterations (each caught by the gate):
1. **Reload re-registers DataStates.** On a Falkor reload `install_skill` no-ops (digest match), so the bundle L3 installer doesn't re-register the `robot.*` DataStates into the fresh CL — `comms.*` (+ step-6 atomics) need them. **Fix:** `bootstrap.py` + `wiring.py` ensure `install_core_datastates(cl)` every boot (idempotent). (DM-3 dodged it — `dm3_check` never goes through `bootstrap`.)
2. **`dm4_check` broke on the `Reported` *state*** — but the report *message* follows it (canonical state-then-message), so the client closed first. **Fix:** break on the terminal report message.
3. **MuJoCo teardown segfault (exit 139)** with `body=yes`. **Fix:** stop the sim engine + `os._exit(rc)` after flushing. Step-7b (node UI-seam verify) made **skip-not-fail** when `demo_ui/` is absent (UI side's folder, not on the backend gate host).

**DM-4 GATE GREEN ON LINUX (2026-06-12).** `body=yes` (real `move_to` motion + 27 pose frames), all `✓` checks, `frame summary {hello:1, state:5, message:3, pose:27}`, `DM-4 GATE PASS` → `ALL MANDATORY CHECKS PASSED — DM-1 + DM-2 + DM-3 + DM-4 gate green`. The Manager dispatches an embodied arm through **both** lifecycles over the BrainBus, narrated as live, IP-sanitized WS frames. **DM-4 core done; zero `mindsos_*` edits.** Remaining: browser confirmation on the UI host (`presentation.html?live=ws://<host>:8765`); then **Mode-A export + `server_status`**; then DM-5 (◆ assembled capacities).

### §22. DM-4 increment — Mode-A L5 export + `server_status` (2026-06-12)

Two adversarial reanalysis passes against the locked §D schema (`ROBOT_DEMO_L5_EXPORT_IMPORT_PROMPT.md`) + the shipped `il.mm`/`consolidate.py`/`chain_artifacts.py`/`execution.py`, **all probed against the real stack, not read**. Decisions (flaw → options → chosen):

- **PB-1 (HIGH) — Episode→chain slice by `task_scope`, not `mm_root_ref`.** Probed: every Episode in a brain shares one `mm_root_ref` (`= il.mm.intelligence_mm.metagraph_id`) and the `chain` graph **accumulates every task's artifacts forever**. The discriminator is the per-task `task_scope`, embedded in BOTH the chain IRIs (`hintset:<scope>:N`) and the Episode node id (the Episode id IS the TaskRun iri — `consolidate.py` keys `episode_id=task_run.iri`; the node id is `…:episode:<user>:taskrun:<scope>:<seq>`). **Chosen:** derive the scope from each Episode id (`<scope>` has no inner colons → unambiguous split) and slice the chain by it. Zero new state; explicitly avoids `mm.root.task_run_ref` (holds only the *last* task — a trap).
- **PB-13 (HIGH) — opaque-token rewrite.** The lineage cross-ref IRIs the UI *requires* to draw the linked chain (`hint_set_ref`/`root_milestone_ref`/`task_run_ref`/…) ARE banned tokens (artifact type names + `demo-<device>`). Direct collision between two locked contracts. **Chosen:** `sanitize.TokenMap` rewrites every internal IRI to a stable per-snapshot opaque token (`n1`/`n2`/…). The UI matches edges by `iri ↔ *_ref` equality, which survives the bijection — lineage renders identically, nothing internal leaks. Verified in sandbox: `pipeline_runs[0].task_run_ref == task_run.iri` post-rewrite. The `?audience=internal` real-IRI view alternative is explicitly "not v1"; hashing leaks structure. This is the centerpiece decision.
- **PB-8/PB-16 — off-loop serialize + targeted reply.** `export_state` must NOT serialize on the WS loop thread (PB-8 — would stall socket I/O + the heartbeat) and `state_snapshot` must reach only the requester, not the hub broadcast (PB-16 — the UI auto-downloads JSON; a broadcast makes every viewer download one viewer's export). **Chosen:** the command handler runs the serialize on a daemon worker and replies via a new thread-safe per-connection `respond(frame)` seam on `ws_server` (marshals onto the loop via `call_soon_threadsafe`). `import_result` (next increment) needs the same seam, so it's paid now.
- **PB-3/PB-7 — wire deviates from the frozen §D to satisfy IP-sanitization (user-approved 2026-06-12: "deviate + note").** PB-3: the locked `server_status` keys `persistence.falkordb`/`globals_persisted`/`mindsos_version` are themselves banned tokens → emit `storage:"connected"` + `state_saved` + **no version**. PB-7: the snapshot's *structural* keys are the chain vocabulary (`task_run`, `pipeline_runs`) — the guard is scoped to string **values + free-text**, never the agreed schema keys the UI locked and renders generically. Both recorded in the coordination doc for the UI chat.
- **PB-17 — `task_input` capture.** Probed: the consolidated Episode stores only `task_input_ref` (a string) and the v0 `hint.global` emits an **empty** HintSet — so the resolved order is recoverable from neither. **Chosen:** `brain.run_task` records `task_input` keyed by scope (bounded FIFO) for the serializer. (Root gap = faithful `retention.resolve_refs` episode→payload reconstruction, **WSD-deferred** — the consumer-side capture is the demo workaround, not a new MindsOS surface.)
- **PB-15/18 — honesty framing (no new contract).** Probed `execution.py`: each brain has its OWN chain (`il.mm` is per-brain) — the Manager's chain holds the allocation decision; the arm's `move_to` runs in the **arm's separate lifecycle**, and even there the v0 leaf `StepExecutionRecord.capacity_iri` is a **notional Pipeline ref**, not the real motion. **Chosen:** render the chain as the real lifecycle *skeleton*, relabel the leaf to a generic "execute step", and never imply it shows `move_to`; the real motion outcome lives in the live `state`/`message` frames. Empty `replans`/`blame`/`dont_know` on the happy path → "not exercised", never hidden.
- **Resolved open items:** PB-11 — the bootstrap smoke runs via `run_task` (unique scope), so every brain has ≥1 chain-sliceable Episode at connect. `server_status` data: sessions via `brain.session`; `state_saved=result.persisted_global`; a single boot timestamp covers `uptime`/`since` (no Session-internal dependency, no `bootstrap.py` change). PB-20 — the heartbeat is an on-loop asyncio task (not a thread), teardown-safe with `os._exit`.

**Built (additive `robot_demo` only, zero `mindsos_*` edits):** new `sanitize.py` (canonical banned list + `TokenMap` + plain-label helpers + values-only `find_leaks` — shared by producers AND tests, kills drift), `serializer.py` (`build_episode_audit_snapshot`); edits `brain.py` (scope→task_input capture), `frames.py` (`snapshot_frame` + `server_status_frame`), `ws_server.py` (thread-safe `respond` seam + on-connect status + on-loop heartbeat), `wiring.py` (`export_state` handler + reverse alias + `make_status_provider`), `main.py` (`_start_ws` wires the provider), `dm4_check.py` (drives `export_state` + asserts a sanitized snapshot + 4-session status). Tests: new `test_dm4_export.py` (8 asserts incl. lineage-token consistency + clean wire), new `test_dm4_server_status.py`, extended `test_dm4_sanitization.py` (snapshot + status under the canonical guard). **Sandbox: full `robot_demo` suite 50 passed / 6 skipped**, plus a real-socket probe (hello → on-connect `server_status` → targeted `state_snapshot` → heartbeat) and an end-to-end serializer probe (episode consolidates, opaque tokens, `task_input` captured, `find_leaks==[]`).

**SHIPPED — Linux gate GREEN + browser-confirmed (2026-06-12).** `dm4_check` (extended) drove `export_state` and asserted the sanitized snapshot + 4-session `server_status` in-container → `ALL MANDATORY CHECKS PASSED — DM-1..DM-4` (commit `70d3cd9`). **Browser confirmation done live from Chrome on the Mac** via a direct console WS to the running `demo-backend`: `server_status` arrived sanitized (`storage:"connected"`/`state_saved`/`uptime_s`/4 display-name sessions/no version/no raw `user`), and `export_state(episode-audit, mgr)` returned a clean `state_snapshot` (`kind:"episode-audit"`, opaque `n*` refs, `task_pattern_iri:"move to home"`, nulled internal refs, captured `task_input`). The dashboard's empty cards / "producer pending" panel / no-download were **localized to the UI's live `datasource.js` wiring** (frames are real + arriving) — handed to the UI chat as B1–B3 (coordination-doc tail).

**Post-ship additions (this chat):** (1) `demo.sh` at the repo root — one-command backend control (`up/down/restart/logs/status/build/help`); `up` waits for `DM-4 WS SERVER LISTENING` then prints the dashboard URL with the LAN IP. (2) UI handoff delivered: a **real** audit fixture `confirmation_docs/fixtures/episode_audit_mgr.json` (two `succeeded` episodes — the demo has **no real refusal path until DM-5/6**, so no fabricated `dont_know` ships; that fixture follows when the embodiment gate is real) + the live-wiring to-do, both appended to the coordination doc. (3) **Decision — v0 milestone name `"root"` left as-is, not prettified**: inventing a friendlier milestone name would fabricate computed depth (the no-fabrication rule both chats hold); the UI renders Stage 3 as "Planned steps · N step(s)" and real milestone names arrive unchanged with DM-5/6's real plan tree. **DM-4 increment fully closed; zero `mindsos_*` edits. Next: DM-5.**

---

## DM-5 — ◆ assembled capacities + embodiment gate + allocation

**Started:** 2026-06-12 (Cowork). Three reanalysis passes (critical-design-reviewer posture) probed against the **real** `mindsos_intelligence`/`mindsos_capacity`, then a sandbox-first build (gate → allocation → ◆ motion → close-out). **Zero `mindsos_*` edits.**

### §23. DM-5 reanalysis — grounded pushbacks (flaw → options → chosen)

Two of the brief's headline asks were **over-promised**; the probes refuted them and reshaped the scope. Decisions (all probed, not assumed):

- **PB-1 (HIGH) — the cinematic `combo_*` clips can't be DM-5's live motion.** Read, not guessed: each `combo_*` (`combo_box_pick`/`combo_box`/`combo_tube*`/…) builds its **own** `Cell()`, runs up to ~45 s of IK *enumeration* (`pick_grasp_branch`, n_seeds=22), `print`s, is per-cell hand-tuned ("r0c2-approved"/"r1c2"), and `_save()`s JSON. And the DM-3 `SimEngine` slot model streams **arm qpos only** (`_apply_frame`) with **no mid-trajectory attach/detach channel** — a grasp→carry→release can't replay through it. Options: (a) extend `SimEngine` with an event-bearing trajectory + pre-fill the cache from `web/anim_*.json` → real *replayed* cinematic motion (big Linux-only sim work, start-pose coupling, demo-freeze on a miss); (b) live-generate combos on a miss (45 s walls, demo-freeze); (c) a real ◆ capacity that **composes the proven ⬡ atomics** (`move_to`→`grip`→`move_to`→release) over the live shared cell — real, live, graph-honest (`find_pipeline` sees the composition), small sim seam, **plainer on camera**. **Chosen: (c)** (user-approved). The authored-clip replay (a) is a later polish, not DM-5.
- **PB-2 (HIGH) — "real milestone names" is undeliverable without a `mindsos_*` edit.** Probed `plan_construction.build`: it hardcodes `writer.emit_milestone("root", 0, …)` and child names `f"m{depth}.{idx}"` — the names are **minted by the orchestrator**, not by any overridable capacity. The `install_override` seam swaps capacity *impls*, so it can control plan *depth* (override `is_leaf`→False + `decompose`→N specs) but **not the names**. Options: (a) edit `plan_construction` — violates zero-`mindsos_*`; (c) ship real *depth* + behavior-level *step labels*, names stay structural. **Chosen: (c)** (user-approved). **Rejected the brief's gate criterion + the coordination owed-item "real milestone names"** — restated as "real plan depth + real step labels." A probe confirmed a >1-leaf decompose survives execution + the per-leaf pipeline loop + consolidation (4 milestone nodes, no `IdentityError`).
- **PB-3 / PB-NEW (HIGH, favorable) — the wrong-gripper refusal IS reachable in shipped v0, but the lever is the per-CL override, not the global toggle.** Probed `orchestrator.run_lifecycle`: `_simplified` defaults `False` and `brain.py` never sets it → the dont-know branch is live. With `should_replan="continue"` (v0 default) and `predicate.sufficient`→`False`, the orchestrator runs `phase_6.diagnose` (a real `BlameVerdict`) → `task_run.status="failed"` → `consolidate(outcome_classification="dont_know")` (via `_OUTCOME_BY_STATUS["failed"]`). **PB-NEW:** the gate must be the **per-CL `install_override`** of `predicate.sufficient`/`phase6.attribute_blame` — NOT the module-global `set_sufficient_result` (shared by all 4 brains in one process → would refuse a concurrent arm's valid order). The predicate body gets **no task context** (`state={}`), so feasibility is computed in the arm's phase-1 `map` (which sees the dispatched item) and stashed on the brain; the override reports the stash. Safe because arms run `max_workers=1` (single-flight). `phase6.attribute_blame` is overridden too so the rich, **sanitized** refusal reason rides `blame.rationale` (the hardcoded `dont_know_reason="INSUFFICIENT"` is unused on the wire). **Chosen: drive the refusal through the real v0 path.**

### §24. DM-5 build (2026-06-12) — sandbox-validated; Linux gate pending

Additive `robot_demo` only, **zero `mindsos_*` edits, zero `sim/` edits**.

**The embodiment gate (the money beat + the UI's blocked refusal fixture):** new `feasibility.py` (item→acceptable-grasps vs body-`provides`; **fail-open** when no embodiment is seeded or the item is unknown — a config gap must never fake an on-camera "I can't"), `gate.py` (the two per-CL overrides + the single-flight stash + `gate_item` nested-invoke). `wiring.py` `arm_map` computes feasibility, `arm_derive` honestly refuses (no motion attempted, `GATED` cap badge); `brain.py` captures the refusal off the `TaskOutcome` keyed by scope (the chain never holds `blame` — only the `TaskOutcome` does); `serializer.py` renders a populated, sanitized `reasoning.dont_know`/`blame`. Real refusal verified end-to-end: tube → suction arm → `outcome_classification:"dont_know"` + `blame.rationale:"blocked — this gripper can't grasp a tube (wrong gripper)"`.

**Allocation + Plan ▸ Resolve producer:** new `allocation.py` — the deterministic 3×3 resolver (`OPEN_QUESTIONS §2`; absolute terms + relational `above/below/left/right/under`, fixed-order tie-break, `9→…→1`) + `make_allocator(events)` replacing the DM-4 fixed `decide=("arm1","home")` and emitting the WS-§5 `resolve` frame; `frames.py` `DemoEvents.resolve`. Cell-index space is UI (`i=row*3+col`, row 0 = top); the §4 motion maps it to `shelf_cell` with the sim's bottom-row-0 flip. `main.py` wires `make_allocator` into `_start_ws`.

**The ◆ assembled capacities (PB-1c):** new `assembled.py` — `pick`=approach→grip→lift, `place_at_cell`=carry→release→retract, `conv.stage_at`=belt sweep, each composing the ⬡ atomics over a `BodyHandle` duck (MuJoCo-free, sandbox-tested with a fake body recording the sequence; honest `dont_know` on a failed grasp / unsafe reach). Registered alongside the ⬡ atomics in `bootstrap.py` step-6 (the profile-declared IRIs `a{n}.pick`/`a{n}.place_at_cell`/`conv.stage_at`). The real cartesian reach is **Linux-gated** (mirrors DM-3 PB-TT): `sim_engine.py` `item_grasp_target`/`cell_target`/`generate_arm_reach` (the shipped `Cell.move_to` straight-line IK + `GRASP_R`; first-cut stand-offs **CALIBRATED AT THE GATE**, the DM-3 §18 precedent), `body_adapter.py` `_reach_cartesian`. `wiring.make_live_run_atomic` now runs the ◆ `pick`→`place_at_cell` (item from the gate stash, cell from the resolved target); a no-item dispatch falls back to the DM-3 base-yaw `move_to`.

**Codec:** `comms.encode_target`/`decode_target` extended to a 3rd `item` segment (the gate downstream needs the grasp requirement); the manager carries it manager-phase-1→phase-2; the arm reads it from the dispatched cmd. Dropped the vestigial `"capacity":"move_to"` from the cmd (it landed in the arm's exported `task_input` — a `find_leaks` leak the DM-5 gate test caught).

**Sandbox:** full `robot_demo` suite **67 passed / 6 skipped** (+14 DM-5: 3 gate, 8 allocation, 6 assembled minus overlap; existing DM-1…DM-4 green). The MuJoCo-host modules `py_compile`-clean. **Real refusal fixture delivered:** `confirmation_docs/fixtures/episode_audit_arm1_refusal.json` (a real, sanitized `dont_know` snapshot — `find_leaks==[]`), unblocking the UI's refusal branch (coordination owed-item 5a). New container gate `dm5_check.py` + `run_linux_tests.sh` step 7c.

**Honesty notes (for the UI / the gate):** milestone names stay structural (PB-2c — the UI keeps its count render); the ◆ motion is plainer than the 46 cinematic clips (PB-1c — clip-replay is later); the cartesian stand-offs are first-cut and were expected to iterate at the gate (DM-3 §18 pattern).

**SHIPPED — Linux gate GREEN (2026-06-13).** `run_linux_tests.sh` passed end-to-end (commit `98e7c5e`) → `ALL MANDATORY CHECKS PASSED — DM-1 + DM-2 + DM-3 + DM-4 + DM-5`. Step 7c (`dm5_check`) green: the Plan ▸ Resolve frame narrowed, the wrong-gripper arm surfaced the `GATED` badge + a real `dont_know` Mode-A export (populated `reasoning.dont_know`/`blame`, `find_leaks==[]`), the ◆ `pick`/`place_at_cell` registered + ran over the live sim. **Notable:** the ◆ cartesian motion passed **first try** — the first-cut grasp/cell stand-offs held with no §18-style iteration (the proximity-gated attach + the `Cell.move_to` warm-started IK absorbed the coarse offsets). Also in the gated commit (found via the UI coordination reply): the `sanitize.plain_task_pattern` 3-field-codec bug fixed (`"move to r1c1:tube"` → behavior-level `"place tube"`) + the refusal fixture re-exported. **DM-5 fully closed; zero `mindsos_*` edits. Next: DM-6** (degradation + replan).

### §25. DM-6 reanalysis — grounded pushbacks (flaw → options → chosen)

Three analysis passes + two sandbox probes against the shipped stack. The approach is **viable with zero `mindsos_*` edits** — both halves were proved live: forcing `decision.should_replan="replan"` minted real `ReplanRecord`s on the manager chain (`reasoning.replans` non-null `{replan_level, verdict:{decision,divergence}, invalidated_refs, spawned_refs}`, `find_leaks==[]`); forcing `sufficient=False` ran `phase_6.diagnose` → a real `BlameVerdict` (`reasoning.blame`/`dont_know` non-null, clean wire). The three passes moved from "is it viable" to concrete mechanical work — the sign to build. Decisions (probed, not assumed):

- **PB-25.1 (CRITICAL) — replan ≠ reroute.** Read `orchestrator.run_lifecycle` + `execution.run`: the replan loop re-runs `execution.run`, which is a **hollow v0 shell** (walks leaf milestones, emits a *notional* `StepExecutionRecord`, dispatches **no real capacity**). All real demo work (allocation, bus dispatch, gate, motion) runs **once in phases 1–2** (`mgr_map`→`MAP_IRI`, `mgr_derive`→`DERIVE_PLAN_IRI` in `plan_construction.build`), *before* the loop. So a `should_replan` override yields a genuinely orchestrator-minted `ReplanRecord` but a **no-op re-execution**; the actual reroute must live in `mgr_derive`. Options: (a) edit `execution.run` to drive the reroute — violates zero-`mindsos_*`; (b) skip the loop, no `ReplanRecord` — fails the gate; (c) reroute inside `mgr_derive` + fire a **one-shot** `should_replan` off a brain stash → one real record. **Chosen: (c).** The record honestly logs a real replan *decision*; the v0 re-execution stub is the same posture DM-5's gate already rides.
- **Fork 1 (CRITICAL, user-agreed) — present the `ReplanRecord` as an honest thin marker.** Two structural facts (probed): the record has **no rationale field** (`verdict` = `decision`+`divergence` only), and the real reroute runs in **arm1's separate lifecycle + separate KL** (`mgr.kl is arm1.kl → False`), so **no manager-chain ref can truthfully point at the recovery** — `spawned_refs`/`invalidated_refs` would have to be *fabricated* to "sell it." Options: (a) sell it as the reroute → fabrication, contradicts the real-not-faked thesis; (b) no record → forfeits a real artifact + fails the gate; (c) thin marker — emit the real decision, tell the reroute story in the live state/message frames, log that faithful on-chain replan content is a WSD/real-catalog item. **Chosen: (c).**
- **PB-25.2 (CRITICAL) — separate per-device KLs; the Manager cannot graph-read the arm's gap.** Probed: `build_device_kl` mints a fresh per-device Global; `mgr.kl is arm1.kl → False`, globals distinct. The arm's `diagnose` gap-write lands in the **arm's** Local. Options: (a) shared KL — architecture change; (b) the gap travels in the arm's **bus report/ack**, the Local `capacity-state` write stays the arm's introspective loop (on-thesis, for the audit/dream story), not the Manager's read channel. **Chosen: (b).** Doc wording "Manager sees the capacity-gap appear" = sees a gap-bearing report, not a shared-graph read.
- **Fork 2 (HIGH, user-agreed) — "partial degradation" is physically incoherent with a frozen joint.** `freeze_joint` clamps one DOF → *all* arm2 precise motion degrades, not just cylinder fine-grasp; `diagnose` returns a **joint-level** list, the reroute is **capability-level**. Options: (a) commit fleet-level partial (arm2's fine-grasp gap → reroute cylinders to arm1; conveyor still stages; never claim arm2 keeps placing carriers) — physically honest, sandbox-buildable; (b) commit scenario-as-written (arm2 keeps carriers, fine-grasp only) — would *fake* arm2's continued success; (c) build the **capability-typed gap** (identical representation under either framing) and run a Linux-gate experiment freezing each arm2 joint to see if a *box* place survives while a *tube* grasp fails — if yes, (b) is *earned*; if no, fall back to (a) with **zero rework**. **Chosen: (c)** — build the safe representation now, let the experiment pick the framing (`dm6_reach_experiment.py`). **RESOLVED → (a) fleet-level (gate-experiment run 2026-06-13):** the honest TCP-pose experiment showed the **healthy** arm2 fine grasp already misses by **676 mm**, far past the demo's own **300 mm** `set_grip` proximity gate (and the coarse place sits at 275 mm ≈ the gate) — the sim's reach precision (healthy residuals in the hundreds of mm) is too coarse to substantiate a working fine-grasp baseline to degrade, so scenario-as-written is **not physically earnable**. (The first cut mis-read this — it compared a 50 mm margin over an already-failing baseline and false-positived joints 4/6 as "earned"; the verdict now judges against the real 300 mm gate.) **Fork 2 = fleet-level partial:** arm2 is down on any **major (unrecoverable)** fault → reroute; no "arm2 keeps carriers" claim. (A **minor recoverable** perturbation is a different class — it recalibrates and keeps working per the PB-25.11 tiered closed-loop; "down on any fault" is scoped to the major tier — C1.) Zero rework — the capability-typed gap is identical. The fault is detected by `diagnose`/`probe_actuators` (PB-25.11), not by reach pose.
- **PB-25.5 (HIGH) — artifacts live in the manager chain; new fixture scope.** The reroute `ReplanRecord` + the dead-end `BlameVerdict` render on the **mgr** chain (probe used `mgr`); arm2's `dont_know` is in arm2's chain. DM-6 owes the UI a **manager-scope** `episode-audit` fixture — a recovery variant (`replans` non-null) + a dead-end variant (`blame` non-null). (Prior fixtures were arm-scoped.)
- **PB-25.6 (MED) — budget hygiene.** Default `DEFAULT_PER_TASK_REPLAN_BUDGET=5`; the probe looped 5× emitting 5 records. The manager's `should_replan` must flip its stash to "continue" after firing once → exactly one record. Keep the **arms'** `should_replan` at v0 "continue" so a frozen arm doesn't burn its own budget re-running dead IK.
- **PB-25.7 (LOW) — sanitize new rationales.** `replanrecord`/`blameverdict`/`capacity-state` are already banned; the replan/blame/cause strings must be behavior-level ("re-routed to the other arm after a detected fault"), no joint indices / internal codes. Extend `dm5_check`→`dm6_check` + `find_leaks`.
- **PB-25.9 (MED) — `dont_know.cause` is hardcoded `"embodiment_gate"` in `serializer.py`.** A fault dead-end would mislabel. Make `cause` fault-aware off the blame stash (behavior-level value, e.g. `"fault"`); verify it survives `find_leaks`.
- **PB-25.10 (CRITICAL) — the Manager is `max_workers=2`, not 1.** Probed `profiles.py`: arms are `max_workers=1` (the *only* reason the DM-5 gate stash is race-safe); `mgr` is `2`. A brain-level `setattr` stash for the one-shot `should_replan` is **not race-safe at 2 workers**. Options: (a) per-task-scoped stash — but `should_replan` doesn't cleanly receive the task key; (b) pin `mgr` to `max_workers=1` for the demo (config-only, no `mindsos_*` edit; the scenario runs orders sequentially). **Chosen: (b).**
- **PB-25.11 (CRITICAL — the true foundation; now the closed-loop verify→replan/report design) — the motion path is freeze-insensitive end to end, so the fault signal is the divergence probe, not motion `.ok`.** Read `gate.py:84` + `body_adapter`/`set_grip:362`: the arm's `sufficient` override reflects only the embodiment gate; `MotionOutcome.ok` checks the *planned* trajectory + jerk + submit success, **never the achieved pose**; and the grasp gate is a loose **30 cm** proximity. So a clamped joint does **not** make `pick`/`place_at_cell` report `dont_know` (verified: the first experiment cut saw all reaches "PASS" under every freeze). The ONLY honest detector is `probe_actuators` (commanded-vs-actual qpos). **Design (user-directed 2026-06-13 — closed-loop verification, the demo's headline differentiator vs open-loop industrial control):** after a commanded reach, **verify joint-level commanded-vs-actual** and compute a real **divergence magnitude** (max abs per-joint error). Drive the shipped 3-way `ReplanVerdict` from it (this maps 1:1 — `divergence` is a real shipped field, currently a 0.0 v0 stub the demo now populates): **~0 → continue**; **small/recoverable → `replan`, and re-plan to the goal FROM THE ACTUAL CURRENT POSE** (`generate_arm_reach` already seeds IK from the live pose — no servo-back-to-setpoint, no wasted backtracking; user's "replan, don't correct-to-plan" point); **large/persistent → report** (escalate to `sufficient=False` → dont-know + blame, surfaced to the user → manager reroute or dead-end). Bounded recalibration retries (2–3); a recalibrate that doesn't resolve escalates to report. The transient case self-heals (recalibrate tier); the frozen joint is the divergence that re-planning-from-current can't close (report tier → DM-6 reroute). **Honesty:** MindsOS supplies the real replan substrate (`should_replan`/`ReplanVerdict.divergence`/`sufficient`); the divergence *computation* + threshold policy + re-path are demo-supplied installed-capability bodies (the real MSUR/SCMS divergence body ships in unbuilt WSD) — message as "MindsOS replan substrate + installed verification capability," never "MindsOS ships closed-loop verification." Don't claim path optimality — claim "re-plans from where it is, no backtracking."
- **PB-25.12 (HIGH) — the report is status-only, which collides DM-5 and DM-6.** The dispatch report carries only `{status, task_pattern}` (`comms.py` `on_done`); `run_atomic`'s cause/stage never leaves the arm's chain. Once PB-25.11 is fixed the Manager sees a bare `dont_know` and **can't tell a fault (reroute) from a DM-5 wrong-gripper refusal (stay terminal)** — rerouting on bare status would silently "fix" the DM-5 tube and destroy that beat. Options: (a) reroute on any dont_know — breaks DM-5; (b) thread a sanitized behavior-level `cause` ("fault" vs "wrong gripper") through the report; Manager reroutes on fault, leaves wrong-gripper terminal. **Chosen: (b).**
- **PB-25.13 (HIGH, forced) — `diagnose_actuators` is never invoked in the live flow.** Registered + body exists, but only `dm3_check`/a unit test call `freeze_joint`+diagnose; the introspective gap-write (`_record_local_gap`) never runs in the actual flow. DM-6 invokes diagnose on motion failure in `arm_derive`.
- **PB-25.14 (HIGH, forced) — no fault-injection trigger.** `freeze_joint`/`clear_freezes` have no WS command / scenario hook — nothing can inject the fault during a live run. DM-6 adds an inject/clear trigger.
- **PB-25.15 (MED) — the reroute requires a conveyor re-stage (disjoint reach).** `seeds.py`: arm1 reaches `{shelf_L, belt_a1}`, arm2 reaches `{shelf_R, belt_a2}`, **no shared point** — only the conveyor bridges them (`belt_a1↔belt_mid↔belt_a2`). A cylinder at `belt_a2` is **unreachable by arm1**; rerouting means `conv.stage_at(belt_a2→belt_a1)` first. Options: (a) full re-stage (conv moves it, then arm1 picks) — richest "fleet recovers" beat, uses shipped `conv.stage_at`, more gate-time motion; (b) pre-stage the rerouted item neutrally (`belt_mid`/`belt_a1`) — simplest, weaker on the scenario's explicit "conveyor re-stages as needed." **Chosen: (a), gated on the Fork-2 experiment** (a conv reach check rides along); fall back to (b) if the motion won't hold.
- **PB-25.16 (LOW) — `task_id` hardcoded `"order-sub"` in `mgr_derive`.** Different-arm reroute is scope-safe (scopes differ by device); a *same-arm* retry would collide chain IRIs (PB-HHH). Mint a distinct `task_id` per dispatch as cheap insurance.

**Build order (dependency-sorted):** closed-loop core (`closed_loop.py` divergence + tier policy; sandbox unit-tested) + `sim_engine.disturb_joint` one-shot perturbation (gate-validated) → PB-25.11 arm-level verify→replan-from-current (recalibrate tier) + escalate-to-report (major tier) via the should_replan-stash → PB-25.13/25.14 (diagnose + fault/perturbation triggers) → PB-25.12 (typed cause through report) → reroute in `mgr_derive` + one-shot manager `should_replan` (mgr pinned to 1 worker, PB-25.10) → dead-end coda (`sufficient=False` + fault-aware blame/`cause`, PB-25.9) → PB-25.15 conv re-stage (gated on experiment) → manager-scope fixtures + `dm6_check` (PB-25.5/25.7). The Fork-2 + conv-reach experiment (`robot_demo/backend/dm6_reach_experiment.py`) runs first at the gate; Fork 2 resolved = fleet-level (major tier).

**§25.A — closed-loop verification + tiered divergence (user-directed 2026-06-13; consistency-reconciled C1–C5).** Tier strategy = **A** (build a recoverable-perturbation injector so the *recalibrate* tier is shown live, not just narrated). Two loops, both **thin-marker** (the real behaviour is in the motion/manager bodies; the orchestrator emits markers since its re-execution is hollow, PB-25.1): **inner** = arm-level verify→replan-from-current (recalibrate), bounded retries, real-divergence `ReplanRecord`s on the **arm** chain; **outer** = manager reroute on a reported major fault, one-shot `ReplanRecord` on the **manager** chain. Divergence is now a **real** value (supersedes the UI "0.0 placeholder" note — re-coordinate: the replan entry can carry a sanitized minor/major band). Perturbation injector = `robot_demo/backend/sim_engine.py` one-shot additive offset auto-cleared on slot drain (demo code, **not** vendored `sim/`). Magnitude separates the tiers (small delta → recalibrate; freeze → large commanded-vs-actual → report); persistence backstops (recalibrate exhausts budget → report). Messaging: MindsOS replan substrate + installed verification capability; no optimality claim. **Refinement (dm6_check gate run 2026-06-14):** the recovery (disturb→recalibrate) path passed first try, but the report path needs **two detectors**, not one — the per-reach commanded-vs-achieved divergence catches a *transient* (the additive disturb always shows a fixed magnitude), but a *persistent frozen joint the approach IK doesn't happen to command to move* leaves the reach matching its target (div≈0) and hides. So when a reach verifies OK, the verified-approach also runs a proactive `probe_actuators` (the reliable frozen-joint detector, scenario L-3 "self-diagnosis"): a dead actuator the motion didn't exercise still reports. Ordered after the reach so it never consumes the one-shot disturb (which diverts to recalibrate first). This is more on-thesis — the introspective health probe is the major-fault detector, the reach-divergence is the transient detector. **Both paths now gate-green: `DM-6 GATE PASS` (2026-06-14)** — `dm6_check` shows the recovery episode (`succeeded` + real recalibration replan) AND the fault episode (`dont_know` + blame), `find_leaks==[]`. The arm-side closed loop (tasks 3+4) is complete; the manager-side reroute (task 6) is next.
