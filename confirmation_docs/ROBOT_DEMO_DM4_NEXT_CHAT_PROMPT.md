# Robot Demo — DM-4 next-chat prompt

We are building the MindsOS Robot Demo. Design and the MindsOS-integration plan are settled — do not re-litigate. **DM-1 (deployment + bootstrap), DM-2 (L2 seeds + device-type skill bundles + per-device Falkor persistence), and DM-3 (Seam C: body adapter + atomic L3 capacities + live-motion + fault injection) are SHIPPED and verified green on the Linux server.** This chat executes **DM-4 (Seam B: BrainBus + `comms.*` + `trace.demo_events`; the G-1/PB-B chain-artifact resolution; and the first thin vertical UI slice — PB-G).**

## Read first, in this order — this prompt deliberately does NOT repeat what's in these files
1. `CLAUDE.md` (root) — the 5-layer stack + working conventions.
2. `confirmation_docs/ROBOT_DEMO_MINDSOS_PLAN.md` — the build plan. Binding for DM-4: §0 decisions **P-2** (direct-to-MindsOS; the Seam-B four-message contract survives as the internal inter-brain protocol, implemented as `interaction.*`/`comms.*` capacities) + **P-6** (plan execution is an L3 capability); **§2.1** (Seam B — the four `comms.*` verbs over an in-process `BrainBus`, **PB-C push-cache** model); **§2.2** (Seam A — backend↔browsers: WS `place_order`/`teach`/`sort` → Manager task submissions; `trace.demo_events` → ring buffer → WS `state` frame; control-token is DM-8, not here); **§4.3** (the `comms.*` + `trace.demo_events` rows) + **§5.2** (the v0-catalog problem); **§7** gap rows **G-1** (real planning catalogs / v0 probe) — note **G-9** UI is DM-8, DM-4 is only the thin slice; **§8** the "DM-4" row + its gate; **§10 PB-B** (chain-artifact authorship — the real criterion), **PB-C** (push-cache, deadlock class), **PB-G** (thin UI slice from DM-4); **§11** the open build-time verifications routed to DM-4 (**P3** `if_exists="upsert"` edge re-emission; **P1** the v0 plan-phase `plan_construction`/`execution` dispatch IRIs).
3. `confirmation_docs/ROBOT_DEMO_MINDSOS_DESIGN_LOG.md` — the decision/pushback record. Read **§1–§2** (the grounded probes that decide DM-4: `CapacityContext.mm_handle` is **read-only** and `ChainArtifactWriter` is built inside `Orchestrator.run_lifecycle` and never handed to a capacity body → §5.2 option-3 is dead; **same-name override under the literal v0 IRIs** — `capacity:process:identity`, `capacity:hint:global`, `capacity:decision:derive_goal`, `capacity:decision:map_to_task_pattern` — is the only honest path; the plan-2 phases' dispatch IRIs are **not yet traced** — that's a DM-4 probe; **P3 upsert edge re-emission** still to verify) and **§15–§18** (DM-3: the shared `SimEngine` + per-brain `BodyHandle`, the `pose_frame.py` DM-4 interface §16, the embodied atomics now registered `session=None` into each brain's CL Global, the `robot.*` DataStates, the mujoco≥3.3.0 + sandbox-repro facts).
4. `confirmation_docs/ROBOT_DEMO_WS_CONTRACT.md` — **DM-4 is the PRIMARY owner of this contract** (read the WHOLE doc, plus `demo_ui/datasource.js` + `demo_ui/mock_ws_server.js` as the worked reference emitter): the `hello`/`state`/`message`/`pose`/`reset` server→browser frames, the `command` browser→server frames, and the **§3 field-persistence** rules (which fields carry forward vs are transient). DM-3 already shaped the `pose` frame (`robot_demo/backend/pose_frame.py`): the 3D `bodies` map is a free match; the 2D `items`/`eff` use a front-elevation `(x,z)` projection with an **identity affine placeholder** — bake the real constants once you confirm the cell-view coordinate box with the UI side (design-log §16). Brain-id alias for `state`/`message`/`pose`: device_id `arm1→a1`, `arm2→a2`; `mgr`/`conv` identical.
5. `confirmation_docs/ROBOT_DEMO_SCENARIO.md` §0a + §5 — the canonical model + the frozen contracts (reference; the Pipeline-artifact contract is DM-6). `confirmation_docs/ROBOT_DEMO_OPEN_QUESTIONS.md` for the control-token / resolver context (control-token itself is DM-8).
6. `demo_ui/` — the frozen **v10** dashboard: `presentation.html` (`?live=ws://<host>:<port>` is the go-live switch), `datasource.js` (the UI seam), `mock_ws_server.js` (runnable reference), `resolve.js` / `graph.js` (the §5 "no live producer yet" panels — future, not DM-4). Record + backlog: `confirmation_docs/ROBOT_DEMO_UI.md`. **`web/player.html`** is the separate baked-clip player.
7. `robot_demo/` — the shipped DM-1+DM-2+DM-3 code. DM-4 adds the bus + comms + WS, reusing the shipped seams: `bootstrap.py` returns a `BootstrapResult` holding the shared `sim_engine` + per-brain `Brain` structs (`session`/`kl`/`cl`/`il`/`orch`/`dispatcher`); the `SimEngine` exposes `subscribe(cb)` for the pose stream; `pose_frame.project_pose(...)` builds the WS pose payload; `capacities.py` is the closure-injection pattern to copy for `comms.*`/`trace.demo_events`. Read `robot_demo/backend/{bootstrap,brain,capacities,sim_engine,body_adapter,pose_frame,main}.py` + `robot_demo/deploy/{Dockerfile,run_linux_tests.sh}`.
8. Memory: `robot-demo-dm3-sim-runtime` (mujoco≥3.3.0; vendored sim assets now committed; **sandbox-repro tip**: `pip install mujoco==3.3.0 tomli --break-system-packages` then `python3 -m robot_demo.backend.dm3_check` exercises the live path locally — use the same trick to debug DM-4 before the container gate), `robot-demo-ui-v10`, `robot-demo-ui-verification` (no Chromium in the sandbox — verify UI via pure-module node tests), `pair-execution-workflow`, `no-sandbox-git-mutations`.

## DM-4 scope (plan §8 "DM-4" + §2.1 + §2.2 + §5.2/PB-B + G-1 + PB-G)
- **BrainBus** (`robot_demo/backend/bus.py`) — in-process per-brain inbox `queue.Queue` + a publish/subscribe map (§2.1). Injected into the capacity implementations as a closure at registration (same pattern as the DM-3 `BodyHandle`); if the topology ever splits into processes only `bus.py` changes.
- **`comms.*` capacities** — register in every brain's CL the four frozen Seam-B verbs (`query_capabilities` **push-cache, PB-C** — brains push their capability report to the Manager at boot + on every `capacity-state` change, no synchronous cross-brain round-trip; `dispatch`; `report`; `share_to_peer` is the F1-min transfer mechanism but the beat-4 transfer itself is DM-7). Consume/produce the DM-2 `robot.*` DataStates; declare `inputs`/`outputs` for the PRODUCES/CONSUMES edges.
- **`trace.demo_events`** — a trace-family capacity registered in all brains that appends thinking events to a ring buffer the WS broadcaster drains into the `state` frame (§2.2).
- **WS server** (FastAPI + websockets, demo-only deps already in `requirements-demo.in`) — emit `hello`/`state`/`message`/`pose`/`reset` per the contract; accept `command` frames (`place_order` → a Manager `run_lifecycle` submission; `play`/`pause`/`reset`). Subscribe the pose broadcaster to `SimEngine.subscribe(...)` and project via `pose_frame`.
- **G-1 / PB-B chain-artifact authorship (the must-not-skip criterion)** — so the UI's thinking panels narrate the REAL L5 chain, not v0 placeholder fiction: register the demo's real Phase-1 logic under the **literal v0 IRIs** (design-log §1) so `phase_1.run` emits honest `HintSet`/`MappingResult` from the dispatched capacity's outputs. **Probe** `if_exists="upsert"` edge re-emission (P3) and trace the plan-2 phases' (`plan_construction`/`execution`) dispatch IRIs first.
- **Thin vertical UI slice (PB-G)** — WS `state` frames + at least one live thinking panel in `presentation.html?live=...`, growing with each beat. **Not** the control token or graph tab (DM-8).

## Gate (plan §8 "DM-4")
Manager dispatches an embodied brain end-to-end through **both** lifecycles (mgr orchestrator → `comms.dispatch` → arm lifecycle runs the dispatched capacity → `comms.report` → mgr), **visible in a browser panel** over live WS frames. **Reconcile first (pushback):** the plan's gate text names `place_at_cell`, which is a ◆ **assembled** capacity (DM-5) — DM-4 should dispatch a shipped **⬡ atomic** (e.g. `move_to`) or a DM-4-scoped step instead; decide and record. Plus the standing spine (§9): MindsOS cumulative gate stays green; **zero `mindsos_*` edits** (additive registration only); the DM-1+DM-2+DM-3 gates stay green; extend `robot_demo/tests/` + `robot_demo/deploy/run_linux_tests.sh`. DM-4 is the **first browser-linked** test — verify the UI headlessly (no Chromium in the sandbox: pure-module node tests against `datasource.js` + a stub socket, per the `robot-demo-ui-verification` memory) and gate the live WS end-to-end on Linux.

## Do-nots (later DMs)
No Manager *real* cognition / planning catalogs (`comprehension.match_items`, `planning.fulfill_order`, the ◆ assembled / ★ learned composites — DM-5/DM-6). No learn loop, peer transfer (beat 4), embodiment gate, or replan (DM-6/DM-7). No control token, graph tab, or remaining v10 affordances (DM-8). **Zero `mindsos_*` edits** — hard rule, plan §9.1.

## Conventions
Critical-design-reviewer posture (project instructions). Record every decision/pushback in `ROBOT_DEMO_MINDSOS_DESIGN_LOG.md` (continue the §-numbering); any net-new MindsOS feature → a new `Fn` in `DEMO_DERIVED_FEATURES_NEXT_CHAT_PROMPT.md`; update `ROBOT_DEMO_STATUS.md` + the plan §8 row at the milestone, and write `ROBOT_DEMO_DM5_NEXT_CHAT_PROMPT.md` at the end. **Pair-execution (strict):** Cowork builds/validates-core (the sandbox can now run the live sim — `pip install mujoco==3.3.0 tomli --break-system-packages`); **Mac only `git add` (scoped paths — the tree holds other chats' parked work) / `commit` / `push`** (watch for a stale `.git/index.lock` that silently no-ops a commit while `push` says "up-to-date"); **Linux `git pull` then runs + tests ALL code** (the authoritative gate — only ever runs committed→pushed→pulled code). The demo owns its Dockerfile (`FROM mindsos:<phase>-prod`, **mujoco≥3.3.0**), reuses the root `falkordb`, and isolates demo data in the `robot_demo` graph (PB-JJ).

**Before writing code:** reanalyze the DM-4 plan against the shipped `robot_demo/` + `demo_ui/` code + the WS contract, list your pushbacks with options and your choice (especially: the gate's `place_at_cell`→atomic reconciliation; the v0-IRI same-name-override vs the plan-2 phase dispatch IRIs; whether the WS/bus run on the SimEngine clock thread or their own; the pose-affine constants), and restate DM-4's deliverables + gate.
