# Phase 25 — Confirmation

> Generated by `mindsos confirm-phase --phase 25 --notes-file …`. Tester
> may hand-edit before commit. CI's smoke check verifies "exists and
> non-empty", not field structure.

---

## phase_number

25

## phase_title

The phase title as it appears in `confirmation_docs/PHASE_MAP.md` §3 / §4 / §5.
Example: `Tooling infrastructure`

L0 / cross — Server: cross-user-read substrate (read_other_local + LocalPersister Protocol + SessionProtocol in KL)

## git_sha

94c747dad54899857aef2e6fa3ec4414bb782b4b

## image_build_hash

unknown (image not built locally — run `docker compose build`)

## falkordb_version

falkordb/falkordb:v4.18.3@sha256:30c530c193ac48cb6ea8c6cae745f793d2c098a0a138f7b3e46c1d90848845ba

## automated_test_summary

- count: 2931
- passed: 2903
- skipped: 28
- failed: 0
- suite_hash: sha256:3e09235e78e3b8a07faa7021479ed14472dd16d9c90e5d47725eee3b7da2a575
- pytest_summary: 2903 passed, 28 skipped, 109 warnings in 1865.08s (0:31:05)

## tester_notes

Free-form. What you observed, anything surprising, deviations from PHASE_MAP's
pass criterion, open questions for the next phase chat. This is the
load-bearing field — read by future phase chats per PHASE_MAP §0.

## Tester notes

### Test results

* Host-native syntax check (`python3 -m py_compile` on all 9 prod files + 16 test files): GREEN.
* Host-native pytest on `tests/phase_25/` (Linux Python 3.12.3): 34 passed in ~30s.
* Host-native pytest on cumulative `tests/`: 2881 passed / 28 skipped after 3 hotfix rounds.
  * 6 pre-existing host-native flakes observed (4 failures + 2 errors in phase_05c/06/08/13 CLI tests); all PASS in isolation; not Phase 25 caused. Docker run cleans them.
  * 10 Phase 13 docker-image-completeness tests + 1 Phase 00 FalkorDB-connection test fail host-native (need docker/FalkorDB containers); pre-existing infrastructure gaps unrelated to Phase 25.
* Docker pytest on `mindsos:phase25-test` (canonical phase-confirm GREEN path): **2903 passed, 28 skipped, 0 failed in 30:31**.

### Manual smoke (host-native per `feedback_smoke_harness_host_native.md`)

All 4 exit codes (0/2/3/10) verified end-to-end:

| Verb | Result | Exit |
|---|---|---|
| `mindsos server bootstrap admin-caller` | admin bootstrapped | 0 |
| `mindsos server login admin-caller` | token written | 0 |
| `mindsos server user create alice --role user` | user created | 0 |
| `mindsos server admin read-local alice` | per-role counts + xrefs/intergraph_edges | 0 |
| `mindsos server admin read-local alice --json` | 4-key JSON; role_graphs sorted alphabetically | 0 |
| `mindsos server admin read-local ghost-user-id` | UserNotFoundError | 2 |
| `mindsos server admin hard-delete-user bob --json` | JSON includes new `local_dump_existed: false` (PB-39) | 0 |
| `mindsos server admin hard-delete-user second-admin` (with seeded `pending_mutations` row) | UserHasPromotionHistoryError (PB-30) | 10 |
| `mindsos server admin read-local <other_user>` (non-admin caller) | PermissionDeniedError (PB-R6-05 outer cap-check) | 3 |

Key verifications:
* PB-39 — `local_dump_existed` audit-extra + return-value field surfaces correctly in both text and JSON output formats.
* PB-30 — UNION ALL pre-check fires inside `admin_tx`; admin_tx rolls back cleanly on raise (user row survives).
* PB-R6-05 — outer cap-check pre-runs before any state lookup; non-admin probing a nonexistent user gets exit 3 (not exit 2 — info-leak fix verified).
* ADR-0008 I-S3 invariant — admin reads never flush; transient install + never-save-on-teardown observed via `fail_save_for` test injection (`test_read_other_local_transient_no_flush_is3.py` GREEN).

### Hotfix ledger

| ID | Symptom | Fix | Files |
|---|---|---|---|
| B-25-T1 | `test_no_mindsos_server_imports_in_mindsos_knowledge` false-positive on docstring literal `from mindsos_server` in `mindsos_knowledge/types.py:26`; literal-substring grep doesn't distinguish real imports from docstring mentions. | Switch from substring scan to `ast.parse` + walk for `ImportFrom`/`Import` nodes; ignores docstrings cleanly. | `tests/phase_25/test_import_isolation_phase25.py` |
| B-25-T2 | 5 CLI tests fail with `TypeError: CliRunner.__init__() got an unexpected keyword argument 'mix_stderr'` (Click 8.2 removed kwarg per memory `project_mindsos_phase_18_implemented.md` B-18-T2). Additionally used wrong entry point (`server_app` direct + `MINDSOS_STATE_DIR` env var that doesn't exist) — bootstrap+login state didn't persist between CliRunner invocations → "not logged in" exit 1. | Rewrite all 3 CLI tests to Phase 22 canonical pattern: `from mindsos_cli.app import app`; env setup via `MINDSOS_SERVER_DB` + `HOME` + delenv `MINDSOS_TOKEN`; full subcommand path (`["server", "admin", "read-local", ...]`); use `server user create` CLI verb instead of direct `insert_user` SQL; drop `mix_stderr=False`. | 3× `tests/phase_25/test_read_local_cli_*.py` |
| B-25-T3 | 3 Phase 22 baseline tests fail with `assert {'local_dump_...': False} == {'prior_role'...}` — the additive `local_dump_existed` key (Phase 25 PB-39 + ADR-0013 §am3) added to `EVT_HARD_DELETE_USER.extra_json`. Phase 22 tests pinned the 3-key shape. Canonical `feedback_phase_baseline_literal_audit.md` decay class. | Extend asserted dict in all 3 Phase 22 baseline tests to the 4-key shape with `local_dump_existed: False` (Phase 22 callers pass `persister=None`, default to False per backward-compat semantics). | `tests/phase_22/test_audit_payload_shapes.py`, `tests/phase_22/test_hard_delete_user.py` |

### Round 6 + Round 7 design-log §am-impl picks (reconciled at impl)

7 picks reconciled the design log §5 literals against probe-verified reality; see notes-phase-25.md §2 for the full table. Summary:
* R6-01 — `errors.py` not `exceptions.py` (extend existing file).
* R6-02 — inline dict-comp over `mg.graphs.values()` instead of nonexistent `mg.graphs_by_role.items()`.
* R6-03 — conftest resets both `_installed_locals` and `_mutex_registry`; fresh `KL.bootstrap()` per test.
* R6-04 — use `kl.local_metagraph(user_id)` for cold-start (canonical name + auto-ensured roles) instead of raw `Metagraph(name=f"local_{user_id}")`.
* R6-05 — `_require_or_audit` at top of `read_other_local_summary` (info-leak fix; Phase 21 precedent).
* R7-01 — `HardDeleteUserResult` appends `local_dump_existed` as 6th field (preserve existing `ts` 5th field).
* R7-02 — `read_other_local` ctx mgr `conn.commit()` after `write_audit` (Phase 21 admin_query_audit precedent).

### Verdict

Phase 25 ships as scoped. Retirement-escape clause unused — substrate is correct + has live consumer + smoke-verified end-to-end. No design-log §amendment required beyond the §am-impl addendum captured in notes-phase-25.md §2.

## timestamp_utc

2026-05-23T21:50:50Z

## mkdocs_pages_updated

- (none — `git diff main..HEAD -- docs/` returned no changes)
