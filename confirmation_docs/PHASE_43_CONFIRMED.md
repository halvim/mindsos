# Phase 43 — Confirmation

> Generated by `mindsos confirm-phase --phase 43 --notes-file …`. Tester
> may hand-edit before commit. CI's smoke check verifies "exists and
> non-empty", not field structure.

---

## phase_number

43

## phase_title

The phase title as it appears in `confirmation_docs/PHASE_MAP.md` §3 / §4 / §5.
Example: `Tooling infrastructure`

L2 schema-v2: 4 new role-graphs + mutation_discipline runtime invariant + per-NodeType storage_mode + bootstrap applies_after field + ADR-0094 §am-1 detector + consolidate Episode retarget + episodic_memories body finalize

## git_sha

db9beff774324a169633e8a993889749f33cee1b

## image_build_hash

unknown (image not built locally — run `docker compose build`)

## falkordb_version

falkordb/falkordb:v4.18.3@sha256:30c530c193ac48cb6ea8c6cae745f793d2c098a0a138f7b3e46c1d90848845ba

## automated_test_summary

- count: 3616
- passed: 3608
- skipped: 8
- failed: 0
- suite_hash: sha256:b76892f246beac7dcb48d54486e1b0a4eba3a666d1917486a7d3e25548411ce1
- pytest_summary: 3608 passed, 8 skipped, 109 warnings in 1955.40s (0:32:35)

## tester_notes

Free-form. What you observed, anything surprising, deviations from PHASE_MAP's
pass criterion, open questions for the next phase chat. This is the
load-bearing field — read by future phase chats per PHASE_MAP §0.

Rail A slot 2 of 11 in the post-Phase-38 plan. Shipped per the design
pass closure at `confirmation_docs/PHASE_43_DESIGN_LOG.md` + impl-time
amendments at §9.

Saturation: 18-round design pass closed pre-impl (R0/R0b drift
reconciliation + R1 impl-locks + R2 amendment-text drafts + R3
cross-check); 3-round pre-impl pushback pass during ship chat (P1-P5
→ Q1-Q5 → R1-R5; 1 round reversal-free; satisfied "diminishing
returns, execute" gate); 6 in-flight impl-time discoveries (R6-R10 +
R11/R12 process carry-forwards).

Implementation surface: 16 commits on `phase-43` branch — PR1 = 8
commits (commit 0 design closure landing + 7 per design log §5.3) + 2
gate-driven follow-ups (5b for R8 top-level mindsos_knowledge
re-exports; 5c for R9 phase_13 schema-v2 expected fields + ADR
sentinels backtick wrapping). PR2 = 6 commits per design log §6.3 + 4
gate-driven follow-ups (1b for mindsos_admin _GLOBAL_ROLE_ORDER tuple
parity with knowledge-side _GLOBAL_NAMED_ROLES 6→9 expansion; 2b for
dual-scope role rejection fix — pending-promotions + learned-parameters
in BOTH _GLOBAL_NAMED_ROLES + _LOCAL_NAMED_ROLES needed
_GLOBAL_ONLY_ROLES / _LOCAL_ONLY_ROLES set-difference helpers; 3b for
17 test-corpus updates across phase_13 + phase_14 + phase_15a + phase_25
+ phase_34 + phase_36 + phase_39 + phase_43 for 6→9 Global count + 2→5
Local count + IRI registry 3→7 entries + Phase 39 schema-shape
rewrite for Phase 43 body + consolidate retarget fixture rename; 4b
for phase_12 role-set CLI + UPPER_LAYER_ROLES expectations).

Cumulative gate (Linux docker pytest): PR1 = 3544 passed / 0 failed / 8
skipped (31:43). PR2 = 3608 passed / 0 failed / 8 skipped (32:24).
mkdocs build clean (17 pre-existing carry-forward broken-link
warnings; zero new from Phase 43 doc additions).

Critical reversals against pre-R0 framing (per design log §3
reconciliation):
- L2Schema(Schema) subclass placement at `mindsos_knowledge.schemas._base`
  per ADR-0153 §amendment-1 — NOT an L1 amendment to
  `mindsos_core.Schema` (R0 N4 probe + PB-43-6 + R0a-10).
- `storage_mode` is per-NodeType property (only `LearnedParameter.value`
  in Phase 43 scope) — NOT a per-role-graph class field (NPB8-1).
- `bootstrap.py` field-only at Phase 43; Kahn topological-sort scheduler
  defers to Phase 44 per L2-37 split (NPB11-1).
- `consolidate.py` retarget at Phase 43 (Memory→Episode + memory_id→
  episode_id) — NOT deferred to Phase 48 (R0 PB-43-9).
- Detector form (`tools/check_phase_43_confidence_state.py`) — NOT a
  migrator (R0 PB-43-10 + Phase 39 PB-8 precedent).
- 6 disciplines, not 5 (`append_only` added for problem-trace per
  R0a-4/S3).
- ADRs 0151/0152/0153 already Accepted on disk per R0a-3 — Phase 43
  IMPLEMENTS contracts; does NOT re-ratify.

Impl-time discoveries (design log §9.1 R6-R12):
- R6: `memory_contains_episode` shipped as regular EdgeType (not
  IntergraphEdgeType). ADR-0152 §7 named it IntergraphEdge but
  `IntergraphEdgeType` lives on MetagraphSchema (ADR-0148 + Phase 05b),
  not on per-graph Schema. Both Episode + Memory NodeTypes in same
  role-graph per Chat B D-B47 "inside the same role-graph";
  within-role-graph EdgeType matches actual data shape.
- R7: design log §5.1 referenced `docs/_workbench/L2_CHAT_DECISIONS.md`
  but file moved to `confirmation_docs/L2_CHAT_DECISIONS.md` per
  Chat C IL-9 workbench migration. PR1 commit 7 edits at actual path.
- R8: PR1 commit 2 added L2Schema/Discipline/StorageMode exports at
  `mindsos_knowledge.schemas` only; sentinel tests imported from
  `mindsos_knowledge` top-level per Phase 13 schema-builder re-export
  convention. PR1 5b gate-fix added top-level re-exports.
- R9: PR1 gate caught 3 test failures resolved at 5c (phase_13
  test_advisory_property_constants v2 expected fields per ADR-0152
  schema-v2 + ADR-0094 §am-1 confidence drop; phase_43
  test_adr_amendment_sentinels backtick-wrapped form for
  `` `mindsos_core.Schema` is unchanged ``).
- R10: docker test image rebuild required after each Mac push;
  `mindsos-test` service in docker-compose has no source bind-mount.
  Documented in HANDOFF §9 + POST_PHASE_38 §1.
- R11: pair-execution discipline established as default —
  Cowork (sandbox) prepares file content via Edit/Write tools;
  user runs git on Mac; Linux runs cumulative gates via docker.
  Cowork sandbox `.git/` is read-only.
- R12: 6-step confirm-phase workflow established — Cowork drafts
  notes-phase-N.md content + layer title + tester_notes copy-blocks;
  tester edits notes file + runs `mindsos confirm-phase` + pushes.

PR2 gate-fix-up cascade: 4 follow-ups (1b, 2b, 3b, 4b) needed beyond
the 6 PR2 boundary commits. Phase 39 ran 2 follow-ups (5b, 5c) total;
Phase 43 ran 6 total (PR1 + PR2). Higher follow-up count tracks the
larger scope-rewrite surface (9 schema audits + 4 new schemas +
KnowledgeLayer.discipline_for + bootstrap dual-scope semantics + 12
existing test-corpus expectations).

Surprises / deviations:

- Design log §6.3 violated own §117 "each commit must be independently
  buildable" rule in TWO places (P2: consolidate.py retarget vs
  tests/phase_33 fixture updates; Q1: tests/phase_13/test_dispatch.py
  exactly-N sentinel vs new schema additions). Both fixed pre-impl by
  co-locating test updates with the schema/code changes at the same
  commit. Carry-forward to future design-pass closures: run a
  buildability scan over locked commit boundaries before ratification.

- Dual-scope role-graph semantic — `pending-promotions` +
  `learned-parameters` appear in both `_GLOBAL_NAMED_ROLES` and
  `_LOCAL_NAMED_ROLES` (Phase 43 §am-5 NEW concept). Original
  scope-rejection in `ensure_*_role_graph` was binary; surfaced as
  79 cascade ERROR + 185 FAILED at PR2 first gate. Fix at 2b
  introduced `_GLOBAL_ONLY_ROLES` + `_LOCAL_ONLY_ROLES`
  set-difference helpers. `learned-parameters` has per-scope
  discipline split (Local: mutable_with_retention; Global:
  admin_authored) routed via the `scope` kwarg on
  `build_learned_parameters_schema` + special-case dispatch in both
  `ensure_*_role_graph`.

- `admin_authored` runtime enforcement in `KLWriteHandle.write_and_validate`
  is admin-gating only (raises `MutationDisciplineError` on writes
  without `_is_admin=True`). Field-level edit enforcement (per-property
  `validate_mutation_discipline` calls at the per-field write boundary)
  is deferred alongside the first L3 capacity that EDITS an existing
  node — Phase 43 capacity surface is CREATE only.

- L2-37 split (NPB11-1): `applies_after` declarations ship at Phase 43
  (`_APPLIES_AFTER_BY_ROLE` dict + kwarg on `ensure_*_role_graph`);
  Kahn topological-sort scheduler that CONSUMES the declarations
  defers to Phase 44 per L2-37(consumer) split row.

- Design-chat-close process gap (P1) — Phase 43 design chat
  (2026-06-03) closed without committing the 4 modified + 2 untracked
  closure files. Resolved via split-land (Commit A on main for Phase
  39 §9 backfill + POST_PHASE_38 §1 IL-3 row; PR1 commit 0 carries
  Phase 43-specific closure artifacts). Carry-forward to future
  design-pass chats: commit closure artifacts BEFORE ending.

- Pre-impl ship chat ran 3 pushback rounds with explicit user confirm
  between each round ("reanalyze the plan and list your pushbacks").
  Pattern: 1st round (P1-P5) raised before reading the design log;
  2nd round (Q1-Q5) raised after reading; 3rd round (R1-R5) raised
  after probing specific code paths. Pattern useful — caught the
  buildability rule violations (P2 + Q1) before commit boundaries
  locked.

Carry-forwards to Phase 44 (per design log §9.6 + HANDOFF §3.1.13):
- L2-37(consumer) Kahn topological-sort scheduler consuming the
  Phase 43 `applies_after` declarations.
- L2-39 `EVT_READ_OTHER_LOCAL_EPISODIC_MEMORY` audit constant +
  capability registration.
- L2-41 KL retention surface (`kl.read_at_version` +
  `kl.retire_version` per ADR-0153 §4 lazy inline-on-retire
  reference-stability framing).
- Pair-execution discipline (Cowork ↔ Mac ↔ Linux).
- 6-step confirm-phase workflow.
- ADR transcription parity probe as default R1 step 0.
- Docker test image rebuild discipline.

`phase-43-confirmed` tag will be cut at the squash-merge commit
on `main` after `mindsos confirm-phase --phase 43` runs on the
post-squash main per Phase 39 §9.5 pre-confirm-phase squash-merge
discipline.

## timestamp_utc

2026-06-04T12:30:08Z

## mkdocs_pages_updated

- docs/_workbench/L2_FUTURE_WORK.md
- docs/_workbench/STREAM_A_BACKLOG.md
- docs/api/knowledge/identifiers.md
- docs/concepts/global-local.md
- docs/concepts/glossary.md
- docs/concepts/identifiers.md
- docs/concepts/knowledge-lifecycle.md
- docs/concepts/mutation-discipline.md
- docs/concepts/promotion-bridge.md
- docs/concepts/role-graphs.md
- docs/concepts/storage-tiers.md
- docs/concepts/user-local-authoring.md
- docs/decisions/adr/0044-memories-move-to-local-per-user.md
- docs/decisions/adr/0045-per-role-iri-builders.md
- docs/decisions/adr/0094-confidence-pipeline-level.md
- docs/decisions/adr/0139-hybrid-invariant-home.md
- docs/decisions/adr/0143-kl-write-handle-pattern.md
- docs/decisions/adr/0146-l3-symmetric-write-invocation-contract.md
- docs/decisions/adr/0147-l3-per-flow-write-capacity-build-pattern.md
- docs/decisions/adr/0150-l2-knowledge-lifecycle.md
- docs/decisions/adr/0151-l2-storage-tiers.md
- docs/decisions/adr/0153-l2-mutation-discipline.md
- docs/decisions/adr/0154-alignment-naming-canonical.md
- docs/decisions/summary/knowledge.md
- docs/dev/internals/capacity.md
- docs/usage/knowledge/episodic-memories.md
- docs/usage/knowledge/iri-cli.md
- docs/usage/knowledge/memories.md
- docs/usage/knowledge/overview.md
