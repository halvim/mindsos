# Phase 12 — Confirmation

> Generated by `mindsos confirm-phase --phase 12 --notes-file …`. Tester
> may hand-edit before commit. CI's smoke check verifies "exists and
> non-empty", not field structure.

---

## phase_number

12

## phase_title

The phase title as it appears in `confirmation_docs/PHASE_MAP.md` §3 / §4 / §5.
Example: `Tooling infrastructure`

L1 → L2 transition: Identifiers + role IRIs + REF_TYPES (ADR-0045 closure)

## git_sha

a17307e2843a4d92f75f1f5fa75a0e6e31f06667

## image_build_hash

sha256:f4810d1ab7596b9a1acdc9ec67cc0e96a6429b3e802ed607190bdb98ae1d0e5d

## falkordb_version

falkordb/falkordb:v4.18.3@sha256:30c530c193ac48cb6ea8c6cae745f793d2c098a0a138f7b3e46c1d90848845ba

## automated_test_summary

- count: 1897
- passed: 1890
- skipped: 7
- failed: 0
- suite_hash: sha256:f26981b97771c711190c5f29d3983a9ec5f3f341147684a36706f7b744059269
- pytest_summary: 1890 passed, 7 skipped, 109 warnings in 1087.54s (0:18:07)

## tester_notes

Free-form. What you observed, anything surprising, deviations from PHASE_MAP's
pass criterion, open questions for the next phase chat. This is the
load-bearing field — read by future phase chats per PHASE_MAP §0.

══════════════════════════════════════════════════════════════════════
Phase 12 — L2 Identifiers + role IRIs + REF_TYPES (ADR-0045 closure).
SHIPPED 2026-05-16.
══════════════════════════════════════════════════════════════════════

## Headline numbers

* Phase 12 isolated suite: 108 collected — 1 skipped in container
  (test_ref_types_and_roles.py::test_adr_0044_amendment_1_user_id_charset
  — ADR file lives in parent dir, not COPYd into runtime image;
  pytest.skip per design, mirrors Phase 11's test_adr_0134_amendments.py
  4-skip pattern).
* Cumulative: 1890 collected = 1890 passed / 0 failed / 7 skipped /
  109 warnings in 1090.53s (18m10s) inside the test image.
* Cumulative delta from Phase 11 (1780) = +110 (+108 Phase 12 isolated
  + 2 net adjustments from sentinel-paths extension fanout).
* Step-0 audit predicted 0 prior-phase cascade across 9 grep probes;
  impl confirmed 0 prior-phase test patches. Second cleanest pre-impl
  audit after Phase 11. See PHASE_12_DESIGN_LOG.md §3.
* 4-package version parity (PB-1 doctor flip): mindsos_core /
  mindsos_cli / mindsos_instances / mindsos_knowledge all at
  0.0.0+phase12; manifest.toml [mindsos] phase = "12"; image tags
  mindsos:phase12-{prod,test}.

## What landed (mapped to PB-1..22 + ADR-0044 amendment-1)

* mindsos_knowledge/__init__.py + identifiers.py (~340 LoC) +
  exceptions.py — NEW top-level package (PB-1; first L2 phase).
  Slim port of v3 mindsos_knowledge/identifiers.py + 7 net-new
  upper-layer builders per ADR-0045 + 5 new prefixes + table-driven
  _KINDS_PER_ROLE replacing v3's hard-coded if/elif (PB-9).
* 14 IRI builders shipped (PB-2 + PB-20 builder-count correction):
  - Seed (v3 verbatim): dolce_iri, oewn_{synset,sense,lemma}_iri,
    framenet_{frame,lu,fe}_iri (7).
  - Upper-layer (ADR-0045 net-new): pipeline_iri, pipeline_step_iri,
    task_pattern_iri, subgoal_template_iri, memory_iri,
    problem_trace_iri, capacity_snapshot_iri (7).
  Full ADR-0045 closure sentinel test confirms all 14 are exported.
* alignment_role(role_a, role_b) graph-name helper per PB-4 —
  NOT a version-qualified IRI; parse_iri rejects it; round-trip
  via direct args, not parse_inverse.
* Table-driven parse_iri + is_version_qualified_iri + ParsedIri
  dataclass (PB-9 + PB-10). Round-trip contract: full-string equality
  (parse_iri(build(*args)).full == build(*args)) for all 14 builders.
  Per-builder inverse field helpers deferred per-consumer-phase.
* REF_TYPES frozenset (7 entries incl PROMOTED) per ADR-0047; L3
  parity test deferred to Phase 27 per PB-3 (L3 doesn't exist yet).
* Ref-key helpers: global_ref_key, local_ref_key, REF_TYPE_KEY +
  8 role constants + 3 frozensets (SEED_ROLES / UPPER_LAYER_ROLES /
  ALL_ROLES) per PB-12.
* user_id charset ^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$ enforced in
  memory_iri + capacity_snapshot_iri per PB-11; documented in
  ADR-0044 §Revisions amendment-1 per PB-17. Phase 18 inherits as
  free invariant.
* capacity_snapshot_iri opaque-body lock per PB-8 — body embeds
  colon-bearing capacity_iri (per ADR-0066) + ISO8601 taken_at;
  parser leaves opaque after `snapshot:`; full-string round-trip
  holds; field-level inverse deferred to Phase 28+.
* KnowledgeError (base) + RefFormatError exception hierarchy per
  PB-7 + PB-21 (independent root from CoreError; parallels
  mindsos_instances pattern).
* mindsos knowledge CLI sub-subgroup per PB-16 + PB-22 + PB-6:
  `iri build|parse|validate` + `ref-types --list` + `roles --list
  [--seed-only|--upper-only]`. Exit codes 0/1/2 per project policy.
* doctor --self-test extended from 3-pkg to 4-pkg version-string
  parity (PB-1 + PB-19 probe 6). New knowledge_version field in
  the JSON report payload.
* Import isolation regression test per PB-18 — mindsos_knowledge
  modules import nothing from mindsos_cli or mindsos_server.
  AST-walk over all .py files in the package. Establishes the
  discipline Phase 25's SessionProtocol seam will enforce more
  rigorously.
* PHASE_MAP §Phase 12 row full rewrite per PB-13 (mirrors Phase 11
  PB-1+3+12+16 rewrite pattern).
* Phase 11 carry-forward re-carried-forward per PB-5: MetagraphSchema
  scanner → Phase 13/14; ADR-0134 Proposed → Accepted flip → Phase 15;
  migration playbook fill → Phase 15.

## Hotfix ledger (B-12-T*)

NONE. First phase with zero hotfix cycles since Phase 11 (also zero).
Both clean-audit phases — design rigor + Step-0 9-probe sweep paying
off. Cumulative streak: 2 phases with zero post-design hotfixes.

## Manual smoke verification

8 smoke boxes covered, all green:

* Smoke 1 — `mindsos knowledge iri build --role memories --kind
  memory --version 1 --user-id alice --memory-id m1` →
  `memories-1:memory:alice:m1`. Confirmed builder + CLI dispatch.
* Smoke 2 — `mindsos knowledge iri parse memories-1:memory:alice:m1
  --json` → role=memories, kind=memory, body=alice:m1. Confirmed
  parser table-driven kind extraction.
* Smoke 3 — `mindsos knowledge ref-types --list --json` → 7-entry
  alphabetical list incl PROMOTED. Confirmed ADR-0047 starter
  vocabulary ports verbatim.
* Smoke 4 (extra A) — `mindsos doctor --self-test --json` → ok=true,
  knowledge_version=0.0.0+phase12. Confirmed PB-1 3→4-pkg flip lands
  end-to-end.
* Smoke 5 (extra B) — `mindsos knowledge roles --list` → 8 rows with
  seed/upper tier labels. Confirmed PB-22 net-new verb.
* Smoke 6 (extra C) — `iri validate` × 3 cases — valid
  (dolce-dul-4.0:PhysicalObject) → exit 0; invalid (not-an-iri) →
  exit 1; alignment graph-name (alignment:lexicon<->concepts) →
  exit 1. Confirmed PB-4 alignment-is-not-an-IRI lock.
* Smoke 7 (extra D) — `iri build --role capacity-state --kind
  snapshot --version 1 --user-id alice --capacity-iri
  capacity:text:tokens --taken-at 2026-05-16T00:00:00Z` →
  `capacity-state-1:snapshot:alice:capacity:text:tokens:2026-05-16T00:00:00Z`.
  Confirmed PB-8 opaque-body round-trip works.
* Smoke 8 (extra E) — `iri build --role memories --kind memory
  --version 1 --user-id "bad:colon" --memory-id m1` →
  RefFormatError: Invalid user_id 'bad:colon' — must match
  ^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$ (ADR-0044 §amendment-1); exit 1.
  Confirmed PB-11 + PB-17 charset enforcement lands at the right
  layer with the right error message + ADR cross-reference.
* Smoke 9 (extra `--help`) — `mindsos knowledge --help` → 3
  subcommands listed (iri / ref-types / roles). Confirmed CLI
  sub-subgroup registration.

## Cross-chat dependencies

### Closed (Phase 11 → Phase 12)

* phase-11-confirmed tag (commit 2eca5c5) was the Phase 12 branch
  point.
* All Phase 11 surfaces (loader policy, migration scanner, LoadReport,
  CLI verbs) unmutated by Phase 12.
* Phase 11's `confirmation_docs/PHASE_11_NEXT_CHAT_PROMPT.md` (left
  untracked at Phase 11 ship) swept into the Phase 12 commit.

### Forward (Phase 12 → Phase 13+)

* L2 Phase 13 (Schemas): consumer for role constants + role-graph
  schema vocabulary; first MetagraphSchema-scanner trigger candidate.
* L2 Phase 14 (KL bootstrap): consumer for ref-key helpers
  (global_ref_key, local_ref_key, REF_TYPE_KEY), alignment_role
  graph-name helper, REF_TYPES frozenset; also drives ADR-0128
  Proposed → Accepted flip via MetagraphView.follow_ref.
* L2 Phase 15 (Importers): consumer for IRI builders (DOLCE / OEWN /
  FrameNet) + drives ADR-0134 Proposed → Accepted flip when first
  importer consumes Phase 11's migrate_from output; fills in
  docs/dev/migration-playbook.md content.
* L2 Phase 16 (Promotion): consumer for pipeline_iri /
  pipeline_step_iri + task_pattern_iri / subgoal_template_iri.
* L0 Phase 18 (Server user store): inherits user_id charset per
  ADR-0044 §amendment-1.
* L0 Phase 25 (SessionProtocol seam): consumes import-isolation
  invariant established in Phase 12 PB-18.
* L3 Phase 27 (DataStates + capacity primitives): owes the REF_TYPES
  parity test deferred per PB-3.
* L3 Phase 28 (12 categories): consumer for capacity_snapshot_iri
  (PB-8 — owns the field-level inverse helper deferred from Phase 12).
* L3 Phase 30 (Pipeline finder): consumer for problem_trace_iri.

## Carry-forward (out of Phase 12 scope; re-carried-forward per PB-5)

* MetagraphSchema scanner → Phase 13 (Schemas) or Phase 14 (KL
  bootstrap).
* ADR-0134 Proposed → Accepted flip → Phase 15 (Importers).
* docs/dev/migration-playbook.md full content → Phase 15.
* ADR-0134 §amendment-3 → reserved for first KL consumer's structural
  feedback (Phase 15).
* Per-builder inverse field helpers (capacity_snapshot, pipeline,
  task_pattern, memory, problem_trace) → per-consumer phase
  (Phase 16 / 28 / 30 etc.).
* Per-edge alignment IRI builder (if Phase 14 needs one) → Phase 14
  alignment metagraph bootstrap.
* REF_TYPES L3 parity test → Phase 27.
* Server-side user_id charset enforcement → Phase 18, inherits the
  regex locked in PB-11 + ADR-0044 §amendment-1.

## ADR matrix (Phase 12 touches)

| ADR | Pre-Phase-12 | Phase 12 action |
|---|---|---|
| 0010 | Accepted | No edit; PB-18 isolation test enforces. |
| 0014 | Accepted | No edit; isolation test forbids reverse-direction. |
| 0044 | Accepted | + §amendment-1 (user_id charset); STAYS Accepted per Model C amendments-via-Revisions rule. |
| 0045 | Accepted | No edit; 14 builders ship as ADR declares; closure sentinel test added. |
| 0047 | Accepted | No edit; 7-element frozenset ports verbatim. |
| 0066 | Accepted | No edit; capacity_snapshot_iri embeds capacity_iri as opaque body per PB-8. |
| 0067 | Accepted | No edit; L3 parity test deferred to Phase 27. |
| 0134 | Proposed | No edit; STAYS Proposed (no Phase 12 KL consumer). |

## Next action

Awaits squash-merge of phase-12 → main, then
`git tag phase-12-confirmed <main-sha>` per
feedback_release_tag_after_squash_merge_only.md (tag MUST point at
the main commit containing this CONFIRMED doc, not the phase-12
branch tip).
══════════════════════════════════════════════════════════════════════

## timestamp_utc

2026-05-17T05:28:52Z

## mkdocs_pages_updated

- docs/api/core/client.md
- docs/api/core/integrity.md
- docs/api/core/loaders.md
- docs/api/core/metagraph-snapshot.md
- docs/api/core/metagraph.md
- docs/api/core/repositories.md
- docs/api/core/soft-delete.md
- docs/api/core/wal.md
- docs/api/core/xref.md
- docs/api/knowledge/identifiers.md
- docs/api/knowledge/ref-types.md
- docs/changelog/CHANGELOG.md
- docs/concepts/identifiers.md
- docs/concepts/identity.md
- docs/concepts/references.md
- docs/concepts/soft-delete.md
- docs/dev/internals/core.md
- docs/dev/internals/snapshots.md
- docs/dev/migration-playbook.md
- docs/dev/repo-layout.md
- docs/usage/core/persistence.md
- docs/usage/knowledge/iri-cli.md
