# Phase 26a — Confirmation

> Generated by `mindsos confirm-phase --phase 26a --notes-file …`. Tester
> may hand-edit before commit. CI's smoke check verifies "exists and
> non-empty", not field structure.

---

## phase_number

26a

## phase_title

The phase title as it appears in `confirmation_docs/PHASE_MAP.md` §3 / §4 / §5.
Example: `Tooling infrastructure`

cross (Core + admin + server) — FalkorDB persistence wiring (Integration A substrate)

## git_sha

2e20ebf43854e6571e77bc1dafc7a4cd3f23f5a5

## image_build_hash

unknown (image not built locally — run `docker compose build`)

## falkordb_version

falkordb/falkordb:v4.18.3@sha256:30c530c193ac48cb6ea8c6cae745f793d2c098a0a138f7b3e46c1d90848845ba

## automated_test_summary

- count: 2951
- passed: 2923
- skipped: 28
- failed: 0
- suite_hash: sha256:2503944dcb48d4f9b50baa97ab0699b839013f9bdd799b1969d5fe09094ef3ef
- pytest_summary: 2923 passed, 28 skipped, 109 warnings in 1838.14s (0:30:38)

## tester_notes

Free-form. What you observed, anything surprising, deviations from PHASE_MAP's
pass criterion, open questions for the next phase chat. This is the
load-bearing field — read by future phase chats per PHASE_MAP §0.

### Test results

* Docker pytest on `mindsos:phase26a-test` (canonical phase-confirm GREEN path): **2923 passed, 28 skipped, 0 failed in 31:05** (1865.02s).
* Docker pytest on `tests/phase_26a/`: **20 passed in 0.67s** (17 unit + 3 E2E FalkorDB smoke).
* Cumulative delta vs Phase 25 baseline (2903 passed): +20 tests/phase_26a/.

### Manual smoke (host-native per `feedback_smoke_harness_host_native.md`)

Phase 26a wiring confirmed end-to-end via direct Python invocation of the substrate (`bootstrap_kl_from_falkordb` + `MetagraphLoader.find_by_name` + `MetagraphRepository.persist`):

| Step | Verb | Result |
|---|---|---|
| 1 | `docker compose up -d falkordb` | FalkorDB container reachable |
| 2 | `docker compose exec falkordb redis-cli PING` | PONG |
| 3 | `MATCH (m:Metagraph) RETURN count(m)` | n=0 (clean pre-state) |
| 4 | `bootstrap_kl_from_falkordb(client)` first call | mint path; `metagraph_id = 401ff013-20ab-440d-906f-591974b4ffb4`; `Global name = global_knowledge` |
| 5 | `MATCH (m:Metagraph {name: 'global_knowledge'})` | matches `m.id = 401ff013-...` |
| 5 | `MATCH (g:Graph) RETURN g.role` | 6 role-graphs: concepts, lexicon, ontology, problem-trace, promoted-pipelines, task-patterns |
| 5 | `CALL db.indexes()` | `Metagraph` index shows `[id, name]` — ADR-0123 §am1 operational |
| 6 | `bootstrap_kl_from_falkordb(client)` second call | load path; SAME `metagraph_id = 401ff013-...` (find_by_name resolves correctly) |
| 6 | `MATCH (m:Metagraph) RETURN count(m)` | n=1 (no double-mint; refcount clean) |

Key verifications:
* R5-PB-4 (a) `MetagraphLoader.find_by_name(name)` resolves O(1) via the new index; both inline `{name: $name}` and WHERE-clause Cypher forms confirmed.
* R4-PB-1 (b) + R6-PB-2 (b) `bootstrap_kl_from_falkordb` load-or-mint seam works as designed: first call mints+persists; subsequent calls load.
* R7-F1 MERGE-idempotency of `MetagraphRepository.persist()` confirmed via `test_repository_persist_is_idempotent_across_calls` (no duplicate anchors on re-persist).
* ADR-0123 §am1 `Metagraph.name` index landed in FalkorDB on first connection bootstrap.
* All 6 Graph nodes carry the matching `metagraph_id` FK back to the canonical Global Metagraph.

### Out of scope at 26a (deferred to 26b)

`release propose-for-promotion` + `release ship` CLI verbs are not smoke-tested at 26a — `_build_global_metagraphs(conn)` (`mindsos_cli/commands/server.py:1613`) rebuilds ephemeral in-memory Metagraphs each CLI invocation; rewiring it to use `bootstrap_kl_from_falkordb` is Phase 26b scope per PHASE_MAP §26b step 7. Documented in `notes-phase-26a.md` §6 + `PHASE_26a_DESIGN_LOG.md` §8 as B-26a-T4 candidate carried to Phase 26b. Phase 26a substrate is healthy; Phase 26b orchestration is the next phase's work.

### Hotfix ledger

| ID | Symptom | Fix |
|---|---|---|
| B-26a-T2 | `docker compose run --rm mindsos-test pytest tests/` failed 6 doctor/compose tests asserting `mindsos:phase26a-prod` in `docker-compose.yml` (still had `phase25` literal). Version-bump checklist had a 10th site I missed. | `docker-compose.yml` lines 39, 69: `mindsos:phase25-{prod,test}` → `mindsos:phase26a-{prod,test}`. Memory `feedback_phase_baseline_literal_audit.md` extension. |
| B-26a-T3 | 32 Phase 24 tests failed `TypeError: propose_for_promotion() / release_update() missing 1 required positional argument: 'client'`. Phase 24 tests use SQLite + in-memory only; they don't care about FalkorDB; strict-required signature was over-restrictive. | `client: Optional[Client] = None` default + guard Cypher writes on `if client is not None`. Matches Phase 25 `hard_delete_user(persister: LocalPersister \| None = None)` precedent — backward-compatible additive change; new behavior opt-in via passing live Client. Phase 26a CLI verbs + smoke pass `FalkorClient` explicitly. Applied to `mindsos_admin/promotion.py`, `mindsos_server/release.py` (release_update + _release_update_locked + _copy_role_pending_to_canonical), `mindsos_admin/audit_gate.py`. |

### Round 6 + Round 7 design-log §am-impl picks (reconciled at impl)

4 R7 §am-impl picks + 1 B-26a-T3 §am-impl reconciliation captured in `notes-phase-26a.md` §2. Summary:

* R7-F1 — `MetagraphRepository.persist()` is MERGE-idempotent everywhere (builders.py lines 62/83/100/155/267/343); bootstrap_kl_from_falkordb simplified — no "first bootstrap?" flag needed.
* R7-F2 — `_safe_run` wraps anchor only; lines 153/174/195/228 raw `_client.run_query`. Wrapper catches PersistenceError at v1; raw driver exceptions surface unchanged; documented as known gap.
* R7-F3 — `ImporterProtocol.target_roles: tuple[str, ...]` — no `target_metagraph_id`; CLI envelope owns Metagraph; importer mutates; CLI persists.
* R7-F4 — Importers don't emit audit; step 4 audit-table expectation pinned to "no audit row" per R1-PB-6 (a).
* B-26a-T3 §am-impl — `client` kwarg relaxed Optional-with-default per Phase 25 precedent (notes-phase-26a.md §2 final row).

### Verdict

Phase 26a ships as scoped. Substrate (`bootstrap_kl_from_falkordb` + `MetagraphLoader.find_by_name` + 19th `DEFAULT_INDEXES` entry + §am3 Cypher templates + `client: Client` kwarg cascade + admin importer FalkorDB persist + `_resolve_client()` CLI helper) is correct, live-FalkorDB-validated end-to-end via host smoke. ADR-0118 §am2 + Phase 14a round-3 lock + `mindsos_cli/commands/admin.py` importer-docstring deferral all close at this ship. CLI orchestration of `release propose-for-promotion` + `release ship` deferred to Phase 26b per PHASE_MAP §26b step 7 (B-26a-T4 candidate documented). ADR delta: ADR-0118 §am3 (NEW), ADR-0010 §am2 (NEW), ADR-0123 §am1 (NEW). 8-site version bump +phase25 → +phase26a + docker-compose.yml prod/test image tags. 8 design rounds (R0-R7); 22 picks; 3 structural reversals from substrate probes; 2 hotfixes shipped.

## timestamp_utc

2026-05-24T02:35:25Z

## mkdocs_pages_updated

- (none — `git diff main..HEAD -- docs/` returned no changes)
