# Robot Demo — Status & Entry Points

> This file holds the Robot Demo workstream status that used to live as an inline
> block in `HANDOFF.md` §0. It was relocated here (tracked) so numbered-phase ship
> chats can stage `HANDOFF.md` cleanly without `git add -p` gymnastics. **Keep
> updating this file** for future demo status instead of re-adding an inline
> HANDOFF block.

**Robot Demo workstream (Cowork, 2026-06-05) — new, parallel to the numbered phases.** A demo showcasing MindsOS as a layered robot brain (2 Franka arms + intelligent conveyor + per-arm vertical shelves; carrier-box cooperation; learn→peer-transfer→embodiment-gate→replan). Design is settled at the demo level. **Build status: Phase A SHIPPED 2026-06-07** (real-asset MuJoCo cell — Franka Panda ×2 with suction + Robotiq 2F-85, continuous belt with a geometry-forced unreachable middle, per-arm vertical 3×3 shelves, Box/Sheet/Tube cargo; P2 reach re-validation PASS via pose-reachability IK — 9/9 shelf cells both arms + forced gap; glTF export for browser render). Artifacts in `sim/` + `web/`; record at `confirmation_docs/ROBOT_DEMO_PHASE_A_REACH.md`. Going-in assumption overturned: the top shelf row was *not* the binding reach constraint — see the record.

**UPDATE 2026-06-10 — DM-1 (deployment + bootstrap) code landed.** `demo_backend/` consumer package built (per-device topology: 4 independent MindsOS device-instances — own Global/Local L2 + L3 + L4 each — in one shared-Server process, plan P-1 revised Round-3 + P-8 DeviceProfile/F7). Core sandbox-validated: 4 KLs → 4 CLs (builtin) → 4 IntelligenceLayers → Orchestrator per brain → enqueue→worker-pool→consolidate → **4 Episodes, per-device Local isolation**; `tests_demo/` 4 passed / 1 integration-skip. Build config: `demo` Dockerfile stage (`FROM prod`) + `demo-backend` compose service + `requirements-demo.in` (PB-O — the pyproject extras group was inert under `--no-deps`). Record: `confirmation_docs/ROBOT_DEMO_MINDSOS_DESIGN_LOG.md` (Rounds 2.5–5 pushbacks + §6 build). All demo code consolidated under **`robot_demo/`** (package `robot_demo.backend`; `tests/`, `docs/`, `deploy/`); docs landed. Deploy = compose overlay (`robot_demo/deploy/docker-compose.demo.yml`) reusing the root `falkordb`; the demo owns its Dockerfile (`FROM mindsos:<phase>-prod`, core Dockerfile demo-free). **DM-1 GATE GREEN ON LINUX (2026-06-11):** real 3.12 prod image, real `mindsos_server` bootstrap → 4 device-instances → 4/4 Episodes → idempotent re-boot verified (PB-V: clear stale sessions before login); RAM 38.3 MB, jitter p99 20.08 ms PASS. One live fix (PB-V), zero `mindsos_*` edits. DM-1 is headless; first browser-linked live test is DM-4. **Next: DM-2** (L2 seeds + 3 device-type skill bundles + per-device Falkor persistence).

**UPDATE 2026-06-11 — DM-2 (L2 seeds + device-type bundles + per-device Falkor persistence) code landed.** Additive `robot_demo` only, **zero `mindsos_*` edits**. New: `installers.py` (realm `robot` + 32 §4.0 DataStates, idempotent triple), 5 TOML skill bundles under `robot_demo/bundles/` (`core`→all 4; `manager`/`arm-suction`/`arm-jaw`/`conveyor`→their device; 26 L2 nodes mapped onto the real OWL/FrameNet/WordNet role schemas), `seeds.py` (F4-min embodiment as a `CapacitySnapshot` property-bag per brain — PB-W), `persistence.py` (per-device named Falkor load-or-mint for Globals + G-5 episode round-trip probe), `bundles.py` (P-8 selection); `bootstrap.py` wiring (admin login → per-device `install_skill` through the real ADR-0180 gate → Local seed → Global persist → G-5 probe) + the `reset.py` wipe body. **Grounded constraints (design-log §10):** schema type-registration is enforced even at `strict=False`, bundles seed **nodes only** (no edges) → requirements/affordances are property-encoded (PB-W/PB-DD); Globals persist, Locals stay in-memory + re-seeded (PB-Z). Sandbox (3.10): **13 passed / 3 integration-skipped**. Follow-ups filed: **F8** (real `embodiment` role-graph), **F9** (durable Local persistence + installer re-activation). **DM-2 GATE GREEN ON LINUX (2026-06-11):** first boot empty-start (per-device-type bundles install + Local seeds + G-5 episode `round-tripped intact`), second boot reload (bundles `(no-op)` on the persisted Global), 4/4 Episodes both boots. PB-GG resolved live (install-record dicts round-trip the ADR-0182 codec; schemaless loaded Global accepted); PB-JJ confirmed (demo isolated in the `robot_demo` graph). Two harness fixes landed during the gate: Dockerfile now COPYs `robot_demo/bundles/`; `run_linux_tests.sh` keeps `falkordb` up across both boots. Record: design-log §10–§14. **Next: DM-3** (Seam C body adapter + atomic L3 capacities + live-motion wrapper).

**UPDATE 2026-06-11 — DM-3 (Seam C: body adapter + atomic L3 capacities + live-motion + fault injection) code landed.** Additive `robot_demo` only, **zero `mindsos_*` edits, zero `sim/` edits** (`sim/` wrapped via `sys.path`, PB-QQ). New MuJoCo-free (sandbox-tested) modules: `motion_cache.py` (PB-F trajectory cache + `Trajectory`), `motion_checklist.py` (subset `atomic_checklist` over a frames array + injected `StructureSpec` — PB-LL), `live_motion.py` (cache→gen→checklist→play|honest-dont-know — PB-F), `pose_frame.py` (DM-4 §2.3 interface: 3D `bodies` free match + 2D `x,z` projection — §16), `capacities.py` (10 atomic ⬡ capacities, closure-injected `BodyHandle`, `session=None` Global registration — PB-UU; diagnose Local-gap closure — PB-NN/K). New MuJoCo-host (Linux-gated) modules: `sim_engine.py` (one shared `Cell` + clock thread + per-actuator slots + freeze + scratch-cell generation + extracted belt sweep — PB-KK/SS/VV/PP/RR), `body_adapter.py` (`BodyHandle` + `build_body_runtime`), `dm3_check.py` (the in-container live gate). `bootstrap.py` step-6 hook is **guarded** (skips without MuJoCo → DM-1/DM-2 gate unaffected, PB-TT); Dockerfile COPYs `sim/`+`web/`; `run_linux_tests.sh` adds the DM-3 gate (step 6). **Sandbox: full `robot_demo` suite 23 passed / 6 integration-skipped.** Two reanalysis passes (PB-KK…PB-VV) + the DM-3↔DM-4 pose interface recorded in design-log §15–§17. No new `Fn` (all consumer-side). **DM-3 Linux gate PENDING** the Mac commit (scoped paths only — parked `mindsos_*` work in the tree; never `git add -A`) + Linux `bash robot_demo/deploy/run_linux_tests.sh`. **Next after the gate: DM-4** (Seam B BrainBus + `comms.*` + WS thin UI slice).

**UPDATE 2026-06-10 — Phase B COMPLETE; L4/L5 SHIPPED; MindsOS-integration plan authored.** The 46-clip animation set is done and user-approved (`ROBOT_DEMO_STATE.md` — supersedes the "blocked on honest grasps" paragraph below). MindsOS Phases 46–50 shipped, removing the Phase-48 gate. Decisions 2026-06-10: **one server process / 4 brain-users topology; stub controller dropped (direct-to-MindsOS); Mac Mini + Docker Compose.** The full implementation plan for the MindsOS side of the demo (deployment, comms capacities, L2 seeds, L3 catalog, L4 wiring, L5 chain, gap analysis) is **`confirmation_docs/ROBOT_DEMO_MINDSOS_PLAN.md`** — the current build entry point. Doc updates landed same day in ARCHITECTURE / SCENARIO §4+§6 / PROTOTYPE_PLAN §0 / OPEN_QUESTIONS §1+§3 / DEMO_DERIVED_FEATURES.

**Phase B (motion) — historical status (2026-06-08, since completed):** Six animations now build + render (both arms: place Box / place Tube / stand Panel / load-into-Box), and idle-item float/knock-off bugs are fixed. **But the grasps are visually fake:** the rear-belt reach sags 0.1–0.5 m and is masked by kinematic attach, so on video the object rides off the gripper and the arm is mis-placed at release. Cargo geometry + belt layout decisions were locked this chat (fat tube; wide suction-only panel; box-outer/cargo-inner spacing; side-insert-only placement). **Full state, the root-cause diagnosis, the file map, and the recommended next scope are in `confirmation_docs/ROBOT_DEMO_MOTION_STATE.md` — read it first for any `sim/` motion work.** Next: make the grasp physically real (likely a belt/pickup geometry rework), one problem at a time. Other Phase B entry points:
- `confirmation_docs/ROBOT_DEMO_SCENARIO.md` — the scenario + contracts (read **§0a Canonical model update** first; it supersedes drifted earlier sections).
- `confirmation_docs/ROBOT_DEMO_PROTOTYPE_PLAN.md` — build plan (Phases A–F; everything except MindsOS = stub controller).
- `confirmation_docs/ROBOT_DEMO_OPEN_QUESTIONS.md` — all pushbacks/decisions tagged resolved/open (see §7 locks + §8 round-2).
- `confirmation_docs/ROBOT_DEMO_ARCHITECTURE.md` — client–server architecture (non-developer).
- `confirmation_docs/DEMO_DERIVED_FEATURES_NEXT_CHAT_PROMPT.md` — **net-new MindsOS features F1–F6 invented by the demo, needing future design** (peer Local↔Local learning, teachable vocabulary, curation governance, body-model affordance graph, Pipeline/Plan split, box-as-resource).
- `demo_ui/` — frozen **v9** presentation UI (`presentation_mockup.html`) + the active **v10 dashboard** (`presentation_v10.html` + sidecars + reference maps + `maps/` regen scripts). **v10 design record + backlog: `confirmation_docs/ROBOT_DEMO_UI_V10.md`; next-chat prompt: `confirmation_docs/ROBOT_DEMO_UI_NEXT_CHAT_PROMPT.md`.** (demo_ui `CHANGELOG.md` + `versions/` removed 2026-06-11 — history folded into the UI-V10 record.) `prototype_zero/` — validated stick-arm MuJoCo cell + reach proof.
- To start the build: `confirmation_docs/ROBOT_DEMO_BUILD_NEXT_CHAT_PROMPT.md`.
