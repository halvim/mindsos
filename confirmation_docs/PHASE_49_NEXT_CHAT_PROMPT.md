You are the Phase 49 chat: Integration C — the first end-to-end L0→L5 trivial-task scenario + the `usage/cookbook/end-to-end.md` cookbook page + the Falkor index-strategy decision (PB-HHH). Phases 39–48 are all SHIPPED + confirmed; Phase 48 (L5 v1) closed the convergence — consolidation, Episode/Memory authoring, the S12 write-half, dream driver, D'1 retention, crash recovery, and monitoring are all in. Phase 49 is the closing integration phase of the post-Phase-38 plan: it does NOT add new feature surface — it wires the existing L0–L5 pieces into one end-to-end exercise, writes the cookbook, and decides the Falkor index strategy. Treat it as a design-pass first, then ship; do NOT assume the ship shape — propose it as a pushback as prior phases did.

YOUR OPERATING SPEC IS THE FILES BELOW. Read them in full before acting; this prompt only routes you — it deliberately does not restate what the files say.

READ FIRST (entry + process)
1. `HANDOFF.md` — §1 (orientation); §3.1.21 (Phase 48 ship — what L5 v1 shipped + the consume surface + the deferrals you must NOT assume are present); §3.1.20 (Phase 47 orchestrator) + §3.1.19 (Phase 46 substrate — the L4 pieces you drive end-to-end); §2.5 (Integration A/B precedent — the scenario-test shape Integration C follows); §3.1.7 (Chat C plan + the Phase 49 placement); §4/§4.1/§4.2 (L5 settled — what an Episode is, retention); §9 (process discipline + the ship-env invariants: no `gh` and no `mindsos` CLI on the Linux gate host → hand-write the confirmation doc; `python3` not `python`; BSD sed → perl; watch CI on the GitHub Actions web page; pair-execution Cowork↔Mac↔Linux; squash-before-confirm; tag at the confirm-artifacts commit; bump the manifest `phase` field alongside `version`); §10 (the "Phase 49 chat (Integration C)" required-reading row + companion-doc list).
2. `confirmation_docs/POST_PHASE_38_PHASE_MAP.md` — §0, §1 (DAG + the high-water version-bump rule + the **effectively-10-surface** bump checklist incl. the manifest `phase` field), the **Phase 49 detail row** (the exact L0→L5 integration scenario step-list + the cookbook page + the PB-HHH Falkor-index decision + pass criteria + tests), and §6 (the post-49 downstream chat sequencing — SKILL_ACQUISITION_PROCESS / WSD / FOL / DWF / code-skill / adapter / maintenance / L4-v2 — so you place Integration C as the LAST numbered phase and hand off correctly).
3. `confirmation_docs/PHASE_48_CONFIRMED.md` + `PHASE_48_DESIGN_LOG.md` — the authoritative record of what L5 v1 shipped and, critically, the **as-shipped deferrals** Integration C must design around (do NOT assume these exist): faithful episode→MM reconstruction + `replay_recorded`-vs-`re_execute_capacities` differentiation + real ALS signal firing are **WSD-gated**; the S7 retention read-consumer is unit-test-only; the transitional `invoke` union + `capacity_layer.invoke` read-path dict are still in place (A1′); retention policy + partial-MM crash-content recovery + a durable Falkor checkpoint store are v1.5.
4. `confirmation_docs/PHASE_47_CONFIRMED.md` + `PHASE_46_CONFIRMED.md` — the orchestrator + substrate surfaces the scenario drives (`IntelligenceLayer` lifecycle, `L4Dispatcher`, the six-phase `run_lifecycle`, the v0 catalogs).

DESIGN GROUND TRUTH (implement; do not re-litigate)
- The Phase 49 row in `POST_PHASE_38_PHASE_MAP.md` is the scenario spec (the L0 login → L4 enqueue → six-phase lifecycle over `planning.*` v0 → L3 `text.tokenize` → mutation-discipline-respecting write → MM write-through → L5 consolidation → Episode in `episodic_memories` → Falkor persister flush → background dream step). Build the integration test to that step-list; do not invent a different scenario.
- Trivial-task scope only (PB-V / Chat C): the feature-complete NLU/skill demo waits for the WSD installation chat. Integration C exercises the substrate end-to-end on a trivial task, not real cognition.
- The `text-realm.md` cookbook (Phase 38, read-side) is the precedent for `usage/cookbook/end-to-end.md`; `_workbench/cookbook_routing.md` records that `end-to-end.md` is owned by Phase 49.
- `PB-HHH` (Falkor index strategy) is explicitly a **Phase 49 R0 decision** — surface it as a pushback with options; the PHASE_MAP §6/open-questions and `_workbench/L*_FUTURE_WORK.md` carry the prior framing.

PREREQ CHECK (run before branching)
- `git tag --list | grep -E "phase-4[0-8]-confirmed"` — 40–48 all present (48 is the new one; `main` tip is the Phase-48 confirm commit `1952260`, tag `phase-48-confirmed`). Branch `phase-49` off `phase-48-confirmed`.
- **VERSION BUMP REQUIRED:** slot 49 > high-water 48 → full bump 48→49 across all surfaces — confirm the exact set from `PHASE_48_CONFIRMED.md` tester_notes / the PHASE_MAP §1 checklist (8 package `__version__` + pyproject + manifest `version` AND manifest `phase` + 2 docker-compose tags + the export-slate assertions). No new top-level package expected → no new-package checklist.
- Working tree has long-standing untracked items (Robot Demo corpus `confirmation_docs/ROBOT_DEMO_*` + `demo_ui/`/`prototype_zero/`/`sim/`/`web/`, and a modified `docs/future_work/L3_FUTURE_WORK.md`) — **never `git add -A`; stage selectively; leave those alone** (every prior phase did).
- Diff-read the modules the scenario touches before locking picks (the Phase-48 `consolidation.py`/`crash_recovery.py`/`dream_cycle.py`/`monitoring.py`, the orchestrator, the L0 `FalkorDBLocalPersister`, the `episodic_memories` schema).

INHERITED LESSONS (apply, don't re-derive)
- Probe-first: read the real shipped signatures + the live `episodic_memories` schema + the FalkorDBLocalPersister surface before locking picks (every Phase 39–48 chat caught a scope reality this way).
- Ground every surface against its real consumer; defer absent-consumer surfaces (the WSD-gated dream/ALS/reconstruction pieces are NOT yours to build — Integration C drives only what shipped).
- Pair-execution: Cowork prepares file content; the user runs git on the Mac; Linux runs the docker cumulative gate. One tagged command-group at a time with expected output; lead git boxes with `rm -f .git/index.lock`; no `cd` and no comments inside the command boxes (user preference). Squash-merge to `main` BEFORE confirm; hand-write `PHASE_49_CONFIRMED.md` via heredoc (no `mindsos` CLI on the gate host); tag `phase-49-confirmed` at the confirm-artifacts commit; `release.yml` requires the confirmation doc present at the tagged commit.
- Integration phases run real Falkor: unlike the in-memory unit tests, the Integration C scenario should exercise the FalkorDBLocalPersister round-trip (the `docker compose` Falkor service) — confirm whether the scenario test needs the live Falkor container and design the gate accordingly.

OPERATING MODE
"Before proceeding with any implementation, reanalyze the plan and list your pushbacks with options, then show me your choice. Any decision can change as we go." Skeptical review each round, terse, options + your pick, pause for authorization before impl.

FIRST ACTION: run the prereq check; ack the required-reading; open `confirmation_docs/PHASE_49_DESIGN_LOG.md` R0 (mirror the prior phases' S-surface format) — enumerate the integration surfaces from the PHASE_MAP Phase 49 row + the cookbook page + the version-bump surfaces; surface the genuine design forks as pushbacks with options (at minimum: PB-HHH Falkor index strategy; whether the end-to-end scenario test runs against live Falkor or the in-memory KL; how much of the dream/ALS background step is exercisable given the WSD-gated deferrals; the cookbook's scope vs the text-realm precedent); then run the pre-impl pushback rounds before branching `phase-49`.
