# Phase 10 — Confirmation

> Generated by `mindsos confirm-phase --phase 10 --notes-file …`. Tester
> may hand-edit before commit. CI's smoke check verifies "exists and
> non-empty", not field structure.

---

## phase_number

10

## phase_title

The phase title as it appears in `confirmation_docs/PHASE_MAP.md` §3 / §4 / §5.
Example: `Tooling infrastructure`

L1 Snapshot + soft-delete substrate + RemovalImpact + XRef setters

## git_sha

ea03a8a7f1340a416ee4eb30d68a3ae85bde5db4

## image_build_hash

sha256:1c940454e815d66ae173edac043e8b7b0584beba8803df4a9b63dcc480002ad5

## falkordb_version

falkordb/falkordb:v4.18.3@sha256:30c530c193ac48cb6ea8c6cae745f793d2c098a0a138f7b3e46c1d90848845ba

## automated_test_summary

- count: 0
- passed: 0
- skipped: 0
- failed: 0
- suite_hash: sha256:877a590e57fdc6ef60a05cfe2d92bc1d053b6c61222be3235c5f04148cb2db69
- pytest_summary: no pytest summary line found

## tester_notes

Free-form. What you observed, anything surprising, deviations from PHASE_MAP's
pass criterion, open questions for the next phase chat. This is the
load-bearing field — read by future phase chats per PHASE_MAP §0.

## Smoke tests (manual, in-container per `feedback_docker_compose_invocation.md`)

* **Box 01 — doctor self-test + 3-package version parity**
  * Command: `docker compose run --rm mindsos doctor --self-test`
  * Expected: exit 0; `mindsos_core` / `mindsos_cli` / `mindsos_instances`
    all at `0.0.0+phase10`; `manifest.toml [mindsos] phase = "10"`.
  * Result: PASS.

* **Box 02 — `xref-list` 10-field JSON (M24 + RR-6)**
  * Seeded `smoke-xref-list` metagraph with one XRef carrying
    `target_stale=True` + `deprecated_at` non-None.
  * Command: `docker compose run --rm mindsos persistence xref-list
    --metagraph smoke-xref-list --json`
  * Expected: 10-key dict including `target_stale: true` and
    `deprecated_at: "2026-..."`; Rich-table form grows 2 extra columns
    when non-default.
  * Result: PASS — JSON shape matches design lock M24.

* **Box 03 — soft-delete setter quartet smoke**
  * Programmatic check: `g.deprecate_edge` returns mutated Edge with
    timezone-aware `deprecated_at`; `g.undeprecate_edge` clears;
    iterator filter (`include_deprecated=False` default) hides the
    deprecated edge.
  * Result: PASS — `soft-delete quartet + filter: OK`.

* **Box 04 — `remove_graph` matrix smoke (P67 + P75 + P81)**
  * Cell 1: `force=False` + dangling XRef → `RemoveGraphBlockedError`
    with `blocked_reason=DANGLING_REFS`. State unchanged.
  * Cell 2: `force=True` → `proceeded=True`; `target_stale=True` stamped
    on incoming XRef.
  * Cell 3 (P81): `cascade=False` + `force=True` + incident MetaEdge →
    STILL raises with `INCIDENT_META_EDGES_CASCADE_FALSE`. `force` does
    NOT override the cascade gate.
  * Result: PASS — all 3 cells green per design lock.

* **Box 05 — `MetagraphSnapshot` identity preservation (ADR-0027)**
  * `id(mg)`, `id(mg.identity)`, `id(g_survivor)` preserved across
    `of(mg)` → mutate → `restore_into(mg)`. Property bags restored on
    Metagraph + Graph scope. Added-after-snapshot graph dropped on
    restore.
  * Result: PASS — `snapshot identity-preserve + state-restore: OK`.

* **Box 06 — state-file v=5 round-trip (programmatic)**
  * `_graph_to_state` writes `_state_version: 5` + `deprecated_at`
    ISO-8601 string per edge; `_state_to_graph` restores
    `Edge.deprecated_at` back to `datetime`.
  * Result: PASS — `state-file v=5 round-trip: OK`.

## Cumulative test sweep

* docker compose run --rm mindsos-test pytest tests/phase_10/ --tb=line → 162 passed in <X>s
* docker compose run --rm mindsos-test pytest tests/ --tb=line → 1660 passed, 2 skipped in ~17 min

* Phase 10 isolated (`tests/phase_10/`): **162** tests (145 unit + 17
  integration including 3 diagnostic per B-10-T2). All pass with
  FalkorDB sidecar.
* Cumulative (`tests/`): **1660** passed; 2 pre-existing skips carried
  from Phase 09; 0 failures across phases 02-10.

## Hotfix ledger (B-10-T*)

* **B-10-T1** — 6 integration test failures: Phase 10 XRef integration
  tests must call `XRefLoader(client).load_into(mg)` explicitly after
  `MetagraphLoader.load` (Phase 09 idiom; the `after_load` observer
  chain requires `attach_xref_loader` to be subscribed first, which
  doesn't happen on the fresh Metagraph constructed inside
  `MetagraphLoader.load`). Also 2 WAL recovery tests used raw-Cypher
  `WALEntry` writes with wrong field names; fixed B-10-T2.

* **B-10-T2** — WAL recovery integration tests switched to
  `WriteAheadLog.begin()` directly (Phase 09
  `test_xref_wal_recovery.py` pattern) instead of raw Cypher. NEW
  `tests/phase_10/test_zzz_diagnostic_xref_persist.py` (3 diagnostic
  tests prefixed `zzz` so they run last) to localize any future
  persist/load defect to a specific pipeline stage (add_xref → persist
  → DB row; SET target_stale → DB; XRefLoader source_metagraph_id
  query). Caching of the Docker test image is the failure mode behind
  the iteration loop on this hotfix — must rebuild after every code
  change.

* **B-10-T3** — 15 prior-phase tests asserted Phase-09-era literals
  invalidated by Phase 10's state-file v=4 → v=5 bump + XRef P53
  reversal + M5 soft-delete fields on Phase 05a Meta(Hyper)Edge.
  Patched 6 files to dynamic CURRENT_VERSION + inverted negative-shape
  assertions to positive (Phase 10 supersedes Phase 09 P53 strip + Phase
  05a P1 soft-delete strip). Audit class is the standing
  `feedback_phase_baseline_literal_audit.md` pattern (Phase 09 B-09-T3
  / T6 / T7 precedent).

## New feedback memories filed this cycle

* `feedback_test_order_current_then_cumulative.md` — Run
  `tests/phase_NN/` to green BEFORE running the cumulative `tests/`
  sweep across all phases. Catches Phase-NN defects in isolation;
  cumulative output buries them.

## In-flight pushbacks resolved (P67-P88)

22 pushbacks total:
* **Pre-impl (P67-P78):** cascade restore (P67); P10+P11 filter merge
  (P68); ADR-0130 snapshot-preservation caveat (P69); raise-on-block
  confirmed (P70); probe-8 pre-verify (P71); SoftDeleteKind enum (P72);
  per-method builders confirmed (P73); strip `DeprecatedFilterPendingWarning`
  (P74); unified `BlockedReason` enum (P75); no CLI verbs in P10 (P76);
  ADR-0135 amendment rewrite (P77); ADR-0133 flip + housekeeping (P78).
* **During impl (P79-P88):** `_soft_delete_dirty` declared early per
  ordering (P79); iterator filter via Phase 09 builder edits (P80);
  cascade gate independent of force (P81 v3 verbatim); Graph iterators
  ship (P82); IntergraphEdge out of M5 (P83); M3 allow-list extended
  for halvim primitives (P84); `Graph.properties` backfill (P85);
  Graph-side dirty bucket split (P86); loader full integration (P87);
  WAL payload schema extended with `scope_id` (P88).

## ADR ledger (chunk-10 commit at project-root per RPB-7)

* **ADR-0027** §Revisions amendment-1 — covered fields + identity-rebuild
  + dirty-set capture.
* **ADR-0128** §Revisions amendment-3 — XRef cleanup setter exists;
  firing trigger Server-phase per O1.
* **ADR-0130** Graph-side §Acceptance flip with P69
  snapshot-preservation-basis caveat.
* **ADR-0133** Proposed → **Accepted** + §Revisions amendment-1
  (substrate + filter ship together per P68 merge; D1-rev strips
  compositional clause; class retained per ADR-0148).
* **ADR-0135** Proposed → **Accepted** + §Revisions amendments 1-3
  (cascade default flip + raise-on-block + unified blocked_reason enum).

## Carry-forward (out of Phase 10 scope)

* ADR-0128 stays Proposed until Phase 14 (`MetagraphView.follow_ref`
  consumer).
* ADR-0129 CI lint rule deferred Phase 18+.
* Soft-delete CLI verbs deferred per P76.
* IntergraphEdge / IntergraphHyperEdge soft-delete deferred per P83.
* Server first-start `mark_xref_stale` auto-trigger Phase 18+ per O1.
* L3 capacities producing XRefs Phase 33+ per ADR-0145.

## timestamp_utc

2026-05-16T20:01:06Z

## mkdocs_pages_updated

- docs/api/core/client.md
- docs/api/core/integrity.md
- docs/api/core/loaders.md
- docs/api/core/metagraph-snapshot.md
- docs/api/core/metagraph.md
- docs/api/core/repositories.md
- docs/api/core/soft-delete.md
- docs/api/core/wal.md
- docs/api/core/xref.md
- docs/changelog/CHANGELOG.md
- docs/concepts/references.md
- docs/concepts/soft-delete.md
- docs/dev/internals/core.md
- docs/dev/internals/snapshots.md
- docs/dev/repo-layout.md
- docs/usage/core/persistence.md
