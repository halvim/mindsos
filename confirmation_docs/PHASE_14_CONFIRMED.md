# Phase 14 — Confirmation

> Generated by `mindsos confirm-phase --phase 14 --notes-file …`. Tester
> may hand-edit before commit. CI's smoke check verifies "exists and
> non-empty", not field structure.

---

## phase_number

14

## phase_title

The phase title as it appears in `confirmation_docs/PHASE_MAP.md` §3 / §4 / §5.
Example: `Tooling infrastructure`

L2 KnowledgeLayer + role-graph bootstrap (Global + Local) + MetagraphView (read-only)

## git_sha

bc6baa426aa25b0835941970f3b92761c6da6793

## image_build_hash

sha256:cfe82be4d20f6c1f66a06789e8b434dde4f3ef1f52c683cc9eaecd9714328a7e

## falkordb_version

falkordb/falkordb:v4.18.3@sha256:30c530c193ac48cb6ea8c6cae745f793d2c098a0a138f7b3e46c1d90848845ba

## automated_test_summary

- count: 2162
- passed: 2148
- skipped: 14
- failed: 0
- suite_hash: sha256:b10713892f27a1cb6dcb08e3110eba2c11dcb63fdf57427b63c74c5d5c4c40fc
- pytest_summary: 2148 passed, 14 skipped, 109 warnings in 1110.02s (0:18:30)

## tester_notes

Free-form. What you observed, anything surprising, deviations from PHASE_MAP's
pass criterion, open questions for the next phase chat. This is the
load-bearing field — read by future phase chats per PHASE_MAP §0.

SMOKE LEDGER (Phase 14 — L2 KnowledgeLayer + role-graph bootstrap + MetagraphView):

1. `mindsos doctor` → 4-package version-string parity at `0.0.0+phase14`
   (mindsos_core / mindsos_cli / mindsos_instances / mindsos_knowledge);
   FalkorDB pin matches manifest; image tag `mindsos:phase14-prod`. ✓
2. `mindsos doctor --self-test` exit 0. ✓
3. `pytest tests/phase_14 -v` — isolated suite green (~96 passed / 4 skipped
   in container; the 4 skips are `test_adr_amendment_sentinels.py` — ADR
   files live in parent project tree per Model C). ✓
4. `pytest tests/` — cumulative sweep: **2148 passed / 14 skipped /
   109 warnings in 18:26**. No regressions vs Phase 13 baseline
   (2027 / 10 skipped / 18:27); +121 new passing tests + 4 new skips
   for Phase 14. ✓
5. KL python smoke — `KnowledgeLayer.bootstrap()` → Global has 6 named
   role-graphs (`concepts`, `lexicon`, `ontology`, `problem-trace`,
   `promoted-pipelines`, `task-patterns`); lazy `local_metagraph("alice")`
   → Local has 2 named role-graphs (`capacity-state`, `memories`) per
   ADR-0044. No alignment pair-graphs at bootstrap (importer-driven per
   Phase 15). ✓
6. Install/extract round-trip — `install_local_metagraph` + `extract_local_
   metagraph` preserves object identity per ADR-0042 §Consequences;
   `AlreadyInstalledError` on double-install; `NotInstalledError` on
   extract-without-install. ✓
7. Phase 12 regression — `mindsos knowledge iri build --role memories
   --kind memory --version v1 --user-id alice --memory-id m1` →
   `memories-v1:memory:alice:m1`. ✓
8. Phase 13 regression — `mindsos knowledge schema show --role ontology
   --json` returns valid JSON; dispatch table unmutated. ✓
9. Alignment-prefix Global-only (ADR-0150 §amendment-1) — Global accepts
   `alignment:ontology:lexicon`; reverse-pair `lexicon:ontology` is a
   distinct (None) lookup; ordered semantics preserved. ✓
10. Scope rejection at runtime — `ensure_global_role_graph("memories")`
    raises `KnowledgeError` ("Local-scoped"); `ensure_local_role_graph
    ("ontology")` raises ("Global-scoped"); `ensure_local_role_graph
    ("alignment:a:b")` raises ("Global-only"). ✓
11. `MetagraphView.step()` returns incident edges within-view only (no
    Local-overlay per PB-10); ADR-0133 `include_deprecated=False`
    default filters soft-deleted edges; `include_deprecated=True`
    re-surfaces them. ✓
12. CLI surface unchanged — `mindsos --help` + `mindsos knowledge --help`
    show no new Phase 14 verbs (PB-13 lock — KL is in-memory only;
    state-file CLI deferred to Phase 26). ✓

DOCS:
- `docs/concepts/global-local.md` NEW (Bootstrap-stage owner per Phase 14a
  synthesis); `last_confirmed_phase: 14`.
- `docs/concepts/knowledge-lifecycle.md` Phase 14 row Status `planned →
  shipped`; front-matter `last_confirmed_phase: 14a → 14`.
- `docs/usage/knowledge/overview.md` amended with KL bootstrap section.
- `CHANGELOG.md` Phase 14 entry.
- `mkdocs.yml` adds `Concepts > Knowledge lifecycle > Global + Local
  metagraphs (Phase 14)`.

ADR AMENDMENTS:
- ADR-0042 §amendment-1 — Global lifecycle via constructor parameter
  (Phase 14 PB-7); file at parent project root per Model C.
- ADR-0150 §amendment-1 — alignment role is Global-only at v1
  (Phase 14 PB-8); file at parent project root per Model C.
The 4 in-container test skips assert these ADR amendments exist; skipped
because parent project tree isn't COPYd into the runtime image.

DESIGN LOG: see `confirmation_docs/PHASE_14_DESIGN_LOG.md` §1 for the
full PB-1..16 ledger across 3 rounds, all user-agreed. Mid-flight
calibrations: PB-12 re-classified the row as "Net-new? Mostly yes"
(no v3 `KnowledgeLayer` Python source existed to repackage); one
test-API fix (Graph.add_node `type_` → `type_name`; EdgeType `name`
only; build_alignment_schema's `extra_edge_types` is `tuple[str, ...]`
not EdgeType objects).

CARRY-FORWARD TO PHASE 15: per-edge alignment-anchor IRI builder
(3rd hop) + MetagraphSchema scanner (4th hop) — both have Phase 15's
Alignments / Importers as their first concrete consumer per PB-1.

## timestamp_utc

2026-05-20T01:38:23Z

## mkdocs_pages_updated

- docs/api/core/client.md
- docs/api/core/integrity.md
- docs/api/core/loaders.md
- docs/api/core/metagraph-snapshot.md
- docs/api/core/metagraph.md
- docs/api/core/repositories.md
- docs/api/core/soft-delete.md
- docs/api/core/wal.md
- docs/api/core/xref.md
- docs/api/knowledge/identifiers.md
- docs/api/knowledge/ref-types.md
- docs/changelog/CHANGELOG.md
- docs/concepts/admin-global-shipping.md
- docs/concepts/global-local.md
- docs/concepts/identifiers.md
- docs/concepts/identity.md
- docs/concepts/knowledge-lifecycle.md
- docs/concepts/promotion-bridge.md
- docs/concepts/references.md
- docs/concepts/soft-delete.md
- docs/concepts/user-local-authoring.md
- docs/dev/internals/core.md
- docs/dev/internals/snapshots.md
- docs/dev/migration-playbook.md
- docs/dev/repo-layout.md
- docs/usage/core/persistence.md
- docs/usage/knowledge/alignment.md
- docs/usage/knowledge/capacity-state.md
- docs/usage/knowledge/concepts.md
- docs/usage/knowledge/iri-cli.md
- docs/usage/knowledge/lexicon.md
- docs/usage/knowledge/memories.md
- docs/usage/knowledge/ontology.md
- docs/usage/knowledge/overview.md
- docs/usage/knowledge/problem-trace.md
- docs/usage/knowledge/promoted-pipelines.md
- docs/usage/knowledge/task-patterns.md
