"""MindsOS CLI — Phase 04-v2 L1 Schema (NodeType / EdgeType / HyperEdgeType + opt-in strict + attach to graph + schema-validated property updates + cumulative state-file migration v=2→v=3)."""

__version__ = "0.0.0+phase04.v2"
